# Claude Code Harness — Plans.md

最終アーカイブ: 2026-05-29（Phase 63/64/66-71/73-76/78/79 → `.claude/memory/archive/Plans-2026-05-29-phase63-79.md`）
前回アーカイブ: 2026-05-08（Phase 47-61 → `.claude/memory/archive/Plans-2026-05-08-phase47-61.md`）

---

## 📦 アーカイブ

完了済み Phase は以下のファイルへ切り出し済み（git history にも残存）:

- [Phase 63/64/66-71/73-76/78/79](.claude/memory/archive/Plans-2026-05-29-phase63-79.md) — stale harness-mem 参照整理 / Plans archive-aware / 3-surface HTML cross-project safety 関連 / Open Issue closeout / Codex 0.130 / harness-review TeamAgent + lightweight / Hokage Core boundary / R03 break-glass / Superpowers tool-first onboarding / repo-health gates / README front door / spec.md+Plans.md co-required / Dependabot benchmark / harness-plan team gates
- [Phase 47-61](.claude/memory/archive/Plans-2026-05-08-phase47-61.md) — Session Monitor 能動監視 / XR-003 / 3-state 依存テスト規約 / CC 2.1.112-2.1.126 + Codex 0.121-0.128 upstream 追従 / Issue #105 English default + Japanese opt-in / External Issue closeout / Skill orchestration design contract / harness-mem managed companion (v4.6.0-v4.7.0) / Sandbagging-Aware Weak-Supervision Harness
- [Phase 44 + 45 + 46](.claude/memory/archive/Plans-2026-04-19-phase44-46.md) — Opus 4.7 / CC 2.1.99-110 追従 "Arcana" (v4.2.0) + Plugin Manifest 公式準拠 + Worker 3 層防御 (#84-#87, v4.3.0)
- [Phase 37 + 41 + 42 + 43](.claude/memory/archive/Plans-2026-04-17-phase37-41-42-43.md) — Hokage 完全体 / Long-Running Harness / Go hot-path migration / Advisor Strategy
- [Phase 39 + 40 + 41.0](.claude/memory/archive/Plans-2026-04-15-phase39-40-41.0.md) — レビュー体験改善 / Migration Residue Scanner / Long-Running Harness Spike

---

## マーカー凡例

PM ↔ Impl 運用で使用する標準マーカー:

| マーカー | 意味 | 誰が付ける |
|---------|------|-----------|
| `pm:requested` / `pm:依頼中` | PM がタスクを起票し、Impl へ依頼中 | PM |
| `cc:todo` / `cc:TODO` | Impl の未着手タスク | Impl |
| `cc:wip` / `cc:WIP` | Impl（Claude Code）が着手中 | Impl |
| `cc:done` / `cc:完了` | Impl が作業完了し、PM の確認待ち | Impl |
| `pm:approved` / `pm:確認済` | PM が最終確認を完了 | PM |

**状態遷移**: 新規・更新時の正規出力は `pm:requested → cc:todo → cc:wip → cc:done → pm:approved`。既存 `pm:依頼中 → cc:TODO → cc:WIP → cc:完了 → pm:確認済` も read-compatible。

**後方互換**: `cursor:依頼中` / `cursor:確認済` は `pm:依頼中` / `pm:確認済` の同義として扱う（Cursor PM 運用時の表記）。

---

## Phase 62: Claude Code 2.1.112-2.1.132 後続活用 + Opus 4.7 upstream follow-up [P1]

Purpose: Phase 56 / Phase 58 で Claude Code `2.1.119`-`2.1.126` を Plans 化し一部 `cc:完了` になっているが、`2.1.112`-`2.1.118` および `2.1.127`-`2.1.132` の 13 バージョンは Feature Table の詳細追跡対象外。CC GitHub CHANGELOG (`https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`) との比較で、Harness の弱点を直接強化できる Tier 1 5 件 (subagent stall, 1h cache, mcp_tool hooks, sandbox deniedDomains, wrapper bypass) と Phase 58 設計済み実装 pending の Tier 2 5 件 (updatedToolOutput, permissionMode, invocation_trigger, SESSION_ID, skillOverrides) を実装 + test 込みで Plans 化する。`62.3.1` で Phase 58 と同じ 4 点接続 (snapshot / Feature Table / CHANGELOG / integration test) を行い、CC 自動継承で済む UI 改善・性能改善・OAuth fix は明示的に対象外と記録する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 62.1.1 | Claude Code `2.1.113` で導入された subagent stall 10 分 timeout を Worker 契約に組み込む。`agents/worker.md` の retry / escalation 節に CC 側 stall timeout (既定 600 秒) と Harness 側 `elicitation-handler.sh` の能動監視を 2 層防御として併記し、Lead が再 spawn する条件を「`cc:WIP` 状態が 10 分超 または CC stall log を観測」と数値で固定。`docs/team-composition.md` の並列 Worker 数判定 (1/2/3 グループ) に stall 発生時の re-spawn ルールを 1 行追記。`tests/test-claude-upstream-integration.sh` に Phase 62.1.1 の文字列参照を追加し、`.claude/rules/opus-4-7-prompt-audit.md` の数字必須要件 (回数上限・時間上限) を満たすこと | (a) `agents/worker.md` に CC 600s stall + Harness elicitation 2 層防御が明記され `grep -E "600\|10 分\|stall" agents/worker.md` で 1 件以上ヒット、(b) `docs/team-composition.md` に re-spawn 判定条件が「N 分超」「stall log 検出」の数値で書かれる、(c) `tests/test-claude-upstream-integration.sh` が `Phase 62.1.1` を参照検出、(d) `.claude/rules/opus-4-7-prompt-audit.md` の checklist で「回数 / 時間が数字」項目を満たす、(e) `./tests/validate-plugin.sh` 全 PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.2 | Feature Table line 198 で `A: 明示追従対象` 分類済みだが未実装の `ENABLE_PROMPT_CACHING_1H` (CC `2.1.108`) を long-running skill で活用する。`skills/harness-loop/SKILL.md`, `skills/breezing/SKILL.md`, `docs/long-running-harness.md` の 3 箇所に「セッション開始時に `export ENABLE_PROMPT_CACHING_1H=1` を行う」ガイダンスと理由 (5 分 TTL では loop / breezing 1 時間超セッションでキャッシュ miss が累積し input token コストが最大 12 倍になりうる) を 1 段落で追加。`scripts/codex-companion.sh task --write --resume-last` 系の long task で env が inherit されるかを確認し、wrapper の修正が不要であれば理由を docs に残す。skill mirror (`skills-codex/`, `codex/.codex/skills/`, `opencode/skills/`) も sync で追従 | (a) `harness-loop` / `breezing` / `long-running-harness.md` の 3 箇所に env 設定例と理由が記載され `rg "ENABLE_PROMPT_CACHING_1H"` で 3 件以上ヒット、(b) `scripts/codex-companion.sh` の env inherit 状況が `docs/long-running-harness.md` または `docs/upstream-update-snapshot-2026-05-07.md` に記録、(c) `tests/test-claude-upstream-integration.sh` が `ENABLE_PROMPT_CACHING_1H` の参照を検出、(d) `bash scripts/sync-skill-mirrors.sh --check` PASS、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.3 | Claude Code `2.1.118` で hooks が `type: "mcp_tool"` で MCP ツールを直接呼び出せるようになった件の採用判断 doc を作成する。実装は次 Phase 判断 (まず設計のみ)。判断材料: (i) PostToolUse から `harness_mem_record_event` を直接呼ぶ場合の遅延 / auth scope / error 伝播の比較、(ii) 現行 `scripts/hook-handlers/*.sh` shell wrapper 経由 vs 直接呼び出しのトレードオフ表、(iii) MCP server 不達時の fallback (silent skip / local ledger queue / drop) の方針、(iv) Phase 61 の `.claude/state/elicitation/events.jsonl` ローカル ledger との関係 | (a) `docs/hooks-mcp-tool-evaluation.md` がある、(b) shell wrapper 経由 vs `type: "mcp_tool"` 直接呼び出しの比較表 (latency / auth / error / fallback の 4 項目) がある、(c) MCP 不達時の fallback 方針が `silent skip` `queue` `drop` のどれかに明示確定、(d) Phase 61 ledger との重複・補完関係が 1 段落で説明、(e) 採用 / 保留 / 却下のいずれかで判断結論が記録、(f) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.4 | Claude Code `2.1.113` で追加された `sandbox.network.deniedDomains` 設定の baseline を Harness テンプレートに導入する。`.claude-plugin/settings.json` の `permissions` に `deniedDomains` baseline (`pastebin.com`, `transfer.sh`, `0x0.st`, `paste.ee`, `gist.github.com` 経由 anonymous gist の制限は対象外で記録のみ) を追加。`harness-init` skill 経由で生成される settings.json に同じ既定値が入ることを確認。`settings.local.json` で project 固有 override を許可。`scripts/ci/check-consistency.sh` で deniedDomains baseline 漏れを検出する | (a) `.claude-plugin/settings.json` の `permissions` ブロックに `deniedDomains` baseline が 4 件以上記載、(b) `skills/harness-init` または同等の生成経路で同じ既定値を出力、(c) `tests/test-settings-baseline.sh` (新規 or 既存拡張) が baseline 既定値の存在を機械検証、(d) `scripts/ci/check-consistency.sh` の本線で deniedDomains 欠落を検出、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] (template canonical, `.claude-plugin/settings.json` sync は user 手動操作) |
| 62.1.5 | Claude Code `2.1.113` で deny ルールが `env`/`sudo`/`watch` wrapper bypass を matching するように強化された件を Harness の Go guardrail で audit する。R06 (force-push), R11 (`reset --hard`), R12 (push to main) の 3 ルールについて、`env GIT_SSL_NO_VERIFY=1 git push --force` / `sudo git push --force` / `watch -n 1 'git push --force'` のような wrapper bypass test を `go/internal/guardrail/rules_test.go` に各 3 ケース合計 9 ケース追加。漏れがあれば `go/internal/guardrail/rules.go` の matcher を強化、無ければ test だけ追加して fix posture を固定 | (a) `go/internal/guardrail/rules_test.go` に R06/R11/R12 の env/sudo/watch wrapper bypass テストが各 3 ケース合計 9 ケース追加、(b) 漏れがあった場合は `rules.go` で対応し commit に diff が残る、(c) `cd go && go test ./internal/guardrail/... -run TestWrapperBypass -v` で 9 ケース全 PASS、(d) `tests/test-claude-upstream-integration.sh` が Phase 62.1.5 の参照を検出、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.2.1 | Claude Code `2.1.121` の `PostToolUse.updatedToolOutput` を Phase 58.2.2 設計方針 (opt-in / redaction-compaction-normalization 限定 / before-after audit) に従って実装する。`.claude-plugin/hooks.json` で 1 つだけ opt-in の PostToolUse hook を有効化 (例: machine-readable JSON normalization、デフォルトは無効)。元 output と更新後 output を `.claude/state/output-audit.jsonl` に append-only で記録。レビュー / テスト証拠の改ざんを禁止する仕組みを test で検証 | (a) `scripts/hook-handlers/posttool-output-normalize.sh` または Go 実装が opt-in で動作、(b) `.claude/state/output-audit.jsonl` に before/after が JSON Lines で append-only 記録、(c) `tests/test-output-governance.sh` が「redaction 用途は PASS」「テスト出力の改ざん用途は FAIL」を機械検証、(d) JSON 契約 tool では人間向け説明を mix しないことを test で固定、(e) `./tests/validate-plugin.sh` PASS | 58.2.2, 62.1.5 | cc:完了 [028034ef] |
| 62.2.2 | Claude Code `2.1.119` で `--agent <name>` が agent frontmatter の `permissionMode` を確実に尊重する fix の reaffirmation。`agents/worker.md` (`acceptEdits`)、`agents/reviewer.md` (Read-only)、`agents/scaffolder.md` (default + ask) の permissionMode が CC `2.1.119+` で実適用されるかを `tests/test-agent-permission-mode.sh` で機械検証。Phase 59.2.3 で「silently ignored になり得る」と判定された field のうち CC 2.1.119+ で reactivate されたものを agent frontmatter に正式記載 | (a) `tests/test-agent-permission-mode.sh` が worker / reviewer / scaffolder の permissionMode を CC `2.1.119+` で検証、(b) Reviewer の Read-only enforcement (Edit/Write/Bash 制限) が test で固定、(c) silently ignored だった field のうち reactivate されたものは agent frontmatter に正式記載、(d) `agents/team-composition.md` の権限境界記述と一致、(e) `./tests/validate-plugin.sh` PASS | 59.2.3, 62.1.5 | cc:完了 [028034ef] |
| 62.2.3 | Claude Code `2.1.126` で `claude_code.skill_activated` OTel event が `invocation_trigger` (human / model / skill-chain) を含むようになった件を活用する。`scripts/skill-trigger-telemetry.sh` または `bin/harness skill-telemetry` (Go) で trigger 別カウントを `.claude/state/skill-trigger-stats.jsonl` に append-only 記録。privacy / retention (既定 30 日) / opt-out フラグを `docs/skill-telemetry-policy.md` に固定。Phase 58.2.3 の「telemetry sink 設計が先」判断と矛盾しないことを test で確認 | (a) `scripts/skill-trigger-telemetry.sh` (or Go 実装) が `invocation_trigger` を読んで trigger 別カウントを `.claude/state/skill-trigger-stats.jsonl` に記録、(b) `tests/test-skill-trigger-telemetry.sh` が human / model / skill-chain の 3 trigger を区別して記録できることを検証、(c) `docs/skill-telemetry-policy.md` に privacy / retention / opt-out が書かれる、(d) Phase 58.2.3 の判断と矛盾しないことが docs に明記、(e) `./tests/validate-plugin.sh` PASS | 58.2.3, 62.1.5 | cc:完了 [028034ef] |
| 62.2.4 | Claude Code `2.1.132` で Bash subprocess に `CLAUDE_CODE_SESSION_ID` が env で渡るようになった件を活用し、`scripts/hook-handlers/*.sh` の session ID 取得を env 直接参照に統一する。後方互換のため env 空のときだけ `$CLAUDE_TRANSCRIPT_PATH` regex fallback を残す。3 状態 (env あり / env 無し / 両方無し = legacy fallback) を `.claude/rules/active-watching-test-policy.md` の test 規約に準拠して検証 | (a) `scripts/hook-handlers/memory-session-start.sh` 等で `${CLAUDE_CODE_SESSION_ID:-$(extract_from_transcript)}` パターンに統一、(b) regex fallback が CC `2.1.131` 以下でも動作することを test で固定、(c) `tests/test-hook-handler-session-id.sh` が env あり / env 無し / 両方無し の 3 状態を検証、(d) `.claude/rules/active-watching-test-policy.md` の `TestXxx_NotConfigured` / `TestXxx_Healthy` / `TestXxx_Corrupted` 命名規約に準拠、(e) `./tests/validate-plugin.sh` PASS | 50.1.1, 62.1.5 | cc:完了 [028034ef] (refactor 不要を確認、policy doc + test を新設) |
| 62.2.5 | Claude Code `2.1.129` で `skillOverrides` 設定が `off`, `user-invocable-only`, `name-only` の 3 mode をサポートするようになった件のガバナンス doc を作成する。3 mode の意味、推奨 default、enterprise governance 用途、Harness の skill manifest (`scripts/generate-skill-manifest.sh`) との関係を `docs/skill-overrides-policy.md` に固定。`harness-init` で生成する settings に default を入れるかの判断 (推奨は `name-only` だが互換のため明示無効化を採用するか) を記録 | (a) `docs/skill-overrides-policy.md` に 3 mode の意味、推奨 default、enterprise 用途、Phase 59.1.2 の skill manifest との関係が書かれる、(b) `harness-init` で default を入れるかの判断 (採用 / 非採用 / project-specific) が記録、(c) `tests/test-settings-baseline.sh` が skillOverrides の存在を許容 (強制しない) するよう更新、(d) Phase 59.1.2 の skill manifest と矛盾しない、(e) `./tests/validate-plugin.sh` PASS | 59.1.2, 62.1.5 | cc:完了 [028034ef] |
| 62.3.1 | Phase 62 の upstream snapshot / Feature Table / CHANGELOG / integration test 接続を Phase 58.1.1-58.1.2 と同じスタイルで行う。`docs/upstream-update-snapshot-2026-05-07.md` を新規作成し、CC `2.1.112`-`2.1.118` および `2.1.127`-`2.1.132` を A/C/P 分類 (`B: 書いただけ` 0 件) で snapshot 化。`docs/CLAUDE-feature-table.md` に Phase 62 追補。`CHANGELOG.md` `[Unreleased]` に Phase 62 の「今まで / 今後」を CC アプデ → Harness での活用 形式で追記。`tests/test-claude-upstream-integration.sh` で Phase 62 参照漏れを検出 | (a) `docs/upstream-update-snapshot-2026-05-07.md` がある、(b) `2.1.112`-`2.1.118`, `2.1.127`-`2.1.132` が表で A/C/P 分類されている、(c) `B: 書いただけ 0 件` の理由がある、(d) `docs/CLAUDE-feature-table.md` に Phase 62 追補、(e) `CHANGELOG.md` `[Unreleased]` に Phase 62 の「CC のアプデ → Harness での活用」記述、(f) `bash tests/test-claude-upstream-integration.sh` PASS、(g) `./tests/validate-plugin.sh` PASS (48/48) | 62.1.1, 62.1.2, 62.1.3, 62.1.4, 62.1.5, 62.2.1, 62.2.2, 62.2.3, 62.2.4, 62.2.5 | cc:完了 [bb3cfa5e] |

---

## Phase 65: 3-Surface HTML for Non-Engineer Vibecoder + harness-mem cross-project safety [P1]

Purpose: 非エンジニアの発注者・プロデューサー職向けに、Claude への発注の (1) 計画理解 / (2) 進行管理 / (3) 受け入れ判断の 3 つの認知負荷ピークを HTML artifact で可視化する。各 surface は harness-mem と双方向連携 (read: 過去決定/パターン/類似案件 / write: 個人選好の累積) するが、cross-project search はデフォルト OFF とし、ユーザーが明示的に `cross-project-groups.yaml` でグループ定義した場合のみ opt-in 横断を許可する。Layer 1 (mem `<private>` strip + project filter) は既存だが、Layer 2 (クライアント辞書 + NER) と Layer 3 (HTML 生成直前最終 scan + 監査ログ) を harness 側で新規実装し、3 層防御で固有名詞漏洩を構造的に防ぐ。Phase A → E の段階的解放で、MVP は横断機能なしで体験価値を先に固める。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 65.1.1 | [Phase A: Plan Brief MVP] HTML rendering infrastructure を新規導入する。`scripts/render-html.sh` (約 100-200 行) で mustache 風 (`{{var}}`) テンプレート展開 + jq による JSON データバインドを実装。テンプレート格納ディレクトリは `templates/html/`、出力先は `.claude/state/views/`。テンプレート schema は `html-render-input.v1` (`{kind: "plan-brief"\|"progress"\|"accept", project: string, generated_at: ISO8601, sections: [...]}`) で固定。生成 HTML は単独で開ける (no server, no JS framework)、CSS は inline、Claude Harness ブランド (off-white BG / near-black FG / harness orange accent) 準拠 | (a) `scripts/render-html.sh` が `--template <name> --data <json> --out <path>` の 3 引数で動作、(b) `templates/html/test-fixture.html.template` で `{{title}}` および `{{#sections}}{{name}}{{/sections}}` の 2 種類が展開可能、(c) `tests/test-render-html.sh` で 4 ケース (正常 / 空 sections / 不正 JSON / 存在しないテンプレート) を機械検証、(d) 出力 HTML は `lynx -dump` で読める text 構造を持つ、(e) CSS palette が Claude Harness ブランド (`#FAFAFA` off-white / `#0F0F0F` near-black / `#F58A4A` harness orange) を使用、(f) `./tests/validate-plugin.sh` PASS | 64.2 | cc:完了 [49f8a0e6] |
| 65.1.2 | `harness-plan-brief` skill を新規作成する。`skills/harness-plan-brief/SKILL.md` (frontmatter `name: harness-plan-brief`, `user-invocable: true`, `argument-hint: "[task-description]"`)。skill の責務は (i) 現プロジェクトのみで harness-mem search を実行 (`project: <current>`, `strict_project: true` 強制)、(ii) 過去 decision (D1-D41) と pattern (P1-P33) と Plans archive 28 件から類似案件を抽出、(iii) `plan-brief-context.v1` schema (`{user_request, my_understanding, options[], risks[], acceptance_criteria[], confidence: 0-100, related_decisions[], similar_past_plans[]}`) を構築、(iv) `templates/html/plan-brief.html.template` 経由で HTML 生成、(v) ブラウザで自動 open (`open` / `xdg-open` / `start` の OS 別 dispatch) | (a) `skills/harness-plan-brief/SKILL.md` 存在 (frontmatter は skill-editing.md 規約準拠)、(b) skill 起動時に `mcp__harness__harness_mem_search` を `project: $(basename $(git rev-parse --show-toplevel))` 強制で 1 回以上呼び出す、(c) cross-project search を**呼ばない** (test で `project` parameter 必須が確認できる)、(d) 出力 `plan-brief-context.v1` schema が JSON Schema validator で valid、(e) `tests/test-harness-plan-brief.sh` で fixture プロジェクトに対し HTML 生成と auto-open dispatch を機械検証 (CI 環境では `BROWSER=true` で skip)、(f) `./tests/validate-plugin.sh` PASS | 65.1.1 | cc:完了 [b523dee1] |
| 65.1.3 | Plan Brief の context compilation logic を実装する。`scripts/plan-brief-compile.sh` (約 150-250 行) で: (i) ユーザー request からキーワード抽出 (FTS で `harness_mem_search`)、(ii) 上位 5 件の decision / pattern / past plan を `expand_links: true` で取得、(iii) 自信度算出ロジック (過去類似案件の成功率 baseline + 現タスクの DoD 明確度の 2 軸) を実装。`confidence: 0-100` の整数で出力。算出根拠を `confidence_evidence: string[]` に同梱 | (a) `scripts/plan-brief-compile.sh` が `--query <text> --project <name>` で context JSON を stdout 出力、(b) `confidence` 算出が「過去類似案件の cc:完了率 (40 点満点) + DoD の数値要件含有率 (30 点満点) + 関連 D/P 件数の有意性 (30 点満点)」の合計、(c) `tests/test-plan-brief-compile.sh` で 4 ケース (類似案件 0 件 / 5 件 / 全完了 / 半数失敗) を機械検証、(d) `confidence_evidence` に「過去 N 件中 M 件成功」のような数値が 1 行以上含まれる、(e) `./tests/validate-plugin.sh` PASS | 65.1.2 | cc:完了 [f545ae03] |
| 65.1.4 | Plan Brief 承認時の memory 書き込み (write side) を実装する。`scripts/plan-brief-record-decision.sh` で `mcp__harness__harness_mem_ingest` 経由で `observation_type: "decision"` の record を作成。schema `personal-preference.v1` (`{user_request_hash: sha256, chosen_option: string, rejected_options: string[], reasoning: string, timestamp: ISO8601, project: string}`) で固定。**個人選好データ蓄積**の起点。承認 / 修正依頼 / 質問の 3 アクションを区別記録 | (a) `scripts/plan-brief-record-decision.sh` が承認 / 修正依頼 / 質問の 3 action mode を持つ、(b) record される observation の `tags` に `personal-preference` `plan-brief-approval` の 2 タグが含まれる、(c) `tests/test-plan-brief-record.sh` で 3 action 各 1 ケース合計 3 ケース機械検証、(d) `mcp__harness__harness_mem_search` で `tags: personal-preference` 検索すると 1 ヶ月後に 30 件以上ヒット可能な構造、(e) `./tests/validate-plugin.sh` PASS | 65.1.2 | cc:完了 [554c5d21] |
| 65.1.5 | Phase 65.1.x の e2e validation。実プロジェクト fixture (`tests/fixtures/plan-brief-e2e/`) で「ユーザー request → Plan Brief HTML 生成 → 承認 → memory 書き込み」のフローを通し検証 | (a) `tests/test-plan-brief-e2e.sh` で 5 ステップ (request 入力 / mem search / HTML 生成 / 承認 / mem write) を順次検証、(b) 生成 HTML が `plan-brief-context.v1` schema と一致、(c) memory 書き込み後 `mcp__harness__harness_mem_search query: "<request hash>"` で当該 record を再取得可能、(d) `./tests/validate-plugin.sh` 49+/49+ PASS (新規テスト追加 1 件以上)、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.1.1, 65.1.2, 65.1.3, 65.1.4 | cc:完了 [2f3aed56] |
| 65.2.1 | [Phase B: Acceptance Demo] `harness-accept` skill を新規作成する (Phase 65.1 と同パターン)。`skills/harness-accept/SKILL.md`、`templates/html/accept.html.template`。schema `acceptance-context.v1` (`{user_request, demo_artifacts: [{kind: "video"\|"screenshot"\|"text", path}], verified_criteria: [{name, passed: bool, evidence}], unverified_caveats[], past_issue_patterns[], recommendation: "ship"\|"wait"\|"reject"}`)。Plan Brief 時に書き込んだ acceptance_criteria を read 側で取得し、各 criterion ごとに verified/unverified を表示 | (a) `skills/harness-accept/SKILL.md` 存在、(b) Plan Brief で書き込んだ `personal-preference.v1` の acceptance_criteria を本 skill が read で取得、(c) `verified_criteria[].evidence` が空文字列のとき HTML で警告表示、(d) `recommendation` 算出ロジック (verified 数 / 全 criteria 数 ≥ 0.8 → ship、≥ 0.5 → wait、それ未満 → reject) が unit test で固定、(e) `tests/test-harness-accept.sh` で 4 ケース (全 verified / 半 verified / 全 unverified / criteria 0 件) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.1.5 | cc:完了 [e62c7027] |
| 65.2.2 | Acceptance Demo の「過去の問題パターン」呼び出し (read side) を実装する。`scripts/accept-past-issues.sh` で patterns.md (P1-P33) と過去の `acceptance-context.v1` record を semantic search し、現タスクと類似する post-launch issue 履歴を最大 3 件取得。**まだ project-only 検索** (cross-project は Phase 65.3 で解放) | (a) `scripts/accept-past-issues.sh` が `--project <name> --task <description>` で過去 issue 履歴 (`past-issue.v1` schema) を出力、(b) cross-project search を**呼ばない** (project parameter 必須を test で固定)、(c) 上位 3 件取得 + 各々に `verified_in_current_task: bool` 付与、(d) `tests/test-accept-past-issues.sh` で 3 ケース (issue 0 件 / 3 件 / verified 半数) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.1 | cc:完了 [a85f903a] |
| 65.2.3 | Acceptance Demo 承認時の memory 書き込み (write side)。schema `acceptance-decision.v1` (`{user_request_hash: sha256, recommendation_taken: bool, override_reason: string?, verified_criteria_at_decision[], post_launch_concerns[], timestamp: ISO8601}`)。Plan Brief で書き込んだ user_request_hash と紐付けて、**プラン → 受け入れの完全 trace** を構築 | (a) `scripts/accept-record-decision.sh` 存在、(b) record の `user_request_hash` が Plan Brief 側 record と join 可能、(c) `mcp__harness__harness_mem_graph` で plan→accept の chain を取得可能 (test で確認)、(d) `tests/test-accept-record.sh` で 3 ケース (recommendation 採用 / override / reject) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.1 | cc:完了 [5c65f1ce] |
| 65.2.4 | Phase 65.2.x の e2e validation。Plan Brief approval → 実装 → Acceptance Demo の 3 段階を 1 つの fixture project で通し、`personal-preference.v1` と `acceptance-decision.v1` が同 user_request_hash で紐付くことを検証 | (a) `tests/test-plan-accept-flow-e2e.sh` で 3 段階 (plan → impl simulation → accept) を順次検証、(b) memory に 2 種の record が join 可能な状態で保存される、(c) HTML 2 種 (plan-brief / accept) が同 task で生成され、両方とも user_request を共有、(d) `./tests/validate-plugin.sh` 50+/50+ PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.2.1, 65.2.2, 65.2.3 | cc:完了 [d2229a04] |
| 65.3.1 | [Phase C: Cross-Project Group + 3 層防御] `cross-project-groups.yaml` schema を策定し、初期テンプレートを設置する。`.claude/rules/cross-project-groups.yaml` (新規 SSOT) を作成し、schema `cross-project-group.v1` (`{groups: [{name: string, members: string[], description: string?}]}`) で固定。default は空 (`groups: []`) で、横断検索は無効化される。設定例として 2 グループ (Personal Tools / Client-Sample) のコメントアウト雛形を含める | (a) `.claude/rules/cross-project-groups.yaml` 存在、初期 `groups: []`、(b) `cross-project-group.v1` schema が `docs/cross-project-groups-schema.md` に記載、(c) `scripts/load-cross-project-groups.sh` で yaml を JSON にパースし、不正な schema 時は exit 1、(d) `tests/test-cross-project-groups-schema.sh` で 4 ケース (空 / 1 group / member 重複 / 不正 schema) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.4 | cc:完了 [4a014137] |
| 65.3.2 | `client-redaction.yaml` schema を策定し、Layer 2 辞書ベースの固有名詞検出を実装する。`.claude/rules/client-redaction.yaml` (新規 SSOT) を作成し、schema `client-redaction.v1` (`{clients: [{name, redact_as, aliases[]?}], people: [...], domains: [...]}`)。default は空。`scripts/redact-by-dictionary.sh` で「テキストを受け取り → 辞書ヒットを redact_as で置換 → 置換数とともに stdout 返却」 | (a) `.claude/rules/client-redaction.yaml` 存在、初期 `clients: [], people: [], domains: []`、(b) `scripts/redact-by-dictionary.sh` が `--input <text> --dict <yaml-path>` で動作、(c) ヒット 0 件時は exit 0 で原文そのまま、ヒット時は stderr に件数記録、(d) `tests/test-redact-by-dictionary.sh` で 5 ケース (ヒット 0 / 1 / 複数 / aliases / 重複 redact_as) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.4 | cc:完了 [5152bed2] |
| 65.3.3 | Layer 2 の NER (Named Entity Recognition) 検出層を追加する。kuromoji-js または同等の lightweight Japanese tokenizer を使用し、形態素解析後 `proper noun` (固有名詞) tag を持つトークンを抽出。`scripts/redact-by-ner.sh` で「テキスト → NER ヒット → `[Entity]` トークンへの置換」を実装。辞書ヒットと NER ヒットは独立に集計し、Layer 3 で合算 | (a) `scripts/redact-by-ner.sh` が `--input <text>` で固有名詞を `[Entity]` に置換、(b) Japanese tokenizer (kuromoji 等) が node_modules または stand-alone CLI として利用可能、(c) `tests/test-redact-by-ner.sh` で 4 ケース (人名 / 会社名 / 地名 / 固有名詞 0 件) を機械検証、(d) NER 失敗時 (tokenizer 不在) は fail-open で原文そのまま + stderr 警告、(e) `./tests/validate-plugin.sh` PASS | 65.3.2 | cc:完了 [20a4478f] |
| 65.3.4 | Layer 3 の HTML 生成直前最終 scan を実装する。`scripts/render-html.sh` を拡張し、HTML 生成前に `redact-by-dictionary.sh` → `redact-by-ner.sh` → 最終 sanity check (固有名詞らしきカタカナ連続 5 文字以上を検出) の 3 段を順次走らせる。3 段目で残骸が見つかった場合は HTML を**生成せず exit 1**、stderr に検出内容と該当テンプレートを出力 (fail-safe) | (a) `scripts/render-html.sh` に `--with-redaction` flag が追加、(b) flag 有効時に dict → NER → final scan の 3 段順次実行、(c) final scan で残骸検出時は HTML 生成中止 + exit 1 + stderr に「detected: <token>, source: <line>」出力、(d) `tests/test-render-html-redaction.sh` で 4 ケース (全 clean / dict のみヒット / NER のみヒット / final scan で検出 = exit 1) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.3.3 | cc:完了 [0ae3f40a] |
| 65.3.5 | Cross-project search の opt-in 機構を harness-plan-brief / harness-accept skill に組み込む。`--cross-project-group <name>` flag を追加し、指定時のみ `cross-project-groups.yaml` から member projects を解決して **MCP `mcp__harness__harness_mem_search` を member ごとに N 回 call し client 側でマージ** (D43 Option α、mem 側 MCP に projects 配列が無いため)。flag 不在時は現プロジェクトのみ (Phase 65.1.2 の挙動を維持)。group 名が yaml に存在しない場合は exit 1 | (a) `harness-plan-brief` / `harness-accept` 両 skill が `--cross-project-group <name>` flag を受け取る、(b) flag 不在時は現プロジェクト single search (Phase 65.1.2 と同じ挙動)、(c) flag 有効時は yaml から member resolve し各 project で MCP search を 1 回ずつ発行 → client でマージ・dedupe・relevance_score 順 sort、(d) group 名が存在しない場合は exit 1 + stderr に「group not found」、(e) `tests/test-cross-project-flag.sh` で 4 ケース (flag 不在 / 有効 group / 無効 group / 空 group) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.3.1, 65.3.4 | cc:完了 [09377eb9] |
| 65.3.6 | 監査ログの実装。Cross-project search が走った場合に `.claude/state/audit/cross-project-search.jsonl` に append-only 記録。schema `cross-project-audit.v1` (`{timestamp: ISO8601, group_name, member_projects[], query_hash: sha256, redaction_count: {dict: int, ner: int}, output_passed_final_scan: bool}`)。実際のクエリ文字列は記録しない (privacy)、hash のみ。HTML の最下部に「この HTML は <N> 件 redact しました」と監査サマリを表示 | (a) `.claude/state/audit/cross-project-search.jsonl` が cross-project search 1 回ごとに 1 行追加、(b) JSON Lines 各行が `cross-project-audit.v1` schema 準拠、(c) クエリ文字列の生記録なし、hash のみ、(d) HTML 最下部に「redacted: dict X 件 + NER Y 件」のサマリが表示、(e) `tests/test-cross-project-audit.sh` で 3 ケース (redaction 0 / 複数 / final scan 失敗) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.3.4, 65.3.5 | cc:完了 [272a8f33] |
| 65.3.7 | Phase 65.3.x の e2e validation。fixture group 設定 + dict + NER 各層を全て通すクロスプロジェクト Plan Brief を生成し、3 層防御で固有名詞が漏れないことを検証 | (a) `tests/test-cross-project-redaction-e2e.sh` で 1 fixture group (3 member projects) で Plan Brief 生成、(b) 各 member project に予め固有名詞を含む observation を挿入、(c) Plan Brief 出力 HTML に固有名詞 (生のクライアント名 / 人名) が含まれないことを `grep -E "(NoraiCorp\|YorozuPro\|田中\|佐藤)" output.html` で 0 件確認、(d) audit log に redaction 件数が正しく記録、(e) `./tests/validate-plugin.sh` 53+/53+ PASS、(f) `bash scripts/ci/check-consistency.sh` 全合格、(g) [D43] envelope (signals + prose) 構造への redaction を test fixture に含め、signals[] と prose の整合性 (validateProseContainsSignals 不変条件) が壊れないこと、(h) [D43] 既存 server 側 `[REDACTED_*]` mark の二重置換ガードを test 検証 (event-recorder.ts:redactContent との衝突防止) | 65.3.1, 65.3.2, 65.3.3, 65.3.4, 65.3.5, 65.3.6 | cc:完了 [c05d6ef8] |
| 65.4.1 | [Phase D: Progress Tracker] `harness-progress` skill skeleton を新規作成する。`skills/harness-progress/SKILL.md`、`templates/html/progress.html.template`。schema `progress-snapshot.v1` (`{project, current_task, progress_pct: 0-100, todo_tasks[], wip_tasks[], done_tasks[], elapsed_minutes: int, estimated_total_minutes: int, cost_so_far_usd: float, cost_estimate_usd: float, alerts[]}`)。生成 HTML は静的 (PWA や WebSocket は使わず、再生成で更新) | (a) `skills/harness-progress/SKILL.md` 存在、(b) `progress-snapshot.v1` schema が JSON Schema validator で valid、(c) HTML 生成時に Plans.md の cc:WIP / cc:TODO / cc:完了 件数を反映、(d) `tests/test-harness-progress.sh` で fixture Plans.md (各 status 含む) で snapshot HTML が正しい % で表示されることを機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.3.7 | cc:完了 [1175b9e4] |
| 65.4.2 | Progress Tracker の自動再生成機構を実装する。PostToolUse hook で「Edit / Write / Bash の発火」をトリガーに、`harness-progress` skill を background 起動して HTML を再生成。生成頻度は最大 1 分に 1 回 (rate limit) で過剰生成を防ぐ | (a) `scripts/hook-handlers/posttool-progress-regen.sh` が PostToolUse hook で起動、(b) `.claude/state/progress-last-regen.txt` に last regen timestamp 記録、(c) 60 秒以内の連続発火は skip (rate limit)、(d) `.claude-plugin/hooks.json` の dual sync が `hooks/hooks.json` と整合 (P29 規約)、(e) `tests/test-progress-regen.sh` で 4 ケース (初回 / 60秒以内 / 60秒超 / hook input 不正) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.4.1 | cc:完了 [847ea36d] |
| 65.4.3 | Drift detection と alert を実装する。schema `progress-alert.v1` (`{kind: "scope-creep"\|"time-overrun"\|"repeated-failure"\|"cost-warning"\|"high-risk-file", severity: "info"\|"warn"\|"critical", message, suggested_action, triggered_at: ISO8601}`)。各 alert kind に対応する検出ロジックを実装: scope-creep (Plans.md にない file 編集を検出)、time-overrun (estimated の 1.5 倍超)、repeated-failure (test fail 3 回連続)、cost-warning (上限の 80%)、high-risk-file (`harness.toml` deny path matching) | (a) `scripts/progress-detect-drift.sh` が 5 alert kind それぞれを検出、(b) 検出結果を `progress-alert.v1` schema で出力、(c) `tests/test-progress-drift.sh` で各 alert kind 計 5 ケース機械検証、(d) HTML 表示時に critical alert は赤、warn は黄、info は青で色分け、(e) `./tests/validate-plugin.sh` PASS | 65.4.2 | cc:完了 [a835bde0] |
| 65.4.4 | Progress Tracker の「過去の判断パターン」表示を実装する (read side)。alert 検出時に `mcp__harness__harness_mem_search` で過去の同種 alert への user 判断 (Phase 65.1.4 で書き込まれた `personal-preference.v1` から該当 alert kind を抽出) を取得し、HTML に「過去 N 件中 M 件で同様の提案を断っています」と表示 | (a) `scripts/progress-past-judgments.sh` が alert kind と現プロジェクト名で過去判断を検索、(b) 上位 3 件の judgment と「断る確率 N%」が JSON 出力、(c) cross-project search はデフォルト OFF (Phase 65.3.5 と同じ flag mechanism)、(d) `tests/test-progress-past-judgments.sh` で 4 ケース (judgment 0 件 / 3 件 / 全断る / 全許可) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.4.3, 65.1.4 | cc:完了 [6be27a68] |
| 65.4.5 | Phase 65.4.x の e2e validation。Plans.md に WIP/TODO/完了タスクを混在させた fixture で Progress Tracker 生成、scope-creep alert 発火、過去判断表示、再生成 rate limit を順次検証 | (a) `tests/test-progress-e2e.sh` で 5 ステップ (初回生成 / Plans 編集後再生成 / scope-creep 発火 / 過去判断表示 / rate limit 検証) を順次実行、(b) 全 5 alert kind が HTML に正しく表示、(c) audit log に regen 履歴が記録、(d) `./tests/validate-plugin.sh` 56+/56+ PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.4.1, 65.4.2, 65.4.3, 65.4.4 | cc:完了 [495dfffc] |
| 65.5.1 | [Phase E: 統合 + 監査 UI + ドキュメント] 3 surface (plan-brief / progress / accept) の統合 e2e。1 fixture project でユーザー request → Plan Brief → impl simulation → Progress (alerts) → Acceptance Demo の全フローを通し、3 つの HTML が同一 user_request_hash で trace 可能な状態を検証 | (a) `tests/test-3-surface-e2e.sh` で 5 段階 (plan / progress regen / drift alert / impl complete / accept) を順次実行、(b) 3 種の memory record (plan-brief-approval / progress-judgment / acceptance-decision) が同 hash で graph join 可能、(c) 3 種の HTML が同 fixture から生成され、それぞれ画面遷移リンクで相互参照、(d) `./tests/validate-plugin.sh` 全 PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.4.5 | cc:完了 [03629d85] |
| 65.5.2 | 監査 UI を 3 つの HTML 全てに統合する。各 HTML 最下部に「🔍 この artifact の根拠」セクションを追加し、(i) 検索範囲 (project 名 + group 名)、(ii) 参照した D / P / past plan の ID リスト、(iii) redact された語の件数 (kind 別)、(iv) 完全な audit log への link (`.claude/state/audit/*.jsonl`) を表示 | (a) 3 種の HTML テンプレートに `audit-trail` セクションが共通追加、(b) 4 項目 (検索範囲 / 参照 ID / redact 件数 / log link) が表示、(c) audit log が JSON Lines で human-readable、(d) `tests/test-audit-ui-presence.sh` で 3 種 HTML 全てに audit-trail セクションが含まれることを `grep -c "audit-trail"` で検証、(e) `./tests/validate-plugin.sh` PASS | 65.5.1 | cc:完了 [56543c7e] |
| 65.5.3 | 開発者 / ユーザー向けドキュメントを整備する。`docs/cognitive-load-surfaces.md` で 3 surface の役割と使い方、`docs/cross-project-safety.md` で 3 層防御の説明、`docs/cross-project-groups-schema.md` でグループ設定方法。各 doc は非エンジニア向けの語彙で書く (技術用語は最小限) | (a) 3 つの新規 doc が `docs/` 配下に存在、(b) 合計 1500 行以下 (簡潔さ重視)、(c) 各 doc に「やりたいこと」「やり方」「気をつけること」のセクションが必ず存在、(d) `docs/CLAUDE-skill-catalog.md` に 3 つの新スキルが追記、(e) CLAUDE.md の Top Skill Areas table に「cognitive-load (Plan Brief / Progress / Accept)」行を追加、(f) `./tests/validate-plugin.sh` PASS | 65.5.2 | cc:完了 [fb47a79e] |
| 65.5.4 | Feature Table と CHANGELOG への追補。`docs/CLAUDE-feature-table.md` の Phase 65 行追加 (`A: 実装あり` 分類)、`CHANGELOG.md` `[Unreleased]` に Phase 65 を「今まで / 今後」形式で追記。`.claude/rules/github-release.md` の release-notes ルール準拠 | (a) `docs/CLAUDE-feature-table.md` に Phase 65 の 3 surface 行追加 (A: 実装あり)、(b) `CHANGELOG.md` `[Unreleased]` に Phase 65 が「今まで (markdown 200 行スクロール) / 今後 (HTML カードでひと目)」の対比形式で記載、(c) Before / After table が含まれる、(d) `tests/test-changelog-format.sh` (既存または新規) PASS、(e) `./tests/validate-plugin.sh` PASS | 65.5.3 | cc:完了 [99695676] |
| 65.5.5 | Phase 65 全体のリリース準備。VERSION/plugin.json/harness.toml/marketplace.json の 4 点同期、SemVer 判定 (新スキル 3 個 + 新 hook 追加 = minor)、リリースタグ準備 | (a) `./scripts/sync-version.sh bump --minor` 実行で 4 ファイル同期 (現 4.8.1 → 4.9.0)、(b) SemVer 判定の根拠 (3 新スキル + Phase 65.4 で hook 1 件追加 + new yaml SSOT 2 件) が `CHANGELOG.md` に明記、(c) `bash tests/validate-plugin.sh` 全 PASS、(d) `bash scripts/ci/check-consistency.sh` 全合格、(e) GitHub Release notes が `.claude/rules/github-release.md` 規約準拠 | 65.5.4 | cc:完了 [8b3ba01b] |

---

## Phase 72: New Harness root spec + lane/stage planning contract [P1]

Purpose: `Plans.md` は作業台帳、root `spec.md` は repo 全体の product contract として分離し、AI が未取得・未観測のデータを「存在しない」と断定しない New Harness の正本を作る。過去の成功パターンである Plans first、spec SSOT、TDD red evidence、major-only / full review の使い分け、OK まで再レビュー、PR / release evidence を、`[lane:fast|gate|release]` と stage gate で一貫運用する。Plan の作り方は「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout」とし、既存 `cc:TODO/WIP/完了` marker は壊さず、lane / stage metadata と DoD で表現する。`harness-review` は read-only boundary を維持し、PR 作成は `harness-work` 側の明示 closeout として扱う。Release lane は PR と公開配布を混同せず、tag / GitHub Release / CI / version surface まで削らない。

V2 Spec Snapshot: root `spec.md` を上位正本、`docs/plans/spec-ssot.md` を spec 運用ルール、`docs/architecture/hokage-core.md` と `go/SPEC.md` を sub-spec とする。作業は `[lane:fast]`, `[lane:gate]`, `[lane:release]` で分類し、stage gate は「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout -> release closeout」。未取得・未観測の情報は `unknown` とし、`not_observed != absent` を V2 の必須契約にする。PR closeout は `harness-work` 側、release は `harness-release` 側、`harness-review` は read-only quality gate のまま維持する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 72.1.1 | [Plan / SSOT] root `spec.md` を `Plans.md` と同階層に新設し、repo 全体の上位 product contract として固定する。`docs/plans/spec-ssot.md` は「spec 運用ルール」、`docs/architecture/hokage-core.md` は architecture sub-spec、`go/SPEC.md` は Go runtime sub-spec と明記し、二重正本化を防ぐ。`spec.md` には Purpose / Users And Workflows / Core Workflow / Lane Taxonomy / Unknown Data Contract / Host Adapter Boundary / Data And Contracts / Non-Goals / Open Decisions / Links を置く。[tdd:skip:docs-only] | (a) root `spec.md` が存在し、`Plans.md is the task ledger` と `spec.md is the product contract` の役割分担が明記される、(b) `spec.md` に `not_observed != absent` または同等の未観測データ契約がある、(c) `docs/plans/spec-ssot.md` の優先 spec path が root `spec.md` -> existing project spec -> `docs/spec/00-project-spec.md` の順になる、(d) `spec.md` が `docs/architecture/hokage-core.md` と `go/SPEC.md` を sub-spec としてリンクし、置き換えないと明記、(e) `bash tests/test-spec-ssot-workflow.sh` PASS | 71.1.1 | cc:完了 |
| 72.1.2 | [Planning Flow] `harness-plan` の create/add 契約を更新し、Plan 作成を「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout」の stage gate で出力する。各 task に `[lane:fast]`, `[lane:gate]`, `[lane:release]` のいずれかを付け、Fast/Gate/Release を新 skill ではなく Plans metadata として扱う。AI が見えていない data / failed search / missing fixture / API unavailable は `unknown` とし、`absent` と断定しない。 | (a) `skills/harness-plan/SKILL.md` と `skills/harness-plan/references/create.md` が root `spec.md` 優先、lane taxonomy、stage gate、unknown data contract を説明、(b) Plans template が既存 5 column format を維持し、Content または DoD に lane/stage metadata を入れる、(c) `[lane:fast]` は low-risk local work、`[lane:gate]` は spec / workflow / mirror / guardrail、`[lane:release]` は public artifact / version / tag / GitHub Release と明記、(d) `bash tests/test-harness-plan-quality.sh` または同等の new governance test が lane/stage/root spec/unknown data contract を検出、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 72.1.1 | cc:TODO |
| 72.1.3 | [Execution / TDD] `harness-work` と Worker / Scaffolder / Reviewer input を更新し、`spec_path`, `lane`, `stage`, `research_evidence`, `tdd_red_log`, `review_artifact`, `pr_closeout` を sprint contract へ渡す。`[tdd:required]` task では red-log または明示 `skip_tdd_reason` がない限り完了扱いにしない。Fast lane は軽くするが、unknown data contract と focused checks は省かない。 | (a) `skills/harness-work/SKILL.md`, `skills/harness-work/references/execution-modes.md`, `agents/worker.md`, `agents/scaffolder.md`, `agents/reviewer.md` が `spec_path` と `lane` を扱う、(b) `scripts/generate-sprint-contract.js` または contract enrichment が lane/stage/evidence fields を出力、(c) TDD required で red evidence 不足なら review/closeout が fail する test がある、(d) Fast lane の closeout は focused tests + evidence pack を要求し、full review を必須にしない、(e) `bash tests/test-spec-ssot-workflow.sh` と `bash tests/test-tdd-enforcement-l1l2l4.sh` PASS | 72.1.2 | cc:TODO |
| 72.1.4 | [Review Gate] `harness-review` / `harness-release` の gate を更新し、root `spec.md` alignment、Plans lane/stage、TDD evidence、unknown data contract、accepted/rejected findings、release-preflight warning handling を確認する。`harness-review` は read-only のままにし、`APPROVE` は commit / push / PR 作成命令ではないと維持する。 | (a) `skills/harness-review/SKILL.md` と references が `spec.md` alignment、`not_observed != absent`、lane/stage、TDD evidence、evidence pack を合格条件に含む、(b) 証拠なしの「問題なし」「データなし」を APPROVE しない contract が test で検出される、(c) `skills/harness-release/SKILL.md` が PR ready と release ready を分け、release lane では version/tag/GitHub Release/CI を要求、(d) `bash tests/test-harness-review-governance.sh` と `bash tests/test-harness-release-governance.sh` PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 72.1.3 | cc:TODO |
| 72.1.5 | [PR Closeout] `harness-work --pr` または `scripts/harness-pr-closeout.sh` を追加し、review APPROVE 後に PR title/body を evidence pack から作る。PR 作成は外部 side effect なので default は dry-run または明示確認 gate とし、`harness-review` から自動 push/PR/merge はしない。 | (a) PR closeout artifact が `base_ref`, `head_ref`, `spec_path`, `lane`, `stage`, review command, focused tests, accepted findings, rejected findings, release-preflight warnings, residual risk を含む、(b) default dry-run で PR body preview を出し、明示 flag または confirmation なしに `git push` / `gh pr create` しない、(c) detached HEAD では branch 作成が必要なことを fail-fast または ask gate で扱う、(d) `bash tests/test-pr-closeout.sh` または同等 test PASS、(e) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` 経路と矛盾しない | 72.1.4 | cc:TODO |
| 72.1.6 | [Mirror / Final Validation] Phase 72 全体の mirror / distribution / no-regression closeout。`skills/` SSOT から `codex/.codex/skills/`, `opencode/skills/`, `.agents/skills/` へ同期し、root `spec.md` と Plans lane/stage 契約が Claude / Codex / OpenCode で drift しないことを検証する。 | (a) `bash scripts/sync-skill-mirrors.sh --check` PASS、(b) `bash tests/test-skill-design-contract.sh` PASS、(c) `bash tests/test-bootstrap-routing-contract.sh` PASS、(d) `bash tests/test-codex-package.sh` PASS、(e) `node scripts/validate-opencode.js` PASS、(f) `bash tests/test-distribution-archive.sh` PASS、(g) `bash scripts/release-preflight.sh --check-adapters` または current supported equivalent PASS、(h) `./tests/validate-plugin.sh` PASS、(i) `Plans.md` の 72.1.1-72.1.6 が evidence commit refs 付きで `cc:完了` に更新される | 72.1.1, 72.1.2, 72.1.3, 72.1.4, 72.1.5 | cc:TODO |

---

## Phase 77: Public issue intake closeout + Done marker canonicalization [P1]

Purpose: v4.12.2 release 後に残った open PR / Issue を、公開ユーザーの痛みと release safety の順に整理する。PR #121 は中国語 description i18n の大型・競合中 PR として後日対応に回し、この Phase では触らない。Issue #147 は単なる parser alias 追加では相手の痛みを解消しないため、新規・更新時の Plans.md / summaries / `.claude/state` notification が `cc:done` を含む English marker family に統一されることを product contract にする。既存 `cc:TODO`, `cc:WIP`, `cc:完了`, `pm:依頼中`, `pm:確認済` は read-compatible として残し、bulk migration は明示コマンドまたはユーザー承認なしに実行しない。Issue #146 は guardrail / runtime reason strings の English default 化、Issue #140 / #131 は既存実装が満たしている範囲を再検証して closeout、PR #136 / #137 は checks rerun 後の低リスク dependency merge、Issue #149 は listing PR を出すかコメントで判断する。

Spec delta: `spec.md` に I18n And Status Marker Contract を追加し、`docs/i18n-language-contract.md` を Issue #147 対応へ更新した。新しい product rule は「新規・更新 writer output は English marker family (`cc:done` など) を emit し、legacy/Japanese markers は read-compatible として受け続ける」。これにより前回案の弱点だった「raw Plans.md の混在言語は残る」問題を解消対象に入れる。Japanese opt-in は説明文・テンプレート文・runtime prose を日本語にできるが、status marker の新規・更新出力は English marker family のままにする。

New Session Start: 次は 77.1.10 から開始する。77.1.2-77.1.3 で #147 の failing tests と canonical marker writer/read compatibility は green になった。77.1.4 は既存 i18n locale resolver / Go guardrail tests で #146 対応済みを確認した。77.1.5-77.1.6 は `docs/research/phase-77-issue-closeout.md` に closeout evidence / comment draft を残した。77.1.7 は PR #136/#137 を個別 merge し、main の `validate-plugin` / `scorecard` が両方 success になったことを確認した。77.1.8 は awesome-codex-plugins の current CONTRIBUTING / README / bundle 状態を確認し、現時点では icon + `interface.composerIcon` と同名 entry ambiguity が未解消のため external PR はまだ出さない判断を evidence 付きで固定した。77.1.9 は Codex companion review の auth failure を記録したうえで manual-pass review closeout を実施し、`docs/research/phase-77-review-closeout.md` に APPROVE evidence を残した。Public issue close/comment は #147 と #131 が main に入ってから実施するため 77.1.10 に分離する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 77.1.1 | `[Plan / Contract Freeze]` `[lane:gate]` `[tdd:skip:docs-contract]` open PR / Issue intake の方針を固定する。#121 は後日対応に回し、#147 は alias-only ではなく new/update writer / notification cleanup として扱う。`spec.md`, `docs/i18n-language-contract.md`, `Plans.md` に product contract と task contract を追加する。 | (a) `spec.md` に new/update status marker contract がある、(b) `docs/i18n-language-contract.md` が Issue #147 と English marker writer output / legacy read compatibility を説明、(c) `Plans.md` に Phase 77 task があり #121 deferred / #147 non-alias-only 方針が明記される、(d) `git diff --check` PASS | 76.1.4 | cc:done |
| 77.1.2 | `[Issue #147 / Tests First]` `[lane:gate]` `[tdd:required]` Status marker localization の failing tests を先に作る。new/update writer/template/notification が `pm:requested`, `cc:todo`, `cc:wip`, `cc:done`, `pm:approved` の同一 family を使い、legacy `cc:TODO`, `cc:WIP`, `cc:完了`, `pm:依頼中`, `pm:確認済` を読み続けることを固定する。 | (a) parser tests が `cc:todo`, `cc:wip`, `cc:done`, `pm:requested`, `pm:approved` と legacy markers の両方を PASS、(b) writer/template tests が mixed marker family を出さない、(c) Japanese opt-in test が prose を日本語化しても marker writes は English family のままになることを固定、(d) `.claude/state` notification / plans watcher / session summary の visible prose が locale resolver を通る、(e) `bash tests/test-i18n-locale-resolver.sh`, relevant Go tests, `bash scripts/plans-format-check.sh` fixture PASS | 77.1.1 | cc:done |
| 77.1.3 | `[Issue #147 / Implementation]` `[lane:gate]` `[tdd:required]` canonical marker writer と locale-aware status summaries を実装する。対象は Plans writer/update helpers、`go/internal/hookhandler/plans_watcher.go`, `go/internal/session/*`, `scripts/stop-plans-reminder.sh`, `scripts/session-monitor.sh`, `scripts/plans-format-check.sh`, sprint-contract status parsing。 | (a) new Plans rows and marker updates emit English aliases, (b) legacy Plans files are not rewritten during read/sync/dry-run, (c) update commands accept both English and Japanese marker inputs, (d) notification prose follows locale while status marker writes stay English family, (e) Issue #147 closeout comment can truthfully say both Plans marker and notification pain are addressed | 77.1.2 | cc:done |
| 77.1.4 | `[Issue #146 / Guardrail English Reasons]` `[lane:gate]` `[tdd:required]` Guardrail / runtime reason strings の English default 化を完了する。Go guardrail の deny/ask/warn reason と shell hook outputs が locale resolver に従い、`CLAUDE_CODE_HARNESS_LANG=ja` では日本語を維持する。 | (a) default env/config で R01/R02/R03/R04/R05 など user-facing deny/ask reason が English、(b) `CLAUDE_CODE_HARNESS_LANG=ja` で既存相当の日本語、(c) hook JSON shape と Decision は変わらない、(d) `cd go && go test ./internal/guardrail ./internal/hookhandler ./internal/session` PASS、(e) `bash tests/test-i18n-locale-resolver.sh` と `bash tests/test-i18n-japanese-ux-regression.sh` PASS | 77.1.1 | cc:done |
| 77.1.5 | `[Issue #140 / Verify and Close Scope]` `[lane:gate]` `[tdd:skip:verification]` R02/R03 protected-path opt-out issue を現行 Phase 71 実装に照らして再検証する。R03 `.env` ask-list break-glass は満たしているが、R02 opt-out / disabled_rules は安全上 Reject した範囲であることを evidence 付きで整理し、必要なら issue comment / close を行う。 | (a) `harness.toml` の `protectedPathAskList` と `go/internal/guardrail/protected_path_policy.go` の挙動を確認、(b) R03 `.env` ask / hard-deny paths / R02 deny の tests PASS、(c) issue comment draft が「満たした範囲」と「意図的にやらない範囲」を分ける、(d) close する場合は user-facing workaround と safety reason を含む | 77.1.1 | cc:done |
| 77.1.6 | `[Issue #131 / Verify and Close]` `[lane:gate]` `[tdd:skip:verification]` codex-loop orphan active job issue を現行実装で再検証する。`runner_lost_job_running`, stop cancellation, cancel failure state, runner exit trap の tests を実行し、再現が解消しているなら closeout comment を作る。 | (a) `bash tests/test-codex-loop-cli.sh` の runner-lost / cancellation 関連 cases PASS、(b) `scripts/codex-loop.sh` が dead runner + active job を `state_stale` だけで潰さない、(c) stop が active companion job を cancel or failure state に残す、(d) issue closeout comment に test command と behavior summary がある | 77.1.1 | cc:done |
| 77.1.7 | `[PR #136 / #137 Dependency Merge]` `[lane:release]` `[tdd:skip:dependency-bump]` Dependabot PR #136 と #137 を checks rerun 後に個別 merge する。どちらも GitHub Actions major upgrade なので、merge 後 main の validate-plugin / scorecard / smoke-install / release-preflight 境界を確認する。 | (a) #136/#137 の current checks が rerun または最新 commit で success、(b) labels missing (`ci`, `dependencies`) は別 maintenance として記録、(c) each PR is merged one at a time, (d) main CI success after merge, (e) no tag/release action is triggered unless explicitly requested。Evidence: #136 merged as `9fd2071b9c40cdd3a4ba6b4e427af8fd89ee302e`; #137 merged as `5f33130b3e6bd95de10e97bb0dfdb1faa75bfda9`; main `validate-plugin` runs `26377642567` / `26377646959` success; main `scorecard` runs `26377642555` / `26377646960` success; no tag/release action was triggered. | 77.1.1 | cc:done |
| 77.1.8 | `[Issue #149 / Listing Decision]` `[lane:fast]` `[tdd:skip:docs-only]` awesome-codex-plugins 掲載提案を判断する。出すなら external README PR のみ、出さないなら issue に理由付きでコメントする。Harness 側 README に premature support claim を追加しない。 | (a) awesome-codex-plugins の CONTRIBUTING format を確認、(b) listing text が current Codex support tier を超えない、(c) external PR or issue comment draft がある、(d) Harness repo に不要な docs churn を入れない。Evidence: target main `68f58ceb19873302b0e771040d47050fc0e5f638`; `CONTRIBUTING.md` requires mirrored bundle + `.codex-plugin/plugin.json` + `interface.composerIcon`; existing target README has `dadwadw233/claude-code-harness` same-name entry; this repo has no `interface.composerIcon` or dedicated icon asset; decision/comment draft recorded in `docs/research/phase-77-issue-closeout.md`. | 77.1.7 | cc:done |
| 77.1.9 | `[Review / Closeout]` `[lane:gate]` Phase 77 の review closeout。#147 / #146 の user pain が実際に解消したか、#140/#131 の close 判断が過大 claim でないか、#136/#137 merge 後 CI が green かをまとめて確認する。 | (a) `harness-review` or `scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE、(b) `bash tests/test-i18n-locale-resolver.sh`, `bash tests/test-i18n-japanese-ux-regression.sh`, relevant Go tests, `bash tests/test-codex-loop-cli.sh`, `bash tests/validate-plugin.sh` PASS、(c) open issues/PRs の残件と public close boundary を明記、(d) CHANGELOG に user-facing fixes を記録、(e) release が必要なら separate `harness-release` gate に渡す。Evidence: companion review failed due Codex auth refresh; `harness-review` manual-pass fallback APPROVE recorded in `docs/research/phase-77-review-closeout.md`; tests passed (`test-codex-loop-cli.sh` 42/42, `validate-plugin.sh` 95/95); public issue close/comment moved to 77.1.10 because #147/#131 fixes must land on main first. | 77.1.2, 77.1.3, 77.1.4, 77.1.5, 77.1.6, 77.1.7, 77.1.8 | cc:done |
| 77.1.10 | `[PR / Public Issue Closeout]` `[lane:release]` `[tdd:skip:external-closeout]` Phase 77 branch を PR 化し、main merge 後に public issue close/comment を実行する。#147 と #131 はこの branch の fix が main に入ってから close、#140/#146 は evidence comment 付き close、#149 は listing blocker comment を残して open 維持、#121 は deferred のまま維持する。 | (a) PR body が #147/#146/#140/#131/#149 の boundary と test evidence を含む、(b) branch CI が green、(c) merge 後 main CI が green、(d) #147/#146/#140/#131 に過大 claim のない close/comment を残す、(e) #149 に icon / `interface.composerIcon` / same-name ambiguity の blocker comment を残す、(f) #121 を触らない、(g) release が必要なら別 `harness-release` gate に渡す。Evidence: PR #150 merged as `d015c803`; branch checks passed including CodeRabbit; main `validate-plugin` run `26379521125`, `smoke-install` run `26379521124`, and `scorecard` run `26379521127` succeeded; clean main preflight passed (`git diff --check`, `bash tests/test-plans-status-markers.sh`, `bash tests/validate-plugin.sh` 95/95); #147/#131/#140/#146 closed with boundary comments; #149 commented and left open; #121 untouched; no release was triggered because Phase 77 closeout did not request a new version/tag, so any release remains a separate `harness-release` gate. | 77.1.9 | cc:done |

---

## Phase 80: Claude Code 2.1.143-2.1.152 + Codex 0.131-0.134 upstream refresh [P1]

Purpose: 2026-05-27 時点の installed/latest は `claude 2.1.152` と `codex-cli 0.134.0` で、`harness-mem versions` でも両方 `up_to_date` と確認した。既存 snapshot は Claude Code `2.1.133-2.1.142` (`docs/upstream-update-snapshot-2026-05-15.md`) と Codex `0.130.0` (`docs/upstream-update-snapshot-2026-05-10.md`) までなので、Claude Code `2.1.143-2.1.152` と Codex `0.131-0.134` を公式 source で棚卸しし、Harness に採るものだけを A/C/P/Reject で実装計画へ落とす。Claude 側の主な候補は `disallowed-tools`, `/reload-skills`, `SessionStart.reloadSkills`, `hookSpecificOutput.sessionTitle`, `MessageDisplay`, `pluginSuggestionMarketplaces`, Auto mode consent 廃止, `/simplify` -> `/code-review` 変更, `claude agents --json`, plugin/sandbox/worktree fix 群。Codex 側の主な候補は `--profile` primary 化, `on-failure` deprecation / granular approval, MCP environment targeting / OAuth, read-only MCP parallel calls, connector schema `$ref`/`$defs`, subagent identity hook input, plugin icon assets, curl/PowerShell installer, managed network proxy, package/release artifact simplification。Feature Table / CHANGELOG 追記だけで終わらせず、snapshot -> adoption plan -> tests -> mirror -> review の gate で閉じる。

Spec delta: `spec.md` に Upstream Tracking Contract を追加した。新しい rule は「Claude Code / Codex update は release note の転記ではなく、local/latest version check、公式 source、dated snapshot、A/C/P/Reject 分類、B=0 原則、Plans/tests/docs/CHANGELOG/review 接続、support-tier gate で扱う」。また upstream 側で auto mode などの opt-in barrier が緩んでも、Harness は専用 phase と test/release note なしに安全 default を緩めない。

team_validation_mode: `subagent`。Product/Architecture/Local-map/Skeptic 視点を subagent で分担し、main run では公式 source、repo SSOT、harness-mem versions、`.claude/memory`、`/Users/tachibanashuuta/.codex/memories/MEMORY.md` を突き合わせた。Codex subagent の一部 release-detail claim は official `rust-v0.134.0` body で確認できないものがあったため、Phase 80 の DoD では「公式 source にない subagent claim は Reject or Unknown」として扱う。

New Session Start: まず 80.1.1 から開始する。実装ではなく `docs/upstream-update-snapshot-2026-05-27.md` の evidence artifact を作り、Claude `2.1.143-2.1.152` / Codex `0.131-0.134` の全 item を A/C/P/Reject に分類する。80.1.2 で security/supply-chain と support claim gate を固定してから、80.1.3 以降の Claude/Codex runtime/docs 変更に入る。release/tag はこの Phase に含めず、release が必要になった場合は別 `harness-release` gate に渡す。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 80.1.1 | `[Research / Evidence]` `[lane:gate]` `[tdd:skip:research-doc]` Claude Code `2.1.143-2.1.152` と Codex `0.131-0.134` の upstream snapshot を作る。公式 source は Anthropic Claude Code release/changelog、OpenAI Codex release/docs、`harness-mem versions`、local `claude --version` / `codex --version`。 | (a) `docs/upstream-update-snapshot-2026-05-27.md` が作成され、local/latest version、release URL、published_at、observed gap、subagent validation summary を持つ、(b) 全 item が `A` / `C` / `P` / `Reject` に分類され `B: 書いただけ 0 件` の理由がある、(c) Claude `2.1.152` release の `disallowed-tools`, `/reload-skills`, `SessionStart.reloadSkills`, `MessageDisplay`, Auto mode consent 変更を含む、(d) Codex `0.134.0` release の `--profile`, MCP environment/OAuth, read-only MCP parallelism, connector schema, subagent identity, installer docs を含む、(e) official source にない subagent claim は `Unknown` / `Reject` として残す、(f) `git diff --check` PASS | 79.1.3 | cc:完了 |
| 80.1.2 | `[Adoption / Threat Model]` `[lane:gate]` `[tdd:skip:design-doc]` upstream item を Harness に採るかどうかの採用判断を固定する。Claude の `disallowed-tools`, `MessageDisplay`, `SessionStart.reloadSkills`, plugin marketplace suggestion, Auto mode consent 廃止、sandbox/worktree permission fix と、Codex の profile/approval/MCP/supply-chain/runtime smoke を security / supply-chain / support-tier で評価する。 | (a) `docs/upstream-adoption-plan-2026-05-27.md` があり、Adopt / Defer / Reject / Auto-inherit と Why を持つ、(b) `MessageDisplay` は採用する場合も audit付き opt-in 表示補助に限定し、assistant output の黙示改変は禁止、(c) upstream Auto mode consent 廃止を理由に `--auto-mode` default 化や hard deny 緩和をしない、(d) Codex CLI は `internal-compatible`, Codex app は `candidate` のまま support claim gate を維持、(e) `claude-codex-upstream-update` を local-only operator tooling のままにするか配布 surface に昇格するかを決める、(f) `bash tests/test-spec-ssot-workflow.sh` PASS | 80.1.1 | cc:完了 |
| 80.1.3 | `[Claude Runtime / Skill-Hook Adaptation]` `[lane:gate]` `[tdd:required]` Claude Code `2.1.143-2.1.152` に合わせて skill / hook / settings / docs を更新する。候補は `disallowed-tools` audit、`/reload-skills` と `/reload-plugins` の使い分け、`SessionStart.reloadSkills` 同一セッション反映、`hookSpecificOutput.sessionTitle`、`MessageDisplay` opt-in policy、`/simplify` references の `/code-review --fix` 置換、`claude agents --json` diagnostic usage、plugin validation強化。 | (a) `skills/*`, `agents/*`, `.claude-plugin/hooks.json`, `hooks/hooks.json`, `templates/claude/settings.security.json.template`, docs の対象箇所が更新される、(b) `allowed-tools` を restriction と誤解している説明があれば `disallowed-tools` / permission deny へ直す、(c) `/code-review --fix` は working-tree write を伴うため自動DoDには入れず operator-local spike として扱う、(d) `tests/test-claude-upstream-integration.sh` が Phase 80 Claude items を検出、(e) `bash tests/test-claude-upstream-integration.sh` と `bash tests/validate-plugin.sh` PASS | 80.1.2 | cc:完了 |
| 80.1.4 | `[Codex Native Config / Companion Adaptation]` `[lane:gate]` `[tdd:required]` Codex `0.131-0.134` と OpenAI docs に合わせて Codex setup / companion / config / docs を更新する。候補は curl/PowerShell installer docs、`--profile` primary 化、`on-failure` deprecation、granular approval、explicit sandbox flags、MCP environment/OAuth、read-only MCP parallelism、plugin icon asset sharing、AGENTS/skills discovery、structured `codex exec --output-schema`。 | (a) `scripts/setup-codex.sh`, `scripts/codex-companion.sh`, `codex/.codex/config.toml`, `.codex-plugin/plugin.json`, `codex/AGENTS.md`, docs が current OpenAI docs と矛盾しない、(b) `--full-auto` / raw `codex exec` を default route に戻さず companion policy を維持、(c) `on-failure` を新規 docs/config で推奨しない、(d) `tests/test-codex-package.sh`, `tests/test-codex-plugin-adapter.sh`, relevant companion tests PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 80.1.2 | cc:完了 |
| 80.1.5 | `[SSOT / Mirror / Support Claim Sync]` `[lane:gate]` `[tdd:required]` Phase 80 の変更を repo SSOT と public support boundary に反映する。`spec.md` status、Phase 72 TODO の残件、Feature Table、CHANGELOG、tool capability matrix、README support wording、skill mirrors を棚卸しする。 | (a) `docs/CLAUDE-feature-table.md` に Phase 80 row がある、(b) `CHANGELOG.md` `[Unreleased]` に user-facing upstream follow-up が記録される、(c) `spec.md` / `Plans.md` / `docs/plans/spec-ssot.md` の phase status と残件が矛盾しない、(d) `docs/tool-capability-matrix.md` と README/README_ja が Codex CLI/App support tier を過大 claim しない、(e) `bash tests/test-tool-capability-matrix.sh`, `bash tests/test-support-claim-wording.sh`, `bash scripts/sync-skill-mirrors.sh --check` PASS | 80.1.3, 80.1.4 | cc:完了 |
| 80.1.6 | `[Validation / Review / PR Closeout]` `[lane:release]` `[tdd:skip:external-closeout]` Phase 80 全体の validation と review closeout。implementation green、snapshot/adoption docs、support boundary、subagent findings の採否を evidence pack にする。release/tag は作らない。 | (a) `git diff --check`, `bash tests/test-claude-upstream-integration.sh`, `bash tests/test-harness-plan-quality.sh`, `bash tests/test-spec-ssot-workflow.sh`, `bash scripts/sync-skill-mirrors.sh --check`, `bash tests/test-codex-package.sh`, `bash tests/validate-plugin.sh` PASS、(b) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE or auth failure + manual-pass fallback evidence、(c) PR body includes official sources, A/C/P/Reject summary, security/supply-chain decisions, tests, support-tier boundary, residual risk、(d) no tag/release action is triggered、(e) Plans markers update after evidence is complete | 80.1.5 | cc:完了 |

---

## Phase 81: Cursor CCH Adapter (candidate) [P1]

Purpose: Cursor から CCH の Plan → Work → Review を使える adapter candidate を、support claim なしで evidence / contract / skeleton / smoke まで通す。PM handoff (`docs/CURSOR_INTEGRATION.md`) と adapter support を分離し、Breezing の multitask 境界と model routing を core contract から切り離す。

New Session Start: Phase 81.1 から開始。Cursor は smoke 完了まで `candidate` のまま。README や release notes で supported claim を出さない。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 81.1 | `[Evidence]` Cursor 公式 docs と repo 契約を照合し adapter candidate evidence を更新する。 | (a) `docs/research/cursor-adapter-candidate.md` が公式 surfaces と PM handoff 分離を持つ、(b) `docs/CURSOR_INTEGRATION.md` が handoff-only boundary を明示、(c) `not_observed != absent` を維持 | - | cc:完了 |
| 81.2 | `[Contract]` spec / capability matrix / bootstrap routing に Cursor 昇格条件と claim boundary を追加する。 | (a) `spec.md` Cursor adapter row 更新、(b) matrix に Cursor 列、(c) bootstrap contract に Cursor AGENTS route + smoke、(d) `bash tests/test-tool-capability-matrix.sh`, `bash tests/test-bootstrap-routing-contract.sh` PASS | 81.1 | cc:完了 |
| 81.3 | `[Skeleton]` setup/test/preflight が消費する Cursor adapter skeleton を追加する。 | (a) `.cursor-plugin/plugin.json`, `.cursor/AGENTS.md`, agents/hooks/mcp 存在、(b) harness-plan/work/review routing 可視、(c) `bash tests/test-cursor-adapter-candidate.sh` PASS | 81.2 | cc:完了 |
| 81.4 | `[Breezing]` Cursor mapping と multitask 境界を core から分離する。 | (a) `skills/breezing/SKILL.md`, `skills/harness-work/SKILL.md`, `docs/team-composition.md` 更新、(b) parallel worker / serial review 契約維持 | 81.3 | cc:完了 |
| 81.5 | `[Model Routing]` Cursor host を model router に追加し override 優先順位を固定する。 | (a) `scripts/model-routing.sh --host cursor`, (b) `docs/model-routing-policy.md`, (c) `agents/advisor.md` Opus 4.7 整合、(d) `bash tests/test-model-routing.sh` PASS | 81.3 | cc:完了 |
| 81.6 | `[Validation]` smoke / support wording / release-preflight gate を追加する。 | (a) `tests/test-cursor-adapter-candidate.sh`, (b) `scripts/release-preflight.sh` adapter gate, (c) `bash tests/test-support-claim-wording.sh`, (d) `CHANGELOG.md` [Unreleased], (e) validation commands PASS | 81.4, 81.5 | cc:完了 |

## Phase 82: cursor-agent CLI workflow smoke 検証 (candidate, 配布なし) [P1]

Purpose: Phase 81 で skeleton / static smoke / model routing まで通した Cursor adapter について、`cursor-agent -p --model composer-2.5` の headless 実行が実際に動くかを **support claim なし・配布なし**で検証する。Route A (shell-out ラッパー) のみを対象とし、Route B (OpenAI 互換 bridge) は二重エージェント問題が致命的なため検証スコープから外す。証拠は `docs/research/cursor-adapter-candidate.md` に集約し、Cursor は `candidate` のまま据え置く。spec.md:181 の昇格条件 (workflow smoke) の materials を貯める段階であり、本フェーズでは昇格・公開・README 変更は行わない。

New Session Start: Phase 82.1 から開始。82.3 の network smoke は Risk Gate (sandbox 無効化 + Cursor cloud への送信) であり、初回実行前にユーザー承認を必須とする。承認なしでは 82.1 / 82.2 (静的・ネットワークなし) までで停止する。

[Decision 2026-05-29] ユーザーが (1) Route B 不採用、(2) 82.3 Risk Gate を承認済み。配布版 (consumer 向け) は本フェーズのスコープ外。検証で得た approval は dev ローカル限定であり consumer には引き継がれない。配布には別途 3 前提が必要: (P1) Cursor cloud host の手動 allowlist recipe — harness は consumer の `~/.claude/settings.json` sandbox を自動無効化できない (deny + self-audit guard) ため、Firecrawl recipe (`docs/sandbox-allowlist-recipe.md`) と同じ「patch 提示→ユーザー手動編集」パターンで配る。(P2) cursor-agent governance rule — read-only default / `--yolo` 禁止 / wrapper-only。`.claude/rules/codex-cli-only.md` を踏襲し、現状欠如している cursor-agent 統制を新設する。(P3) release preflight 経由の support-tier 昇格 (`candidate` → 上位、`test-support-claim-wording.sh` 緩和)。これらは Phase 82 通過後に Phase 84 (distribution) として起票する (Phase 83 は永続バックエンド機能本体に割当)。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 82.1 | `[Fact-check]` ローカル実測 (ネットワークなし) の事実検証結果を candidate 証拠 doc に記録する。確認済み: `cursor-agent` version `2026.05.28-a70ca7c`、全 spec フラグ実在 (`-p`/`--output-format json`/`--model`/`--force`/`--yolo`/`--mode plan\|ask`/`--resume`/`--continue`)、`timeout`/`gtimeout` 不在、spec 未記載フラグ (`--list-models`/`--sandbox`/`--trust`/`CURSOR_API_KEY`) 発見、`model-routing.sh` に素の `composer-2.5` slug が無い (table は `composer-2.5-fast`/`composer-2-fast` のみ)。 [tdd:skip:docs-only] | (a) `docs/research/cursor-adapter-candidate.md` に "cursor-agent CLI fact-check (local, no network)" 節を追加し確認済み/未確認を表で区別、(b) Route B を「検証対象外」と理由付きで明記、(c) support claim / README 変更なし、(d) `bash tests/test-support-claim-wording.sh` PASS | - | cc:done [79d785e6] |
| 82.2 | `[Static smoke]` ネットワークなしで動く検証ハーネスを追加する。`cursor-agent` 存在 tri-state (`not-configured`/`unreachable`/`healthy`) と model-routing の cursor tier マッピングを静的に assert する。モデル呼び出しは `HARNESS_CURSOR_AGENT_SMOKE=1` の時だけ。CLI 不在は WARNING+pass。 [tdd:required] | (a) `tests/test-cursor-agent-cli-smoke.sh` を追加、(b) `not-configured`/`unreachable`/`healthy` の 3 arm を持つ (active-watching-test-policy 準拠)、(c) default CI path は静的のみ・`validate-plugin.sh` を網羅しない、(d) `bash tests/test-cursor-agent-cli-smoke.sh` PASS | 82.1 | cc:done [79d785e6] |
| 82.3 | `[Network smoke — Risk Gate]` opt-in の単一 go/no-go 実行。throwaway dir (`mktemp -d`) で `cursor-agent -p --output-format json --model composer-2.5 --mode ask "Reply with PONG"` を実行 (NO `--yolo`、cursor-agent `--sandbox enabled`)。`.result` schema・エラー時の key 形状・レイテンシ (`time`) と素の `composer-2.5` slug の可用性を記録する。 [tdd:skip:external-auth-required-non-deterministic] | (a) 実行前にユーザーが Risk Gate (sandbox 無効化 + cloud 送信) を承認 [2026-05-29 承認済]、(b) 結果・レイテンシ・エラー形状を証拠 doc に記録、(c) merge を gate しない、(d) repo working tree 外で実行されたことを記録、(e) Cursor cloud egress hostname を記録 (配布版 Phase 83 の allowlist recipe 材料、P1 用) | 82.2 | cc:done [6c6648b1] |
| 82.4 | `[Decision]` go/no-go と Route B 不採用を記録する。 | (a) 証拠 doc に「Route A 検証結果 / Route B 不採用 (理由: 二重エージェント問題)」の判断を記録、(b) Cursor は `candidate` のまま、(c) `CHANGELOG.md` [Unreleased] に検証のみ (support claim なし) と記載、(d) `bash tests/test-support-claim-wording.sh` PASS | 82.3 | cc:done [25d9e5f1] |

## Phase 83: Persistent execution-backend selection + Cursor implementation backend (local opt-in, candidate) [P1]

Purpose: 「脳みそ Opus 4.8 / 体 composer-2.5」構成を実現する。Lead(Opus) が plan/review/integrate を担い、Worker(body) の実装を `cursor-agent --model composer-2.5-fast` に **ガッツリ委譲**する (Route A、既存 Codex モードと同型)。backend は `claude`(既定)/`codex`/`cursor` から選び、**選好を永続化して毎回フラグ不要**にする。安全性は *使用を制限するのでなく、Codex と同等の containment* で担保する: cursor-agent 自前の `--sandbox enabled` + 専用 `.git` worktree + 通信は allowlist + env-guard + Lead の diff レビュー&cherry-pick。実装作業そのものは通常 Worker と同じ「worktree で書く→Lead がレビュー」ライフサイクルであり、composer の作業量は制限しない。Cursor は `candidate` のまま、consumer 配布はしない (配布前提は Phase 84)。

Decision基盤: [2026-05-29] (1) composer は実装を制限せず**ガッツリ担う** = Claude Worker と同じ worktree→review ライフサイクル。「制限」ではなく「安全装備を着せて全力で使う」。(2) Codex 前例 (実測): `codex-companion.sh task --write` → `--sandbox workspace-write` + `codex-primary-environment-guard.sh`、丸裸の `--force`+sandbox無効では一度も走らせていない。(3) [2026-05-29 止める検証] cursor 自前の `--sandbox enabled` は **workspace 外への書込を止めなかった** (Codex workspace-write 相当ではない)。確実な停止は `--mode ask`/`plan` (読取専用、`--force` でも破れない=実測)。**書込モードの封じ込めは cursor 自前 sandbox に頼らず、CC 外側 sandbox の write 範囲を worktree に限定 + 通信 allowlist で行う** (83.3b で cursor-agent が外側 sandbox 配下で動くか検証)。読取委譲 (`--mode ask`) は ack 不要、書込委譲は 83.3b で外側 sandbox 封じ込めが確認できるまで per-session Risk Gate。worktree(専用`.git`)+Lead review は常時必須。完全 default-on の丸裸 `--force`+sandbox無効のみ不採用 (3視点合意)。

Spec delta: root `spec.md` に「execution backend selection」を追加 — backend ∈ {`claude`(default), `codex`, `cursor`(candidate)}、precedence = per-command flag > `HARNESS_IMPL_BACKEND` env > default `claude`、cursor は opt-in で実装委譲は Codex 同等 containment (自前 `--sandbox enabled` + worktree + allowlist + env-guard)、support tier は不変 (cursor=candidate)。Harness が delta を生成、consumer は承認・修正のみ。

New Session Start: Phase 83.1 から開始。検証済 (83.3a): cursor-agent は書込でき、`--mode ask` は確実な読取専用ストップ、ただし cursor 自前 `--sandbox enabled` は **workspace 外を閉じ込めない**。次の make-or-break は **83.3b** = CC 外側 sandbox (allowlist + write=worktree 限定) で書込モードを封じ込められるか。読取委譲は今すぐ安全 (`--mode ask`)、書込委譲は 83.3b 次第で ack 要否確定。永続化対象は backend 選好のみ。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 83.1 | `[Spec]` 上記 Spec delta を root `spec.md` に反映する。 [tdd:skip:docs-only] | (a) `spec.md` に backend 3値 / precedence (flag > `HARNESS_IMPL_BACKEND` > `claude`) / cursor=candidate・opt-in・per-session write gate を明記、(b) support tier 表は不変 (cursor=`candidate`)、(c) `bash tests/test-support-claim-wording.sh` PASS | - | cc:done [991a16e7] |
| 83.2 | `[Persist]` `scripts/set-impl-backend.sh <claude\|codex\|cursor>` (env.local に `HARNESS_IMPL_BACKEND=` を冪等書込、`scripts/enable-1h-cache.sh` 准拠、`--show`/`--unset` 対応) + `scripts/resolve-impl-backend.sh` (precedence flag > env > default `claude`)。[tdd:required] | (a) 2 script 追加、(b) precedence テスト (flag が env を上書き / env が default を上書き) PASS、(c) cursor 不在環境でも resolve は `cursor` を返す (実行可否は別判定)、(d) `bash tests/test-shell-lint.sh` PASS | - | cc:done [fe06949e] |
| 83.3a | `[Spike — make-or-break]` cursor-agent `--sandbox enabled --force` が throwaway worktree 内で **ファイル書込できるか**を実測する (Codex `workspace-write` 相当か)。Risk Gate: network のため allowlist 追加 or sandbox 無効化が要る。[tdd:skip:spike-verification] | (a) 「write a file foo.txt containing PONG」を `--sandbox enabled --force` で実行し書込可否を記録、(b) 結果を `docs/research/cursor-adapter-candidate.md` に追記、(c) 可否で 83.3 の既定経路 (enabled vs disabled+ack) を確定 | 82.3 | cc:done [66f281b8] |
| 83.3 | `[Wrapper]` `scripts/cursor-companion.sh task [--write] "<prompt>"` (codex-companion 准拠)。model は `bash scripts/model-routing.sh --host cursor --role worker --field model` で解決、read-only 既定、`--write` は `cursor-agent -p --output-format json --model <m> --workspace <worktree>` を実行し、自動実行は **`--force` でなく `~/.cursor/permissions.json` の `Allowlist (with Sandbox)`** に依存 (Cursor 公式が Run Everything を "Never use")。`--force` は明示 opt-in fallback のみ。読取委譲は `--mode ask`。`.result` 抽出だが **exit code 必須チェック** (エラー時 stdout JSON 無し=Phase 82 知見)、専用 `.git` worktree に confine (write 封じ込めは cursor 非依存、83.3b の外側 sandbox 検証に従う)。[tdd:required] | (a) script 追加、(b) exit-code チェックテスト (`jq -r .result` 単独に依存しない)、(c) read-only 既定・`--write` opt-in テスト、(d) env-guard で worktree 外書込を拒否するテスト、(e) 静的 + opt-in network smoke (`HARNESS_CURSOR_AGENT_SMOKE=1`)、(f) `bash tests/test-shell-lint.sh` PASS | 83.2, 83.3a | cc:done [bcadfc52] |
| 83.3b | `[Spike — write containment]` cursor 自前 sandbox は閉じ込めない (83.3a follow-up で実測済) ため、**CC 外側 sandbox で封じ込められるか**を検証する。`~/.claude/settings.json` sandbox を「network=Cursor egress allowlist + filesystem write=worktree のみ」に設定した状態で cursor-agent が (i) 動作し (ii) worktree 外書込を拒否されるかを確認。[tdd:skip:spike-verification] | (a) 外側 sandbox 封じ込め下で cursor-agent が worktree 内に書け、worktree 外書込が deny される (または不可と判明) を記録、(b) 結果を `docs/research/cursor-adapter-candidate.md` に追記、(c) 書込モードが ack 不要化できるか (封じ込め成立) / per-session Risk Gate 据置か を確定 | 83.3a | cc:done [25d9e5f1] |
| 83.4 | `[Governance]` `.claude/rules/cursor-cli-only.md` 新設。**Cursor 公式統制 (cursor.com/docs/agent/security) を採用**: `--force`(Run Everything、公式 "Never use") を避け `~/.cursor/permissions.json` の `Allowlist (with Sandbox)` で許可コマンドのみ自動実行 / 読取委譲は `--mode ask` / `.cursorignore` に `.env`・secrets・`.git` を列挙し読取遮断 (exfil 対策) / 専用 `.git` worktree 隔離 / Lead-only cherry-pick / cursor の書込は project に閉じ込められない&R01-R13 は cursor 側で効かない旨を明示。wrapper-only。[tdd:skip:docs-only] | (a) rule 追加、(b) `CLAUDE.md` に 1 行ポインタ、(c) `.claude/rules/codex-cli-only.md` と非矛盾、(d) `~/.cursor/permissions.json` (`terminalAllowlist`/`mcpAllowlist`) + `.cursorignore` テンプレを記載し、**allowlist は公式に best-effort で security boundary ではない・実境界は worktree+Lead review** と明示、(e) settings.json への `Bash(cursor-agent:*)` deny 手動手順を記載 | - | cc:done [503de15c] |
| 83.5 | `[Routing]` harness-work / breezing に backend 3-way switch を配線。`HARNESS_IMPL_BACKEND` は **role-scoped = implementation(worker) ロールのみ**に効かせる。review / advisor は **brain(Opus, claude host) に固定**し body(composer) に自作レビューさせない (model-routing の cursor `review`→composer-2.5-fast はこの用途では使わず、cursor-backed work の reviewer は Lead(Opus) もしくは `--host claude --role reviewer` を使う)。model は既存 `model-routing.sh --host <backend> --role <role>` で解決 (Phase 83 は routing を再実装せず host 値を渡すだけ)。Worker 入力 JSON に `backend` 追加、cursor は cursor-companion へ委譲。非Claude backend では `self_review` 5件ゲートをスキップし diff レビューを唯一の品質ゲートにする (Codex 前例)。cursor-backed run で loud banner を出す。[tdd:required] | (a) `skills/harness-work/SKILL.md` + `skills/breezing/SKILL.md` に backend switch、(b) `agents/worker.md` 入力 schema に `backend`、(c) review/advisor が brain 固定 (cursor backend でも reviewer は composer にしない) を明記、(d) 非Claude で self_review gate N/A・diff review 必須を明記、(e) flag > env precedence テスト、(f) `.claude/rules/opus-4-7-prompt-audit.md` 合格 | 83.2, 83.3, 83.4 | cc:done [4f4ddc65] |
| 83.6 | `[Memory]` body(composer) が brain(Opus) と記憶を共有できるよう `.cursor/mcp.json` に harness-mem を配線する。cursor-agent CLI が project `.cursor/mcp.json` を読むか、global `~/.cursor/mcp.json` 依存かを確認し、決定を記録。[tdd:skip:config-shape] | (a) `.cursor/mcp.json` に harness-mem server 定義 (secret は env 参照、値は埋め込まない)、(b) cursor-agent が project mcp を読むか実測し doc 化、(c) `bash tests/test-cursor-adapter-candidate.sh` の mcp 形状 assert PASS、(d) MCP Trust Policy (CLAUDE.md) 準拠。【closeout 2026-05-29】harness-mem は global `~/.cursor/mcp.json` に既存確認 → repo `.cursor/mcp.json` 配線は冗長。runtime-load 検証は CC 再起動後に持ち越し | 83.3 | cc:done [25d9e5f1] |
| 83.7 | `[Cursor governance — verification-gated]` 「CCH を Cursor 側で執行」案の make-or-break を検証: **Cursor のフックが PreToolUse 相当の deny (protected-path 書込ブロック) を実行できるか** (証拠 doc 79行「Hook runtime block parity ... not observed」)。できる→`.cursor/hooks.json` に R01-R13 相当 guardrail を配線し、不透明 subprocess 問題を構造的に解消 (per-session ack も不要化可)。できない→Cursor フックは observe/warn のみと記録し、containment は 83.3 の sandbox+worktree+env-guard に依存と確定。[tdd:skip:capability-verification] | (a) Cursor hook deny 可否を実測し `docs/research/cursor-adapter-candidate.md` に記録、(b) 可なら `.cursor/hooks.json` に guardrail 配線 + テスト、(c) 不可なら fallback containment を doc 明記、(d) いずれも Cursor は `candidate` のまま。【deferred 2026-05-29】Cursor hook deny の live test は CC 再起動 (allowlist 反映) 後に実施。本体実装をブロックしない独立 spike | 83.4 | cc:todo |
| 83.8 | `[Validation]` テスト集約 / CHANGELOG / boundary。[tdd:skip:test-and-docs-aggregation] | (a) backend precedence + cursor-companion 契約テストが `tests/` で PASS、(b) backend routing 静的 assert (`tests/test-cursor-agent-cli-smoke.sh` 拡張 or 新規)、(c) `CHANGELOG.md` [Unreleased] に local opt-in / candidate / no-support-claim 記載、(d) `bash tests/test-support-claim-wording.sh` + `bash tests/validate-plugin.sh` PASS、(e) Cursor は `candidate` のまま | 83.5, 83.6 | cc:done [aff04e80] |
| 83.9 | `[Codex-host wiring]` Codex ホストから駆動した時も backend スイッチが効くよう `skills-codex/harness-work` (+`skills-codex/breezing`) に Execution Backend Selection を配線する。`resolve-impl-backend.sh` で解決し、worker のみ選択 backend（cursor は `cursor-companion.sh`）、review/advisor は brain 固定。Spec skip reason: spec.md「Execution Backend Contract」が host-neutral に宣言済みで新 product contract なし。[tdd:required] | (a) `skills-codex/harness-work/SKILL.md`(+breezing) に backend switch + `resolve-impl-backend.sh` 参照、(b) role-scoped (review/advisor=brain) 明記、(c) `bash scripts/sync-skill-mirrors.sh` 後 `bash scripts/ci/check-consistency.sh` PASS、(d) `tests/test-cursor-backend-codex-wiring.sh`（skills-codex に backend/resolve 参照があるか grep 契約）追加 + `validate-plugin.sh` に組込、(e) `.claude/rules/opus-4-7-prompt-audit.md` 合格 | 83.5 | cc:done [29ba3bb1] |
| 83.10 | `[Onboarding]` `harness-setup` に cursor backend 導入動線を追加する。AI が編集できない箇所（`~/.cursor/permissions.json`・`.cursorignore`・`~/.claude/settings.json` の `*.cursor.sh` allowlist）は**手動手順として印字**し、AI が実行できる `set-impl-backend.sh [--user] cursor` は案内する。Spec skip reason: 既存能力の提示（導入動線）で product contract 変更なし。[tdd:required] | (a) `skills/harness-setup/SKILL.md` に backend 導入セクション（`set-impl-backend.sh` 案内 + 手動3点: permissions.json/.cursorignore/allowlist）、(b) `.claude/rules/cursor-cli-only.md` と `docs/sandbox-allowlist-recipe.md` を参照、(c) AI 編集不可箇所を手動明示、(d) `bash scripts/sync-skill-mirrors.sh` 後 `check-consistency.sh` PASS、(e) 導入動線の grep 契約テスト追加 + `validate-plugin.sh` 組込、(f) audit 合格 | 83.2, 83.4 | cc:done [29ba3bb1] |

## Phase 84: harness-review closeout fixes + Cursor ACP boundary record [P1]

Purpose: `/harness-review` で APPROVE と同時に出た 3 件の小修正（Plans drift・shellcheck 衛生・cursor-companion デバッグ観測性）をリリース前に整え、Cursor の新プロトコル ACP（cursor.com/docs/cli/acp）を「現状不採用」として境界に記録する。spec.md「Execution Backend Contract」「Onboarding Contract」には影響なし（Spec skip reason: 内部品質・docs・debug UX）。CHANGELOG `[Unreleased]` に追記。

Decision基盤: [2026-05-29] (a) ユーザー判断「3 件すべて実施。デバッグの際必須」。(b) ACP は公式自身が「通常のターミナル運用には `agent` を使え」と書く双方向 streaming プロトコルで、harness の whole-task 委譲（投げて結果1個受け取る）には**過剰**。要件不在のため不採用。`spec.md > Open Decisions` 相当 (任意 docs) と `docs/research/cursor-adapter-candidate.md` に Not adopted を記録し、将来 streaming UX や別 IDE 統合が要件化したら再評価。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 84.1 | `[Plans drift]` 82.4 を `cc:done` に更新（DoD 全項目満足を確認）。Spec skip reason: marker only。[tdd:skip:marker-only] | (a) `Plans.md` 82.4 が `cc:done [<決定 commit hash>]`、(b) `cc:todo` 残数が Phase 83.7 のみ、(c) `bash scripts/ci/check-consistency.sh` PASS | - | cc:done [75ac5cb9] |
| 84.2 | `[Shell hygiene]` shellcheck SC2295 と SC2005 を `scripts/resolve-impl-backend.sh` と `scripts/set-impl-backend.sh` で解消する。`${file_line#export ${KEY}=}` → `${file_line#export "${KEY}"=}`、`echo "$(...)"` の冗長を除去。Spec skip reason: 内部 hygiene。[tdd:skip:lint-only] | (a) 修正後 `shellcheck --severity=info scripts/resolve-impl-backend.sh scripts/set-impl-backend.sh` で SC2295/SC2005 が **0 件**、(b) `bash tests/test-impl-backend.sh` PASS（i〜l の user-scope ケース含む）、(c) `bash tests/test-shell-lint.sh` PASS | - | cc:done [36c20f64] |
| 84.3 | `[Debug observability]` `cursor-companion.sh` に `--debug`（または `HARNESS_CURSOR_DEBUG=1`）を追加。有効時は (i) model-routing 失敗時の原因（exit code・stderr）を stderr に出力、(ii) cursor-agent の実行コマンドラインを mask 後に stderr に出力（`--api-key` 値・`Authorization` ヘッダ等の秘密値は伏字）、(iii) cursor-agent stderr を非サプレスで透過。既定動作は不変（後方互換）。Spec skip reason: debug UX、API は不変。[tdd:required] | (a) `scripts/cursor-companion.sh` に debug 出力経路を追加し、`MODEL_ROUTER` 失敗時の error 抑制（`2>/dev/null \|\| true`）を debug 時のみ無効化、(b) 秘密値 mask テスト（mock で `--api-key` 含む引数を渡し、debug 出力に値が出ないことを assert）、(c) `tests/test-cursor-companion.sh` に debug 経路の assertion を追加（既存テストは不変で PASS）、(d) `shellcheck --severity=error` clean、(e) audit 合格（vague-word grep 0） | - | cc:done [a20fbfb3] |
| 84.4 | `[ACP boundary]` Cursor ACP（Agent Client Protocol、cursor.com/docs/cli/acp）を**現状不採用**として `docs/research/cursor-adapter-candidate.md` に Not adopted セクションを追加する。判断軸：ACP は双方向 streaming プロトコルで、harness の whole-task 委譲には過剰。将来 streaming UX または別 IDE 統合が要件化したら再評価。Cursor は `candidate` のまま。Spec skip reason: docs-only、support tier 不変。[tdd:skip:docs-only] | (a) `docs/research/cursor-adapter-candidate.md` に「## Cursor ACP — Not adopted (2026-05-29)」節を追加、判断と再評価条件を Why 付きで記録、(b) `bash tests/test-support-claim-wording.sh` PASS（ACP=未採用、support claim 追加なし） | - | cc:done [83ff93e7] |
| 84.5 | `[Closeout]` 4 件の改善を CHANGELOG `[Unreleased]` に追記し、`/harness-review` を再実行して APPROVE 0 件 を確認後、`/harness-release` 可能な状態にする。[tdd:skip:docs-and-validation] | (a) `CHANGELOG.md` `[Unreleased]` に "Cursor backend debug observability"・"shell hygiene"・"Plans drift fix"・"Cursor ACP not adopted (rationale recorded)" を追記、(b) `bash tests/validate-plugin.sh` PASS（CC sandbox 無効時 0 failed）、(c) `bash scripts/ci/check-consistency.sh` PASS、(d) `bash scripts/sync-skill-mirrors.sh` 後の mirror 同期、(e) `bash tests/test-support-claim-wording.sh` PASS | 84.1, 84.2, 84.3, 84.4 | cc:done [129072d4] |

## Phase 85: backend topology SSOT + Lead review contract-grep gate [P2]

Purpose: v4.13.0 リリース後の README リファクタリング test-run で観測した 2 つの実運用ギャップを SSOT 化する。(1) 配線誤解: 非 claude backend では Worker 層を介在させず Lead が直接 cursor/codex-companion を呼ぶ（私が Worker を間に挟んで余計な中継を作った）。(2) Lead 1 次レビューが目視 diff だけで契約文字列 (`5動詞ワークフロー` 等の i18n 固定句) の破壊を検知できず、cherry-pick 後に check-consistency で fail → revert 必要だった。両方を skill 正本と shareable rule に反映し、CHANGELOG に「reviewer guidance hardening」として記載する。

Decision基盤: [2026-05-29] ユーザー判断「全部 Yes、リリースまで、(1)(2) 両方」。私（Lead）の test-run でのミスから抽出した再発防止策。spec.md「Execution Backend Contract」「Review Contract」は既存記述で正しく、追加は skill 正本側 (実運用手順) の補強で十分。

Spec skip reason: docs / skill prose only。spec.md・support tier・公開 API に影響なし。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 85.1 | `[Wiring SSOT]` Lead → composer 直接委託の正本表記を強化する。`skills/harness-work/SKILL.md` 「Execution Backend Selection」節 (L78-) と「非 `claude` バックエンドの self_review ゲート」節 (L102-) に「非 claude backend では Worker agent (`claude-code-harness:worker`) を spawn せず、Lead が直接 `cursor-companion.sh` / `codex-companion.sh` を呼ぶ。Worker 層介在は backend=claude のときだけ」を明示。`.claude/rules/cursor-cli-only.md` の「Role scope」節にも同等の 1 行を追記し配信可能化する。`.claude/memory/patterns.md` (ローカル SSOT) には Phase 85 学びを Why 付きで記録する (gitignore 対象なので配布外)。[tdd:skip:docs-only] | (a) `skills/harness-work/SKILL.md` + opencode/codex mirror の該当節に「Worker 介在は backend=claude のみ」明示、(b) `.claude/rules/cursor-cli-only.md` Role scope 節に 1 行追記 (配信可能)、(c) `.claude/memory/patterns.md` に Phase 85 entry (ローカル)、(d) `bash scripts/sync-skill-mirrors.sh` 後 `bash scripts/ci/check-consistency.sh` PASS、(e) audit (vague-word grep) 0 件 | - | cc:done [33814e73] |
| 85.2 | `[Review gate hardening]` Lead 1 次レビューに「目視 diff + contract grep」の二段ゲートを明示する。`skills/harness-work/SKILL.md` のレビューループ節に「cherry-pick 前必須コマンド」テーブルを追加 (`bash tests/test-support-claim-wording.sh` / `bash scripts/ci/check-consistency.sh` / `bash tests/validate-plugin.sh`)。「docs / locale / capability-matrix 等の固定文字列契約は目視 diff では検知できない」根拠と Phase 85 test-run の reference を Why として併記。[tdd:skip:docs-only] | (a) `skills/harness-work/SKILL.md` のレビューループ節 (Review Policy 周辺) に gate テーブル追加、(b) opencode/codex mirror 同期、(c) `bash scripts/ci/check-consistency.sh` PASS、(d) 目視 diff のみで composer 出力を APPROVE しないと明示、(e) audit 合格 | - | cc:done [33814e73] |
| 85.3 | `[Closeout]` 改善を CHANGELOG `[Unreleased]` に追記し、`/harness-release` 可能な状態にする。Bump は **patch** (`4.13.0` → `4.13.1`、内部 hygiene + skill prose のみ・API/機能不変・後方互換)。[tdd:skip:docs-and-validation] | (a) `CHANGELOG.md` `[Unreleased]` に "Lead → composer 直接委託 (Worker 介在は backend=claude のみ)" と "Lead review に contract-grep ゲート追加" を Documentation/Changed として記載、(b) `bash tests/validate-plugin.sh` PASS、(c) `bash scripts/ci/check-consistency.sh` PASS、(d) mirror 同期済み、(e) `bash tests/test-support-claim-wording.sh` PASS | 85.1, 85.2 | cc:todo |
