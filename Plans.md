# Claude Code Harness — Plans.md

最終アーカイブ: 2026-05-08（Phase 47-61 → `.claude/memory/archive/Plans-2026-05-08-phase47-61.md`）
前回アーカイブ: 2026-04-19（Phase 44 + 45 + 46 → `.claude/memory/archive/Plans-2026-04-19-phase44-46.md`）

---

## 📦 アーカイブ

完了済み Phase は以下のファイルへ切り出し済み（git history にも残存）:

- [Phase 47-61](.claude/memory/archive/Plans-2026-05-08-phase47-61.md) — Session Monitor 能動監視 / XR-003 / 3-state 依存テスト規約 / CC 2.1.112-2.1.126 + Codex 0.121-0.128 upstream 追従 / Issue #105 English default + Japanese opt-in / External Issue closeout / Skill orchestration design contract / harness-mem managed companion (v4.6.0-v4.7.0) / Sandbagging-Aware Weak-Supervision Harness
- [Phase 44 + 45 + 46](.claude/memory/archive/Plans-2026-04-19-phase44-46.md) — Opus 4.7 / CC 2.1.99-110 追従 "Arcana" (v4.2.0) + Plugin Manifest 公式準拠 + Worker 3 層防御 (#84-#87, v4.3.0)
- [Phase 37 + 41 + 42 + 43](.claude/memory/archive/Plans-2026-04-17-phase37-41-42-43.md) — Hokage 完全体 / Long-Running Harness / Go hot-path migration / Advisor Strategy
- [Phase 39 + 40 + 41.0](.claude/memory/archive/Plans-2026-04-15-phase39-40-41.0.md) — レビュー体験改善 / Migration Residue Scanner / Long-Running Harness Spike

---

## マーカー凡例

PM ↔ Impl 運用で使用する標準マーカー:

| マーカー | 意味 | 誰が付ける |
|---------|------|-----------|
| `pm:requested` / `pm:依頼中` | PM がタスクを起票し、Impl へ依頼中 | PM |
| `cc:todo` / `cc:TODO` | Impl の未着手タスク | Impl |
| `cc:wip` / `cc:WIP` | Impl（Claude Code）が着手中 | Impl |
| `cc:done` / `cc:完了` | Impl が作業完了し、PM の確認待ち | Impl |
| `pm:approved` / `pm:確認済` | PM が最終確認を完了 | PM |

**状態遷移**: 新規・更新時の正規出力は `pm:requested → cc:todo → cc:wip → cc:done → pm:approved`。既存 `pm:依頼中 → cc:TODO → cc:WIP → cc:完了 → pm:確認済` も read-compatible。

**後方互換**: `cursor:依頼中` / `cursor:確認済` は `pm:依頼中` / `pm:確認済` の同義として扱う（Cursor PM 運用時の表記）。

---

## Phase 62: Claude Code 2.1.112-2.1.132 後続活用 + Opus 4.7 upstream follow-up [P1]

Purpose: Phase 56 / Phase 58 で Claude Code `2.1.119`-`2.1.126` を Plans 化し一部 `cc:完了` になっているが、`2.1.112`-`2.1.118` および `2.1.127`-`2.1.132` の 13 バージョンは Feature Table の詳細追跡対象外。CC GitHub CHANGELOG (`https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`) との比較で、Harness の弱点を直接強化できる Tier 1 5 件 (subagent stall, 1h cache, mcp_tool hooks, sandbox deniedDomains, wrapper bypass) と Phase 58 設計済み実装 pending の Tier 2 5 件 (updatedToolOutput, permissionMode, invocation_trigger, SESSION_ID, skillOverrides) を実装 + test 込みで Plans 化する。`62.3.1` で Phase 58 と同じ 4 点接続 (snapshot / Feature Table / CHANGELOG / integration test) を行い、CC 自動継承で済む UI 改善・性能改善・OAuth fix は明示的に対象外と記録する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 62.1.1 | Claude Code `2.1.113` で導入された subagent stall 10 分 timeout を Worker 契約に組み込む。`agents/worker.md` の retry / escalation 節に CC 側 stall timeout (既定 600 秒) と Harness 側 `elicitation-handler.sh` の能動監視を 2 層防御として併記し、Lead が再 spawn する条件を「`cc:WIP` 状態が 10 分超 または CC stall log を観測」と数値で固定。`docs/team-composition.md` の並列 Worker 数判定 (1/2/3 グループ) に stall 発生時の re-spawn ルールを 1 行追記。`tests/test-claude-upstream-integration.sh` に Phase 62.1.1 の文字列参照を追加し、`.claude/rules/opus-4-7-prompt-audit.md` の数字必須要件 (回数上限・時間上限) を満たすこと | (a) `agents/worker.md` に CC 600s stall + Harness elicitation 2 層防御が明記され `grep -E "600\|10 分\|stall" agents/worker.md` で 1 件以上ヒット、(b) `docs/team-composition.md` に re-spawn 判定条件が「N 分超」「stall log 検出」の数値で書かれる、(c) `tests/test-claude-upstream-integration.sh` が `Phase 62.1.1` を参照検出、(d) `.claude/rules/opus-4-7-prompt-audit.md` の checklist で「回数 / 時間が数字」項目を満たす、(e) `./tests/validate-plugin.sh` 全 PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.2 | Feature Table line 198 で `A: 明示追従対象` 分類済みだが未実装の `ENABLE_PROMPT_CACHING_1H` (CC `2.1.108`) を long-running skill で活用する。`skills/harness-loop/SKILL.md`, `skills/breezing/SKILL.md`, `docs/long-running-harness.md` の 3 箇所に「セッション開始時に `export ENABLE_PROMPT_CACHING_1H=1` を行う」ガイダンスと理由 (5 分 TTL では loop / breezing 1 時間超セッションでキャッシュ miss が累積し input token コストが最大 12 倍になりうる) を 1 段落で追加。`scripts/codex-companion.sh task --write --resume-last` 系の long task で env が inherit されるかを確認し、wrapper の修正が不要であれば理由を docs に残す。skill mirror (`skills-codex/`, `codex/.codex/skills/`, `opencode/skills/`) も sync で追従 | (a) `harness-loop` / `breezing` / `long-running-harness.md` の 3 箇所に env 設定例と理由が記載され `rg "ENABLE_PROMPT_CACHING_1H"` で 3 件以上ヒット、(b) `scripts/codex-companion.sh` の env inherit 状況が `docs/long-running-harness.md` または `docs/upstream-update-snapshot-2026-05-07.md` に記録、(c) `tests/test-claude-upstream-integration.sh` が `ENABLE_PROMPT_CACHING_1H` の参照を検出、(d) `bash scripts/sync-skill-mirrors.sh --check` PASS、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.3 | Claude Code `2.1.118` で hooks が `type: "mcp_tool"` で MCP ツールを直接呼び出せるようになった件の採用判断 doc を作成する。実装は次 Phase 判断 (まず設計のみ)。判断材料: (i) PostToolUse から `harness_mem_record_event` を直接呼ぶ場合の遅延 / auth scope / error 伝播の比較、(ii) 現行 `scripts/hook-handlers/*.sh` shell wrapper 経由 vs 直接呼び出しのトレードオフ表、(iii) MCP server 不達時の fallback (silent skip / local ledger queue / drop) の方針、(iv) Phase 61 の `.claude/state/elicitation/events.jsonl` ローカル ledger との関係 | (a) `docs/hooks-mcp-tool-evaluation.md` がある、(b) shell wrapper 経由 vs `type: "mcp_tool"` 直接呼び出しの比較表 (latency / auth / error / fallback の 4 項目) がある、(c) MCP 不達時の fallback 方針が `silent skip` `queue` `drop` のどれかに明示確定、(d) Phase 61 ledger との重複・補完関係が 1 段落で説明、(e) 採用 / 保留 / 却下のいずれかで判断結論が記録、(f) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.1.4 | Claude Code `2.1.113` で追加された `sandbox.network.deniedDomains` 設定の baseline を Harness テンプレートに導入する。`.claude-plugin/settings.json` の `permissions` に `deniedDomains` baseline (`pastebin.com`, `transfer.sh`, `0x0.st`, `paste.ee`, `gist.github.com` 経由 anonymous gist の制限は対象外で記録のみ) を追加。`harness-init` skill 経由で生成される settings.json に同じ既定値が入ることを確認。`settings.local.json` で project 固有 override を許可。`scripts/ci/check-consistency.sh` で deniedDomains baseline 漏れを検出する | (a) `.claude-plugin/settings.json` の `permissions` ブロックに `deniedDomains` baseline が 4 件以上記載、(b) `skills/harness-init` または同等の生成経路で同じ既定値を出力、(c) `tests/test-settings-baseline.sh` (新規 or 既存拡張) が baseline 既定値の存在を機械検証、(d) `scripts/ci/check-consistency.sh` の本線で deniedDomains 欠落を検出、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] (template canonical, `.claude-plugin/settings.json` sync は user 手動操作) |
| 62.1.5 | Claude Code `2.1.113` で deny ルールが `env`/`sudo`/`watch` wrapper bypass を matching するように強化された件を Harness の Go guardrail で audit する。R06 (force-push), R11 (`reset --hard`), R12 (push to main) の 3 ルールについて、`env GIT_SSL_NO_VERIFY=1 git push --force` / `sudo git push --force` / `watch -n 1 'git push --force'` のような wrapper bypass test を `go/internal/guardrail/rules_test.go` に各 3 ケース合計 9 ケース追加。漏れがあれば `go/internal/guardrail/rules.go` の matcher を強化、無ければ test だけ追加して fix posture を固定 | (a) `go/internal/guardrail/rules_test.go` に R06/R11/R12 の env/sudo/watch wrapper bypass テストが各 3 ケース合計 9 ケース追加、(b) 漏れがあった場合は `rules.go` で対応し commit に diff が残る、(c) `cd go && go test ./internal/guardrail/... -run TestWrapperBypass -v` で 9 ケース全 PASS、(d) `tests/test-claude-upstream-integration.sh` が Phase 62.1.5 の参照を検出、(e) `./tests/validate-plugin.sh` PASS | 61.8 | cc:完了 [6ab79d32] |
| 62.2.1 | Claude Code `2.1.121` の `PostToolUse.updatedToolOutput` を Phase 58.2.2 設計方針 (opt-in / redaction-compaction-normalization 限定 / before-after audit) に従って実装する。`.claude-plugin/hooks.json` で 1 つだけ opt-in の PostToolUse hook を有効化 (例: machine-readable JSON normalization、デフォルトは無効)。元 output と更新後 output を `.claude/state/output-audit.jsonl` に append-only で記録。レビュー / テスト証拠の改ざんを禁止する仕組みを test で検証 | (a) `scripts/hook-handlers/posttool-output-normalize.sh` または Go 実装が opt-in で動作、(b) `.claude/state/output-audit.jsonl` に before/after が JSON Lines で append-only 記録、(c) `tests/test-output-governance.sh` が「redaction 用途は PASS」「テスト出力の改ざん用途は FAIL」を機械検証、(d) JSON 契約 tool では人間向け説明を mix しないことを test で固定、(e) `./tests/validate-plugin.sh` PASS | 58.2.2, 62.1.5 | cc:完了 [028034ef] |
| 62.2.2 | Claude Code `2.1.119` で `--agent <name>` が agent frontmatter の `permissionMode` を確実に尊重する fix の reaffirmation。`agents/worker.md` (`acceptEdits`)、`agents/reviewer.md` (Read-only)、`agents/scaffolder.md` (default + ask) の permissionMode が CC `2.1.119+` で実適用されるかを `tests/test-agent-permission-mode.sh` で機械検証。Phase 59.2.3 で「silently ignored になり得る」と判定された field のうち CC 2.1.119+ で reactivate されたものを agent frontmatter に正式記載 | (a) `tests/test-agent-permission-mode.sh` が worker / reviewer / scaffolder の permissionMode を CC `2.1.119+` で検証、(b) Reviewer の Read-only enforcement (Edit/Write/Bash 制限) が test で固定、(c) silently ignored だった field のうち reactivate されたものは agent frontmatter に正式記載、(d) `agents/team-composition.md` の権限境界記述と一致、(e) `./tests/validate-plugin.sh` PASS | 59.2.3, 62.1.5 | cc:完了 [028034ef] |
| 62.2.3 | Claude Code `2.1.126` で `claude_code.skill_activated` OTel event が `invocation_trigger` (human / model / skill-chain) を含むようになった件を活用する。`scripts/skill-trigger-telemetry.sh` または `bin/harness skill-telemetry` (Go) で trigger 別カウントを `.claude/state/skill-trigger-stats.jsonl` に append-only 記録。privacy / retention (既定 30 日) / opt-out フラグを `docs/skill-telemetry-policy.md` に固定。Phase 58.2.3 の「telemetry sink 設計が先」判断と矛盾しないことを test で確認 | (a) `scripts/skill-trigger-telemetry.sh` (or Go 実装) が `invocation_trigger` を読んで trigger 別カウントを `.claude/state/skill-trigger-stats.jsonl` に記録、(b) `tests/test-skill-trigger-telemetry.sh` が human / model / skill-chain の 3 trigger を区別して記録できることを検証、(c) `docs/skill-telemetry-policy.md` に privacy / retention / opt-out が書かれる、(d) Phase 58.2.3 の判断と矛盾しないことが docs に明記、(e) `./tests/validate-plugin.sh` PASS | 58.2.3, 62.1.5 | cc:完了 [028034ef] |
| 62.2.4 | Claude Code `2.1.132` で Bash subprocess に `CLAUDE_CODE_SESSION_ID` が env で渡るようになった件を活用し、`scripts/hook-handlers/*.sh` の session ID 取得を env 直接参照に統一する。後方互換のため env 空のときだけ `$CLAUDE_TRANSCRIPT_PATH` regex fallback を残す。3 状態 (env あり / env 無し / 両方無し = legacy fallback) を `.claude/rules/active-watching-test-policy.md` の test 規約に準拠して検証 | (a) `scripts/hook-handlers/memory-session-start.sh` 等で `${CLAUDE_CODE_SESSION_ID:-$(extract_from_transcript)}` パターンに統一、(b) regex fallback が CC `2.1.131` 以下でも動作することを test で固定、(c) `tests/test-hook-handler-session-id.sh` が env あり / env 無し / 両方無し の 3 状態を検証、(d) `.claude/rules/active-watching-test-policy.md` の `TestXxx_NotConfigured` / `TestXxx_Healthy` / `TestXxx_Corrupted` 命名規約に準拠、(e) `./tests/validate-plugin.sh` PASS | 50.1.1, 62.1.5 | cc:完了 [028034ef] (refactor 不要を確認、policy doc + test を新設) |
| 62.2.5 | Claude Code `2.1.129` で `skillOverrides` 設定が `off`, `user-invocable-only`, `name-only` の 3 mode をサポートするようになった件のガバナンス doc を作成する。3 mode の意味、推奨 default、enterprise governance 用途、Harness の skill manifest (`scripts/generate-skill-manifest.sh`) との関係を `docs/skill-overrides-policy.md` に固定。`harness-init` で生成する settings に default を入れるかの判断 (推奨は `name-only` だが互換のため明示無効化を採用するか) を記録 | (a) `docs/skill-overrides-policy.md` に 3 mode の意味、推奨 default、enterprise 用途、Phase 59.1.2 の skill manifest との関係が書かれる、(b) `harness-init` で default を入れるかの判断 (採用 / 非採用 / project-specific) が記録、(c) `tests/test-settings-baseline.sh` が skillOverrides の存在を許容 (強制しない) するよう更新、(d) Phase 59.1.2 の skill manifest と矛盾しない、(e) `./tests/validate-plugin.sh` PASS | 59.1.2, 62.1.5 | cc:完了 [028034ef] |
| 62.3.1 | Phase 62 の upstream snapshot / Feature Table / CHANGELOG / integration test 接続を Phase 58.1.1-58.1.2 と同じスタイルで行う。`docs/upstream-update-snapshot-2026-05-07.md` を新規作成し、CC `2.1.112`-`2.1.118` および `2.1.127`-`2.1.132` を A/C/P 分類 (`B: 書いただけ` 0 件) で snapshot 化。`docs/CLAUDE-feature-table.md` に Phase 62 追補。`CHANGELOG.md` `[Unreleased]` に Phase 62 の「今まで / 今後」を CC アプデ → Harness での活用 形式で追記。`tests/test-claude-upstream-integration.sh` で Phase 62 参照漏れを検出 | (a) `docs/upstream-update-snapshot-2026-05-07.md` がある、(b) `2.1.112`-`2.1.118`, `2.1.127`-`2.1.132` が表で A/C/P 分類されている、(c) `B: 書いただけ 0 件` の理由がある、(d) `docs/CLAUDE-feature-table.md` に Phase 62 追補、(e) `CHANGELOG.md` `[Unreleased]` に Phase 62 の「CC のアプデ → Harness での活用」記述、(f) `bash tests/test-claude-upstream-integration.sh` PASS、(g) `./tests/validate-plugin.sh` PASS (48/48) | 62.1.1, 62.1.2, 62.1.3, 62.1.4, 62.1.5, 62.2.1, 62.2.2, 62.2.3, 62.2.4, 62.2.5 | cc:完了 [bb3cfa5e] |

---

## Phase 63: stale `harness-mem-client.sh` 参照を managed-companion 整合に更新 [P2]

Purpose: Phase 60 (v2.20.10) で `scripts/harness-mem-client.sh` が claude-harness 配布から除外された (managed-companion 移行) が、`scripts/auto-checkpoint.sh` と `scripts/codex-loop.sh` の `HARNESS_MEM_CLIENT` / `MEM_CLIENT` default path が削除済みファイルを指したままだった。両 script は graceful skip 設計を持つため運用上の実害はないが、誤解を招く dead default を「明示的 skip-by-default」へ更新し、shell client は test fixture 用のみとして整理する。Phase 62 の test design upgrade 4 件 (`a2489ea3`) と同じ系統の cleanup として Phase 63 で扱う。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 63.1.1 | `scripts/auto-checkpoint.sh` line 14-15 の `HARNESS_MEM_CLIENT` default と `scripts/codex-loop.sh` line 48 の `MEM_CLIENT` default を、削除済み path (`${SCRIPT_DIR}/harness-mem-client.sh` / `${HARNESS_INSTALL_ROOT}/scripts/harness-mem-client.sh`) から **空文字** に変更し、Phase 60 移行の経緯コメントを両ファイルに付加する。test (`test-auto-checkpoint.sh`, `test-codex-loop-cli.sh`) は env 経由で fake client を渡す既存契約を維持し、env 未指定時の skip path を実際の運用既定にする | (a) `scripts/auto-checkpoint.sh` の `HARNESS_MEM_CLIENT` default が空文字、(b) `scripts/codex-loop.sh` の `MEM_CLIENT` default が空文字、(c) 両ファイルに Phase 60 移行 + `docs/harness-mem-companion-contract.md` への pointer コメントが付加、(d) `bash tests/test-auto-checkpoint.sh` PASS=5 FAIL=0、(e) `bash tests/test-codex-loop-cli.sh` passed=30 failed=0、(f) `./tests/validate-plugin.sh` 48/48 PASS、(g) `bash scripts/ci/check-consistency.sh` ✅ 全合格 | 62.3.1 | cc:完了 |

---

## Phase 64: Plans.md archive 対応 + test-claude-upstream-integration.sh の archive-aware 拡張 [P2]

Purpose: v4.8.0 release 準備中、Plans.md (279 行、200 limit 超過) の `/maintenance` archive (Phase 47-61 → `.claude/memory/archive/Plans-2026-05-08-phase47-61.md`) を試みたが、`tests/test-claude-upstream-integration.sh` が Phase 51-58 の詳細タスク行を Plans.md 内に永続要求 (10 箇所超の `grep -q '...' Plans.md` ハードコード) しており、archive 操作と本質的に非互換と判明した。`.claude/rules/test-quality.md` の「テスト改ざん禁止」ルールに従い v4.8.0 では rollback したが、Plans.md は今後も成長するため archive 対応の根本修正が必要。`.claude/rules/migration-policy.md` ルール 4 (retroactive validation) を流用して、archive ファイルへの fallback 検索が機能することを過去 commit で検証する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 64.1.1 | `tests/test-claude-upstream-integration.sh` の Plans.md grep を archive-aware に拡張する。対象 grep の検索範囲を `${ROOT_DIR}/Plans.md` 単独から「Plans.md + `.claude/memory/archive/Plans-*.md` の和集合」へ拡張するヘルパー関数 (`grep_plans_or_archive`) を導入。`.claude/rules/test-quality.md` 例外手順 (承認リクエスト) に従い、変更前にユーザー承認を取り、テストが「記録が現存する」を検証する意図は維持する (改ざんではなく archive 機能との整合性更新と明示) | (a) `tests/test-claude-upstream-integration.sh` に `grep_plans_or_archive` ヘルパー関数が定義され、Phase 51-58 系の Plans.md 永続要求 grep 全 10 箇所超が同ヘルパー経由に置換、(b) ヘルパーは Plans.md と `.claude/memory/archive/Plans-*.md` 全ファイルを対象に grep し、いずれかで一致すれば PASS、(c) 変更前にユーザー承認を `.claude/rules/test-quality.md` の例外フォーマット (理由 / 変更内容 / 影響範囲 / 代替案) で取得した記録が PR description に残る、(d) `bash tests/test-claude-upstream-integration.sh` がヘルパー導入後も既存 (archive 前) の Plans.md で PASS、(e) `./tests/validate-plugin.sh` 48/48 PASS | - | cc:完了 [20c60c0a] |
| 64.1.2 | 64.1.1 のヘルパー導入後、Plans.md から Phase 47-61 を `.claude/memory/archive/Plans-YYYY-MM-DD-phase47-61.md` (実施日) へ archive する。archive ファイル名は最終 archive 日 (実施日) と Phase 範囲を含める命名規約 (`Plans-YYYY-MM-DD-phaseNN-MM.md`) を踏襲。Plans.md header の archive list 先頭に新エントリを追加し、最終/前回アーカイブ日付を更新 | (a) `.claude/memory/archive/Plans-YYYY-MM-DD-phase47-61.md` が作成 (line 1 の archive メタ情報 + Phase 47-61 内容)、(b) Plans.md から Phase 47-61 のセクションが削除され、line 数が 100 行以下、(c) Plans.md の最終/前回アーカイブ表記が更新され、archive list 先頭に新エントリ追加、(d) `./tests/validate-plugin.sh` 48/48 PASS、(e) `bash scripts/ci/check-consistency.sh` ✅ 全合格、(f) `bash scripts/check-residue.sh` 0 件 | 64.1.1 | cc:完了 [aa3333d5] |
| 64.1.3 | archive 後の retroactive validation (`.claude/rules/migration-policy.md` ルール 4 準拠) を実施し、archive operational pattern を docs 化する。archive 直前 / 直後の双方で test が PASS することを過去 commit に対して検証し、ヘルパーの 3 状態 (Plans.md のみ一致 / archive のみ一致 / 両方一致) を unit test で固定 | (a) archive 直前の commit と archive 後 HEAD の双方で `bash tests/test-claude-upstream-integration.sh` PASS、(b) `grep_plans_or_archive` ヘルパーが「Plans.md にある場合」「archive にある場合」「両方にある場合」の 3 状態すべてで一致を返すことが unit test (`tests/test-grep-plans-or-archive.sh` 新規) で固定、(c) `.claude/rules/migration-policy.md` ルール 5 (false positive ゼロ) を満たす、(d) Plans.md cleanup の operational pattern を `docs/plans-archive-pattern.md` (新規) に記録し、将来の archive 操作で再発防止 | 64.1.2 | cc:完了 [0e9b06b7] |
| 64.2 | be2a1781 commit 後に観測された deniedDomains SSOT inversion 事故 (skills/monitors/agents block strip と同型の「片肺 sync」5 回目) の根治。`harness.toml` の `[safety.sandbox.network].deniedDomains` を 3 件 → 9 件 canonical (cloud-metadata 3 件 + paste-site 6 件) に格上げ。`bin/harness sync` に drift detection (`reportSettingsDrift()`) を追加して書き込み前に内容変化を stderr に warning 表示。`tests/test-settings-baseline.sh` に観点 7 (`harness.toml` ↔ `settings.json` 件数一致 + paste-site 6 件 SSOT 存在) を FAIL レベルで追加し、観点 5 を WARN→FAIL に格上げ。retroactive emergency hotfix (Plans 外起票だが harness-sync で記録に取り込み) | (a) `harness.toml` deniedDomains が 9 件で template canonical と一致、(b) `go/cmd/harness/sync.go::reportSettingsDrift()` が新規/idempotent run では silent、内容変化時のみ stderr に件数差と remediation hint 出力、(c) `go/cmd/harness/sync_test.go` に 5 unit test (新規 file / identical / removed / added / parse) 追加、(d) `tests/test-settings-baseline.sh` 観点 7 が `harness.toml` ↔ `settings.json` 件数一致と paste-site 6 件 SSOT 存在を FAIL で assert、(e) `./tests/validate-plugin.sh` 48/48 PASS、(f) v4.8.1 リリースに含む | 62.1.4 | cc:完了 [9bbf804d] |

---

## Phase 65: 3-Surface HTML for Non-Engineer Vibecoder + harness-mem cross-project safety [P1]

Purpose: 非エンジニアの発注者・プロデューサー職向けに、Claude への発注の (1) 計画理解 / (2) 進行管理 / (3) 受け入れ判断の 3 つの認知負荷ピークを HTML artifact で可視化する。各 surface は harness-mem と双方向連携 (read: 過去決定/パターン/類似案件 / write: 個人選好の累積) するが、cross-project search はデフォルト OFF とし、ユーザーが明示的に `cross-project-groups.yaml` でグループ定義した場合のみ opt-in 横断を許可する。Layer 1 (mem `<private>` strip + project filter) は既存だが、Layer 2 (クライアント辞書 + NER) と Layer 3 (HTML 生成直前最終 scan + 監査ログ) を harness 側で新規実装し、3 層防御で固有名詞漏洩を構造的に防ぐ。Phase A → E の段階的解放で、MVP は横断機能なしで体験価値を先に固める。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 65.1.1 | [Phase A: Plan Brief MVP] HTML rendering infrastructure を新規導入する。`scripts/render-html.sh` (約 100-200 行) で mustache 風 (`{{var}}`) テンプレート展開 + jq による JSON データバインドを実装。テンプレート格納ディレクトリは `templates/html/`、出力先は `.claude/state/views/`。テンプレート schema は `html-render-input.v1` (`{kind: "plan-brief"\|"progress"\|"accept", project: string, generated_at: ISO8601, sections: [...]}`) で固定。生成 HTML は単独で開ける (no server, no JS framework)、CSS は inline、Claude Harness ブランド (off-white BG / near-black FG / harness orange accent) 準拠 | (a) `scripts/render-html.sh` が `--template <name> --data <json> --out <path>` の 3 引数で動作、(b) `templates/html/test-fixture.html.template` で `{{title}}` および `{{#sections}}{{name}}{{/sections}}` の 2 種類が展開可能、(c) `tests/test-render-html.sh` で 4 ケース (正常 / 空 sections / 不正 JSON / 存在しないテンプレート) を機械検証、(d) 出力 HTML は `lynx -dump` で読める text 構造を持つ、(e) CSS palette が Claude Harness ブランド (`#FAFAFA` off-white / `#0F0F0F` near-black / `#F58A4A` harness orange) を使用、(f) `./tests/validate-plugin.sh` PASS | 64.2 | cc:完了 [49f8a0e6] |
| 65.1.2 | `harness-plan-brief` skill を新規作成する。`skills/harness-plan-brief/SKILL.md` (frontmatter `name: harness-plan-brief`, `user-invocable: true`, `argument-hint: "[task-description]"`)。skill の責務は (i) 現プロジェクトのみで harness-mem search を実行 (`project: <current>`, `strict_project: true` 強制)、(ii) 過去 decision (D1-D41) と pattern (P1-P33) と Plans archive 28 件から類似案件を抽出、(iii) `plan-brief-context.v1` schema (`{user_request, my_understanding, options[], risks[], acceptance_criteria[], confidence: 0-100, related_decisions[], similar_past_plans[]}`) を構築、(iv) `templates/html/plan-brief.html.template` 経由で HTML 生成、(v) ブラウザで自動 open (`open` / `xdg-open` / `start` の OS 別 dispatch) | (a) `skills/harness-plan-brief/SKILL.md` 存在 (frontmatter は skill-editing.md 規約準拠)、(b) skill 起動時に `mcp__harness__harness_mem_search` を `project: $(basename $(git rev-parse --show-toplevel))` 強制で 1 回以上呼び出す、(c) cross-project search を**呼ばない** (test で `project` parameter 必須が確認できる)、(d) 出力 `plan-brief-context.v1` schema が JSON Schema validator で valid、(e) `tests/test-harness-plan-brief.sh` で fixture プロジェクトに対し HTML 生成と auto-open dispatch を機械検証 (CI 環境では `BROWSER=true` で skip)、(f) `./tests/validate-plugin.sh` PASS | 65.1.1 | cc:完了 [b523dee1] |
| 65.1.3 | Plan Brief の context compilation logic を実装する。`scripts/plan-brief-compile.sh` (約 150-250 行) で: (i) ユーザー request からキーワード抽出 (FTS で `harness_mem_search`)、(ii) 上位 5 件の decision / pattern / past plan を `expand_links: true` で取得、(iii) 自信度算出ロジック (過去類似案件の成功率 baseline + 現タスクの DoD 明確度の 2 軸) を実装。`confidence: 0-100` の整数で出力。算出根拠を `confidence_evidence: string[]` に同梱 | (a) `scripts/plan-brief-compile.sh` が `--query <text> --project <name>` で context JSON を stdout 出力、(b) `confidence` 算出が「過去類似案件の cc:完了率 (40 点満点) + DoD の数値要件含有率 (30 点満点) + 関連 D/P 件数の有意性 (30 点満点)」の合計、(c) `tests/test-plan-brief-compile.sh` で 4 ケース (類似案件 0 件 / 5 件 / 全完了 / 半数失敗) を機械検証、(d) `confidence_evidence` に「過去 N 件中 M 件成功」のような数値が 1 行以上含まれる、(e) `./tests/validate-plugin.sh` PASS | 65.1.2 | cc:完了 [f545ae03] |
| 65.1.4 | Plan Brief 承認時の memory 書き込み (write side) を実装する。`scripts/plan-brief-record-decision.sh` で `mcp__harness__harness_mem_ingest` 経由で `observation_type: "decision"` の record を作成。schema `personal-preference.v1` (`{user_request_hash: sha256, chosen_option: string, rejected_options: string[], reasoning: string, timestamp: ISO8601, project: string}`) で固定。**個人選好データ蓄積**の起点。承認 / 修正依頼 / 質問の 3 アクションを区別記録 | (a) `scripts/plan-brief-record-decision.sh` が承認 / 修正依頼 / 質問の 3 action mode を持つ、(b) record される observation の `tags` に `personal-preference` `plan-brief-approval` の 2 タグが含まれる、(c) `tests/test-plan-brief-record.sh` で 3 action 各 1 ケース合計 3 ケース機械検証、(d) `mcp__harness__harness_mem_search` で `tags: personal-preference` 検索すると 1 ヶ月後に 30 件以上ヒット可能な構造、(e) `./tests/validate-plugin.sh` PASS | 65.1.2 | cc:完了 [554c5d21] |
| 65.1.5 | Phase 65.1.x の e2e validation。実プロジェクト fixture (`tests/fixtures/plan-brief-e2e/`) で「ユーザー request → Plan Brief HTML 生成 → 承認 → memory 書き込み」のフローを通し検証 | (a) `tests/test-plan-brief-e2e.sh` で 5 ステップ (request 入力 / mem search / HTML 生成 / 承認 / mem write) を順次検証、(b) 生成 HTML が `plan-brief-context.v1` schema と一致、(c) memory 書き込み後 `mcp__harness__harness_mem_search query: "<request hash>"` で当該 record を再取得可能、(d) `./tests/validate-plugin.sh` 49+/49+ PASS (新規テスト追加 1 件以上)、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.1.1, 65.1.2, 65.1.3, 65.1.4 | cc:完了 [2f3aed56] |
| 65.2.1 | [Phase B: Acceptance Demo] `harness-accept` skill を新規作成する (Phase 65.1 と同パターン)。`skills/harness-accept/SKILL.md`、`templates/html/accept.html.template`。schema `acceptance-context.v1` (`{user_request, demo_artifacts: [{kind: "video"\|"screenshot"\|"text", path}], verified_criteria: [{name, passed: bool, evidence}], unverified_caveats[], past_issue_patterns[], recommendation: "ship"\|"wait"\|"reject"}`)。Plan Brief 時に書き込んだ acceptance_criteria を read 側で取得し、各 criterion ごとに verified/unverified を表示 | (a) `skills/harness-accept/SKILL.md` 存在、(b) Plan Brief で書き込んだ `personal-preference.v1` の acceptance_criteria を本 skill が read で取得、(c) `verified_criteria[].evidence` が空文字列のとき HTML で警告表示、(d) `recommendation` 算出ロジック (verified 数 / 全 criteria 数 ≥ 0.8 → ship、≥ 0.5 → wait、それ未満 → reject) が unit test で固定、(e) `tests/test-harness-accept.sh` で 4 ケース (全 verified / 半 verified / 全 unverified / criteria 0 件) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.1.5 | cc:完了 [e62c7027] |
| 65.2.2 | Acceptance Demo の「過去の問題パターン」呼び出し (read side) を実装する。`scripts/accept-past-issues.sh` で patterns.md (P1-P33) と過去の `acceptance-context.v1` record を semantic search し、現タスクと類似する post-launch issue 履歴を最大 3 件取得。**まだ project-only 検索** (cross-project は Phase 65.3 で解放) | (a) `scripts/accept-past-issues.sh` が `--project <name> --task <description>` で過去 issue 履歴 (`past-issue.v1` schema) を出力、(b) cross-project search を**呼ばない** (project parameter 必須を test で固定)、(c) 上位 3 件取得 + 各々に `verified_in_current_task: bool` 付与、(d) `tests/test-accept-past-issues.sh` で 3 ケース (issue 0 件 / 3 件 / verified 半数) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.1 | cc:完了 [a85f903a] |
| 65.2.3 | Acceptance Demo 承認時の memory 書き込み (write side)。schema `acceptance-decision.v1` (`{user_request_hash: sha256, recommendation_taken: bool, override_reason: string?, verified_criteria_at_decision[], post_launch_concerns[], timestamp: ISO8601}`)。Plan Brief で書き込んだ user_request_hash と紐付けて、**プラン → 受け入れの完全 trace** を構築 | (a) `scripts/accept-record-decision.sh` 存在、(b) record の `user_request_hash` が Plan Brief 側 record と join 可能、(c) `mcp__harness__harness_mem_graph` で plan→accept の chain を取得可能 (test で確認)、(d) `tests/test-accept-record.sh` で 3 ケース (recommendation 採用 / override / reject) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.1 | cc:完了 [5c65f1ce] |
| 65.2.4 | Phase 65.2.x の e2e validation。Plan Brief approval → 実装 → Acceptance Demo の 3 段階を 1 つの fixture project で通し、`personal-preference.v1` と `acceptance-decision.v1` が同 user_request_hash で紐付くことを検証 | (a) `tests/test-plan-accept-flow-e2e.sh` で 3 段階 (plan → impl simulation → accept) を順次検証、(b) memory に 2 種の record が join 可能な状態で保存される、(c) HTML 2 種 (plan-brief / accept) が同 task で生成され、両方とも user_request を共有、(d) `./tests/validate-plugin.sh` 50+/50+ PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.2.1, 65.2.2, 65.2.3 | cc:完了 [d2229a04] |
| 65.3.1 | [Phase C: Cross-Project Group + 3 層防御] `cross-project-groups.yaml` schema を策定し、初期テンプレートを設置する。`.claude/rules/cross-project-groups.yaml` (新規 SSOT) を作成し、schema `cross-project-group.v1` (`{groups: [{name: string, members: string[], description: string?}]}`) で固定。default は空 (`groups: []`) で、横断検索は無効化される。設定例として 2 グループ (Personal Tools / Client-Sample) のコメントアウト雛形を含める | (a) `.claude/rules/cross-project-groups.yaml` 存在、初期 `groups: []`、(b) `cross-project-group.v1` schema が `docs/cross-project-groups-schema.md` に記載、(c) `scripts/load-cross-project-groups.sh` で yaml を JSON にパースし、不正な schema 時は exit 1、(d) `tests/test-cross-project-groups-schema.sh` で 4 ケース (空 / 1 group / member 重複 / 不正 schema) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.4 | cc:完了 [4a014137] |
| 65.3.2 | `client-redaction.yaml` schema を策定し、Layer 2 辞書ベースの固有名詞検出を実装する。`.claude/rules/client-redaction.yaml` (新規 SSOT) を作成し、schema `client-redaction.v1` (`{clients: [{name, redact_as, aliases[]?}], people: [...], domains: [...]}`)。default は空。`scripts/redact-by-dictionary.sh` で「テキストを受け取り → 辞書ヒットを redact_as で置換 → 置換数とともに stdout 返却」 | (a) `.claude/rules/client-redaction.yaml` 存在、初期 `clients: [], people: [], domains: []`、(b) `scripts/redact-by-dictionary.sh` が `--input <text> --dict <yaml-path>` で動作、(c) ヒット 0 件時は exit 0 で原文そのまま、ヒット時は stderr に件数記録、(d) `tests/test-redact-by-dictionary.sh` で 5 ケース (ヒット 0 / 1 / 複数 / aliases / 重複 redact_as) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.2.4 | cc:完了 [5152bed2] |
| 65.3.3 | Layer 2 の NER (Named Entity Recognition) 検出層を追加する。kuromoji-js または同等の lightweight Japanese tokenizer を使用し、形態素解析後 `proper noun` (固有名詞) tag を持つトークンを抽出。`scripts/redact-by-ner.sh` で「テキスト → NER ヒット → `[Entity]` トークンへの置換」を実装。辞書ヒットと NER ヒットは独立に集計し、Layer 3 で合算 | (a) `scripts/redact-by-ner.sh` が `--input <text>` で固有名詞を `[Entity]` に置換、(b) Japanese tokenizer (kuromoji 等) が node_modules または stand-alone CLI として利用可能、(c) `tests/test-redact-by-ner.sh` で 4 ケース (人名 / 会社名 / 地名 / 固有名詞 0 件) を機械検証、(d) NER 失敗時 (tokenizer 不在) は fail-open で原文そのまま + stderr 警告、(e) `./tests/validate-plugin.sh` PASS | 65.3.2 | cc:完了 [20a4478f] |
| 65.3.4 | Layer 3 の HTML 生成直前最終 scan を実装する。`scripts/render-html.sh` を拡張し、HTML 生成前に `redact-by-dictionary.sh` → `redact-by-ner.sh` → 最終 sanity check (固有名詞らしきカタカナ連続 5 文字以上を検出) の 3 段を順次走らせる。3 段目で残骸が見つかった場合は HTML を**生成せず exit 1**、stderr に検出内容と該当テンプレートを出力 (fail-safe) | (a) `scripts/render-html.sh` に `--with-redaction` flag が追加、(b) flag 有効時に dict → NER → final scan の 3 段順次実行、(c) final scan で残骸検出時は HTML 生成中止 + exit 1 + stderr に「detected: <token>, source: <line>」出力、(d) `tests/test-render-html-redaction.sh` で 4 ケース (全 clean / dict のみヒット / NER のみヒット / final scan で検出 = exit 1) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.3.3 | cc:完了 [0ae3f40a] |
| 65.3.5 | Cross-project search の opt-in 機構を harness-plan-brief / harness-accept skill に組み込む。`--cross-project-group <name>` flag を追加し、指定時のみ `cross-project-groups.yaml` から member projects を解決して **MCP `mcp__harness__harness_mem_search` を member ごとに N 回 call し client 側でマージ** (D43 Option α、mem 側 MCP に projects 配列が無いため)。flag 不在時は現プロジェクトのみ (Phase 65.1.2 の挙動を維持)。group 名が yaml に存在しない場合は exit 1 | (a) `harness-plan-brief` / `harness-accept` 両 skill が `--cross-project-group <name>` flag を受け取る、(b) flag 不在時は現プロジェクト single search (Phase 65.1.2 と同じ挙動)、(c) flag 有効時は yaml から member resolve し各 project で MCP search を 1 回ずつ発行 → client でマージ・dedupe・relevance_score 順 sort、(d) group 名が存在しない場合は exit 1 + stderr に「group not found」、(e) `tests/test-cross-project-flag.sh` で 4 ケース (flag 不在 / 有効 group / 無効 group / 空 group) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.3.1, 65.3.4 | cc:完了 [09377eb9] |
| 65.3.6 | 監査ログの実装。Cross-project search が走った場合に `.claude/state/audit/cross-project-search.jsonl` に append-only 記録。schema `cross-project-audit.v1` (`{timestamp: ISO8601, group_name, member_projects[], query_hash: sha256, redaction_count: {dict: int, ner: int}, output_passed_final_scan: bool}`)。実際のクエリ文字列は記録しない (privacy)、hash のみ。HTML の最下部に「この HTML は <N> 件 redact しました」と監査サマリを表示 | (a) `.claude/state/audit/cross-project-search.jsonl` が cross-project search 1 回ごとに 1 行追加、(b) JSON Lines 各行が `cross-project-audit.v1` schema 準拠、(c) クエリ文字列の生記録なし、hash のみ、(d) HTML 最下部に「redacted: dict X 件 + NER Y 件」のサマリが表示、(e) `tests/test-cross-project-audit.sh` で 3 ケース (redaction 0 / 複数 / final scan 失敗) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.3.4, 65.3.5 | cc:完了 [272a8f33] |
| 65.3.7 | Phase 65.3.x の e2e validation。fixture group 設定 + dict + NER 各層を全て通すクロスプロジェクト Plan Brief を生成し、3 層防御で固有名詞が漏れないことを検証 | (a) `tests/test-cross-project-redaction-e2e.sh` で 1 fixture group (3 member projects) で Plan Brief 生成、(b) 各 member project に予め固有名詞を含む observation を挿入、(c) Plan Brief 出力 HTML に固有名詞 (生のクライアント名 / 人名) が含まれないことを `grep -E "(NoraiCorp\|YorozuPro\|田中\|佐藤)" output.html` で 0 件確認、(d) audit log に redaction 件数が正しく記録、(e) `./tests/validate-plugin.sh` 53+/53+ PASS、(f) `bash scripts/ci/check-consistency.sh` 全合格、(g) [D43] envelope (signals + prose) 構造への redaction を test fixture に含め、signals[] と prose の整合性 (validateProseContainsSignals 不変条件) が壊れないこと、(h) [D43] 既存 server 側 `[REDACTED_*]` mark の二重置換ガードを test 検証 (event-recorder.ts:redactContent との衝突防止) | 65.3.1, 65.3.2, 65.3.3, 65.3.4, 65.3.5, 65.3.6 | cc:完了 [c05d6ef8] |
| 65.4.1 | [Phase D: Progress Tracker] `harness-progress` skill skeleton を新規作成する。`skills/harness-progress/SKILL.md`、`templates/html/progress.html.template`。schema `progress-snapshot.v1` (`{project, current_task, progress_pct: 0-100, todo_tasks[], wip_tasks[], done_tasks[], elapsed_minutes: int, estimated_total_minutes: int, cost_so_far_usd: float, cost_estimate_usd: float, alerts[]}`)。生成 HTML は静的 (PWA や WebSocket は使わず、再生成で更新) | (a) `skills/harness-progress/SKILL.md` 存在、(b) `progress-snapshot.v1` schema が JSON Schema validator で valid、(c) HTML 生成時に Plans.md の cc:WIP / cc:TODO / cc:完了 件数を反映、(d) `tests/test-harness-progress.sh` で fixture Plans.md (各 status 含む) で snapshot HTML が正しい % で表示されることを機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.3.7 | cc:完了 [1175b9e4] |
| 65.4.2 | Progress Tracker の自動再生成機構を実装する。PostToolUse hook で「Edit / Write / Bash の発火」をトリガーに、`harness-progress` skill を background 起動して HTML を再生成。生成頻度は最大 1 分に 1 回 (rate limit) で過剰生成を防ぐ | (a) `scripts/hook-handlers/posttool-progress-regen.sh` が PostToolUse hook で起動、(b) `.claude/state/progress-last-regen.txt` に last regen timestamp 記録、(c) 60 秒以内の連続発火は skip (rate limit)、(d) `.claude-plugin/hooks.json` の dual sync が `hooks/hooks.json` と整合 (P29 規約)、(e) `tests/test-progress-regen.sh` で 4 ケース (初回 / 60秒以内 / 60秒超 / hook input 不正) を機械検証、(f) `./tests/validate-plugin.sh` PASS | 65.4.1 | cc:完了 [847ea36d] |
| 65.4.3 | Drift detection と alert を実装する。schema `progress-alert.v1` (`{kind: "scope-creep"\|"time-overrun"\|"repeated-failure"\|"cost-warning"\|"high-risk-file", severity: "info"\|"warn"\|"critical", message, suggested_action, triggered_at: ISO8601}`)。各 alert kind に対応する検出ロジックを実装: scope-creep (Plans.md にない file 編集を検出)、time-overrun (estimated の 1.5 倍超)、repeated-failure (test fail 3 回連続)、cost-warning (上限の 80%)、high-risk-file (`harness.toml` deny path matching) | (a) `scripts/progress-detect-drift.sh` が 5 alert kind それぞれを検出、(b) 検出結果を `progress-alert.v1` schema で出力、(c) `tests/test-progress-drift.sh` で各 alert kind 計 5 ケース機械検証、(d) HTML 表示時に critical alert は赤、warn は黄、info は青で色分け、(e) `./tests/validate-plugin.sh` PASS | 65.4.2 | cc:完了 [a835bde0] |
| 65.4.4 | Progress Tracker の「過去の判断パターン」表示を実装する (read side)。alert 検出時に `mcp__harness__harness_mem_search` で過去の同種 alert への user 判断 (Phase 65.1.4 で書き込まれた `personal-preference.v1` から該当 alert kind を抽出) を取得し、HTML に「過去 N 件中 M 件で同様の提案を断っています」と表示 | (a) `scripts/progress-past-judgments.sh` が alert kind と現プロジェクト名で過去判断を検索、(b) 上位 3 件の judgment と「断る確率 N%」が JSON 出力、(c) cross-project search はデフォルト OFF (Phase 65.3.5 と同じ flag mechanism)、(d) `tests/test-progress-past-judgments.sh` で 4 ケース (judgment 0 件 / 3 件 / 全断る / 全許可) を機械検証、(e) `./tests/validate-plugin.sh` PASS | 65.4.3, 65.1.4 | cc:完了 [6be27a68] |
| 65.4.5 | Phase 65.4.x の e2e validation。Plans.md に WIP/TODO/完了タスクを混在させた fixture で Progress Tracker 生成、scope-creep alert 発火、過去判断表示、再生成 rate limit を順次検証 | (a) `tests/test-progress-e2e.sh` で 5 ステップ (初回生成 / Plans 編集後再生成 / scope-creep 発火 / 過去判断表示 / rate limit 検証) を順次実行、(b) 全 5 alert kind が HTML に正しく表示、(c) audit log に regen 履歴が記録、(d) `./tests/validate-plugin.sh` 56+/56+ PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.4.1, 65.4.2, 65.4.3, 65.4.4 | cc:完了 [495dfffc] |
| 65.5.1 | [Phase E: 統合 + 監査 UI + ドキュメント] 3 surface (plan-brief / progress / accept) の統合 e2e。1 fixture project でユーザー request → Plan Brief → impl simulation → Progress (alerts) → Acceptance Demo の全フローを通し、3 つの HTML が同一 user_request_hash で trace 可能な状態を検証 | (a) `tests/test-3-surface-e2e.sh` で 5 段階 (plan / progress regen / drift alert / impl complete / accept) を順次実行、(b) 3 種の memory record (plan-brief-approval / progress-judgment / acceptance-decision) が同 hash で graph join 可能、(c) 3 種の HTML が同 fixture から生成され、それぞれ画面遷移リンクで相互参照、(d) `./tests/validate-plugin.sh` 全 PASS、(e) `bash scripts/ci/check-consistency.sh` 全合格 | 65.4.5 | cc:完了 [03629d85] |
| 65.5.2 | 監査 UI を 3 つの HTML 全てに統合する。各 HTML 最下部に「🔍 この artifact の根拠」セクションを追加し、(i) 検索範囲 (project 名 + group 名)、(ii) 参照した D / P / past plan の ID リスト、(iii) redact された語の件数 (kind 別)、(iv) 完全な audit log への link (`.claude/state/audit/*.jsonl`) を表示 | (a) 3 種の HTML テンプレートに `audit-trail` セクションが共通追加、(b) 4 項目 (検索範囲 / 参照 ID / redact 件数 / log link) が表示、(c) audit log が JSON Lines で human-readable、(d) `tests/test-audit-ui-presence.sh` で 3 種 HTML 全てに audit-trail セクションが含まれることを `grep -c "audit-trail"` で検証、(e) `./tests/validate-plugin.sh` PASS | 65.5.1 | cc:完了 [56543c7e] |
| 65.5.3 | 開発者 / ユーザー向けドキュメントを整備する。`docs/cognitive-load-surfaces.md` で 3 surface の役割と使い方、`docs/cross-project-safety.md` で 3 層防御の説明、`docs/cross-project-groups-schema.md` でグループ設定方法。各 doc は非エンジニア向けの語彙で書く (技術用語は最小限) | (a) 3 つの新規 doc が `docs/` 配下に存在、(b) 合計 1500 行以下 (簡潔さ重視)、(c) 各 doc に「やりたいこと」「やり方」「気をつけること」のセクションが必ず存在、(d) `docs/CLAUDE-skill-catalog.md` に 3 つの新スキルが追記、(e) CLAUDE.md の Top Skill Areas table に「cognitive-load (Plan Brief / Progress / Accept)」行を追加、(f) `./tests/validate-plugin.sh` PASS | 65.5.2 | cc:完了 [fb47a79e] |
| 65.5.4 | Feature Table と CHANGELOG への追補。`docs/CLAUDE-feature-table.md` の Phase 65 行追加 (`A: 実装あり` 分類)、`CHANGELOG.md` `[Unreleased]` に Phase 65 を「今まで / 今後」形式で追記。`.claude/rules/github-release.md` の release-notes ルール準拠 | (a) `docs/CLAUDE-feature-table.md` に Phase 65 の 3 surface 行追加 (A: 実装あり)、(b) `CHANGELOG.md` `[Unreleased]` に Phase 65 が「今まで (markdown 200 行スクロール) / 今後 (HTML カードでひと目)」の対比形式で記載、(c) Before / After table が含まれる、(d) `tests/test-changelog-format.sh` (既存または新規) PASS、(e) `./tests/validate-plugin.sh` PASS | 65.5.3 | cc:完了 [99695676] |
| 65.5.5 | Phase 65 全体のリリース準備。VERSION/plugin.json/harness.toml/marketplace.json の 4 点同期、SemVer 判定 (新スキル 3 個 + 新 hook 追加 = minor)、リリースタグ準備 | (a) `./scripts/sync-version.sh bump --minor` 実行で 4 ファイル同期 (現 4.8.1 → 4.9.0)、(b) SemVer 判定の根拠 (3 新スキル + Phase 65.4 で hook 1 件追加 + new yaml SSOT 2 件) が `CHANGELOG.md` に明記、(c) `bash tests/validate-plugin.sh` 全 PASS、(d) `bash scripts/ci/check-consistency.sh` 全合格、(e) GitHub Release notes が `.claude/rules/github-release.md` 規約準拠 | 65.5.4 | cc:完了 [8b3ba01b] |

---

## Phase 66: Open GitHub Issue closeout (#128, #126, #124, #123, #67) [P0/P1/P2]

Purpose: 2026-05-11 に `gh issue list --state open --limit 200` で確認した open Issue 5 件を、実行安全性を壊すものから順に解決する。優先順位は (1) Worker/worktree isolation 破壊 #128、(2) codex-loop background start の false-success #123、(3) stale broadcast inbox #126、(4) release tag opencode drift #124、(5) named multiple Plans enhancement #67。#67 は機能追加であり緊急バグではないが、open Issue を全て閉じるという依頼範囲に含める。実装開始前に detached HEAD ではなく `codex/issue-closeout-phase66` 系 branch を使い、Issue close は targeted tests + full validation + GitHub comment の後だけ行う。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 66.1.1 | [P0 / #128] WorktreeCreate hook stdout JSON が worktree path として扱われる事故を潰す。`scripts/hook-handlers/worktree-create.sh` と `go/internal/hookhandler/worktree_create.go` の契約を揃え、hook decision JSON と worktree path の責務を分離する。Breezing / Worker / reenter 経路に stdout-as-path の取り違えが残っていないか監査する | (a) shell 版 `worktree-create.sh` が `cwd` に `{"decision":...}` 形式の文字列を受けても、その文字列名の directory を作らず approve/no-op で終わる、(b) Go 版 `HandleWorktreeCreate` と shell 版の invalid cwd / real cwd / no payload の挙動が同じ、(c) new or updated test で literal JSON path directory が作られないこと、real tmp worktree では `.claude/state/worktree-info.json` が作られることを検証、(d) `agents/`, `scripts/`, `skills/`, `docs/` の stdout-as-path pattern audit が 0 件、(e) `bash tests/test-reenter-worktree-json.sh`, `bash tests/test-windows-worktree-support.sh`, new worktree-create regression, `./tests/validate-plugin.sh` PASS、(f) Issue #128 に再現条件と検証結果を comment して close | - | cc:完了 [f6ad77e3] |
| 66.1.2 | [P0 / #123] `harness codex-loop start ...` が background runner の即死を成功表示しないようにする。`scripts/codex-loop.sh cmd_start` に post-start health check を追加し、child pid alive / first state patch / runner log のどれも確認できない場合は `startup_failed` として fail fast する。early stderr / exit reason を durable に残す | (a) `cmd_start` が `nohup ... &` 後に短い bounded health check を行い、runner が即死した場合は non-zero exit + `status=startup_failed` + startup log path を出す、(b) status check が `state_stale` だけで原因を潰さず、`error_message` に early exit reason または runner log tail を保持、(c) sibling repo 想定 (`PROJECT_ROOT != HARNESS_INSTALL_ROOT`) と alphanumeric task id (`S108-001`) の regression fixture がある、(d) `tests/test-codex-loop-cli.sh` または new test で「即死 runner は成功表示しない」「生きている runner は従来通り start する」を検証、(e) foreground `run --run-id` 成功 path は壊さない、(f) `bash tests/test-codex-loop-cli.sh` と `./tests/validate-plugin.sh` PASS、(g) Issue #123 に evidence を comment して close | 66.1.1 | cc:完了 [5a1533e1] |
| 66.1.3 | [P1 / #126] `broadcast.md` inbox check が新規 session で古い通知を永久表示する問題を修正する。`go/internal/hookhandler/inbox_check.go` / `session_auto_broadcast.go` と bash fallback の方針を揃え、既読更新・時刻表示・stale cwd の扱いを明確化する | (a) 新規 session は既存 `broadcast.md` の古い entry を未読扱いしない、または表示した entry の最大 timestamp で `.last_inbox_read_<session>` を自動更新して同じ 5 件を再表示しない、(b) 表示形式が `HH:MM` だけでなく日付または relative age を含み、2 ヶ月前の通知が今日の通知に見えない、(c) broadcast 書込側は削除済み cwd / file path を検出したら書き込みを skip または stale として扱う、(d) Go tests で zero last-read / repeated check after throttle / old broadcast / deleted cwd / self-session filter を検証、(e) bash fallback (`scripts/pretooluse-inbox-check.sh`, `scripts/session-auto-broadcast.sh`) との契約差分が docs or tests に残る、(f) `cd go && go test ./internal/hookhandler -run 'Inbox|Broadcast|AutoBroadcast' -v` と `./tests/validate-plugin.sh` PASS、(g) Issue #126 に evidence を comment して close | 66.1.1 | cc:完了 [7344bdb8] |
| 66.1.4 | [P1 / #124] release tag 前に `opencode/` mirror drift を止める preflight gate を追加する。CI で落ちるまで待つのではなく、tag 作成前の `scripts/release-preflight.sh` / release skill / docs に mirror sync check を組み込む。Actions Node runtime warning も同じ release hygiene として audit する | (a) `scripts/release-preflight.sh` が `node scripts/build-opencode.js`, `node scripts/validate-opencode.js`, `bash scripts/sync-skill-mirrors.sh --check`, `git diff --quiet opencode/ skills-codex/ codex/.codex/skills/` 相当を release 前 fail gate として実行、(b) `tests/test-release-preflight.sh` に opencode drift fixture があり、drift 時は preflight が fail する、(c) `skills/harness-release*/SKILL.md` と `docs/release-preflight.md` に「tag 前に mirror drift 0」を明記、(d) `.github/workflows/opencode-compat.yml` / release workflow の Node runtime warning を current official action versions で audit し、更新するか「現時点保留」の根拠を CHANGELOG or docs に残す、(e) `bash tests/test-release-preflight.sh`, `bash scripts/ci/check-consistency.sh`, `./tests/validate-plugin.sh` PASS、(f) Issue #124 に evidence を comment して close | 66.1.1 | cc:完了 [71fef407] |
| 66.2.1 | [P2 / #67] multiple Plans を named-plan registry として実装する。既存 `Plans.md` は backward-compatible な `default` のまま残し、同一 repo 内の複数 roadmap / team / priority backlog を `--plan <name>` または active plan で明示選択できるようにする。1 run で複数 plan を混ぜる設計は禁止する | (a) source-controlled registry (`plans/manifest.json` or equivalent) が plan name -> relative path を保持し、`default` は既存 `Plans.md` を指す、(b) active plan pointer は local state (`.claude/state/active-plan.json` or equivalent) に保存され、explicit `--plan` が active pointer より優先される、(c) path traversal / absolute path / symlink escape / missing plan name を拒否する tests がある、(d) `harness-plan list`, `harness-plan switch <name>`, `harness-work --plan <name>`, `breezing --plan <name>`, `harness-sync --plan <name>`, `harness-loop/codex-loop --plan <name>` の routing contract が実装または明確に documented + tested、(e) `scripts/config-utils.sh` / `scripts/codex-loop.sh` / plan parser / sprint-contract generator が named plan path を使用する、(f) default-only project は既存 tests が無変更で PASS、(g) new named-plan tests + `bash tests/test-plans-status-markers.sh` + `bash tests/test-plans-issue-bridge.sh` + `./tests/validate-plugin.sh` PASS、(h) Issue #67 に implemented UX と migration note を comment して close | 66.1.2, 66.1.3, 66.1.4 | cc:完了 [015f6c1f] |
| 66.3.1 | Phase 66 全体の integration / release / Issue closeout。5 Issue の修正を 1 本の user-facing release にまとめ、バグ修正と named-plan enhancement を混ぜる SemVer 判断を明記する | (a) `CHANGELOG.md` に #128/#123/#126/#124/#67 の Before/After と migration notes がある、(b) SemVer は #67 の named-plan UX 追加を含むため minor bump (4.9.0 -> 4.10.0 相当) か、#67 を別 PR に分離する場合は patch + enhancement PR の判断根拠が書かれる、(c) `bash scripts/release-preflight.sh` PASS、(d) `bash scripts/ci/check-consistency.sh` PASS、(e) `./tests/validate-plugin.sh` PASS、(f) branch push / PR 作成後、Issue 5 件それぞれに test evidence を comment し、merge/release 後に close、(g) GitHub Actions `validate-plugin`, `Mirror Compatibility Check`, `auto-release` の relevant runs が green | 66.1.1, 66.1.2, 66.1.3, 66.1.4, 66.2.1 | cc:完了 [36c870d1] |

---

## Phase 67: Codex 0.130.0 stable 追従 [P1]

Purpose: OpenAI Codex CLI `rust-v0.130.0` stable が 2026-05-08 に公開され、`codex remote-control`、app-server thread pagination、selected environment 経由の `view_image`、Bedrock `aws login` profile credentials、plugin hooks/details/share discoverability、live config refresh / turn diff accuracy などが追加・修正された。Phase 58 の `0.125.0`-`0.128.0` 追従と Phase 62 の Claude Code follow-up と同じ型で、一次情報 snapshot、Codex setup / plugin workflow policy、README / setup skill、integration tests を更新する。alpha からの推測実装ではなく、公式 GitHub release `rust-v0.130.0` の stable 本文と changelog URLだけを根拠にする。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 67.1.1 | Codex `0.130.0` stable の upstream snapshot を作成する。`docs/upstream-update-snapshot-2026-05-10.md` に release metadata (`rust-v0.130.0`, published `2026-05-08T23:09:55Z`, compare URL)、New Features / Bug Fixes / Documentation / Chores の version-by-version 分解、A/C/P 分類、`B: 書いただけ 0 件` の理由を記録する。`docs/CLAUDE-feature-table.md` に Phase 67 行を追加し、`CHANGELOG.md` `[Unreleased]` に「Codex のアプデ → Harness での活用」形式で追記する | (a) `docs/upstream-update-snapshot-2026-05-10.md` が存在、(b) snapshot が `codex remote-control`, `Thread pagination APIs`, `view_image`, `aws login`, `plugin hooks`, `live threads from latest config snapshot`, `turn diffs` を含む、(c) `docs/CLAUDE-feature-table.md` に Phase 67 行、(d) `CHANGELOG.md` `[Unreleased]` に Phase 67 追記、(e) `tests/test-claude-upstream-integration.sh` が snapshot / Feature Table / CHANGELOG / Plans 参照を検出 | 62.3.1 | cc:完了 |
| 67.1.2 | Codex provider / app-server / multi-environment policy を 0.130.0 stable に更新する。`docs/codex-provider-setup-policy.md` に Bedrock `aws login` profile credentials を追記し、`docs/codex-plugin-workflows-policy.md` に `codex remote-control`, live app-server config refresh, thread pagination, selected-environment `view_image`, partial `apply_patch` 後の turn diff accuracy を追記する。runtime wrapper は変更しない判断を明記する | (a) provider doc が `aws login` / console-login credentials / `amazon-bedrock` の境界を説明、(b) plugin workflow doc が `codex remote-control` と app-server thread pagination を説明、(c) selected environment `view_image` は read-only first / one primary environment per write turn と矛盾しない、(d) turn diff accuracy は Harness 側 workaround 不要の `C: 自動継承` と明記、(e) `tests/test-codex-package.sh` または upstream integration test が主要文字列を検出 | 67.1.1 | cc:完了 |
| 67.1.3 | Codex distribution docs / setup skill / config comments を 0.130.0 stable に同期する。`codex/README.md` に remote-control, Bedrock `aws login`, app-server thread pagination, selected-environment `view_image`, plugin hooks/details/share discoverability、configurable OTel trace metadata、built-in MCPs、`CODEX_HOME` environments TOML provider、skills root cleanup の safe default を追記する。`skills/harness-setup/SKILL.md` と `codex/.codex/config.toml` のコメントも同じ policy に合わせる。plugin-bundled hooks は引き続き opt-in とし、Harness shipped config に inline hooks を追加しない | (a) `codex/README.md` に Codex `0.130.0` stable の section がある、(b) `skills/harness-setup/SKILL.md` が `docs/codex-plugin-workflows-policy.md` / `docs/codex-provider-setup-policy.md` の 0.130.0 追補を案内、(c) `codex/.codex/config.toml` が shipped no-inline-hooks と remote-control/Bedrock/OTel/MCP/`CODEX_HOME` environment guidance をコメントで保持、(d) `tests/test-codex-package.sh` が README / setup / config の 0.130.0 guidance を検出、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 67.1.2 | cc:完了 |
| 67.1.4 | Phase 67 の integration validation と Plans marker 更新。`tests/test-claude-upstream-integration.sh` と `tests/test-codex-package.sh` に Phase 67 の検出を追加し、対象テストを実行する。PASS 後に Phase 67 の task status を `cc:完了` に更新する | (a) `bash tests/test-claude-upstream-integration.sh` PASS、(b) `bash tests/test-codex-package.sh` PASS、(c) `bash scripts/sync-skill-mirrors.sh --check` PASS、(d) `./tests/validate-plugin.sh` PASS、(e) `Plans.md` の 67.1.1-67.1.4 が `cc:完了` | 67.1.1, 67.1.2, 67.1.3 | cc:完了 |

---

## Phase 68: harness-review TeamAgent debate + acceptance gate hardening [P1]

Purpose: `/harness-review` を単なる所感レビューではなく、TeamAgent の異なる見解、明確な合格ライン、仕様正本、Plans.md、デグレ確認、NG 後の修正後再レビュー、Asktool による意思決定確認まで含む gate に更新する。Claude と Codex の両 surface で同じ review contract を配布し、mirror drift をテストで検出する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 68.1.1 | `harness-review` の正本を更新し、内容に応じた TeamAgent Debate、仕様正本 / Plans.md / デグレ gate、明確な `APPROVE` 合格ライン、`REQUEST_CHANGES` 後の修正後再レビュー loop、修正に意思決定が必要な場合の `AskUserQuestion` 境界を明文化する。`skills/harness-review/` を SSOT とし、`codex/.codex/skills/harness-review/` と `opencode/skills/harness-review/` の mirror を同期する | (a) `skills/harness-review/SKILL.md` に TeamAgent Debate trigger、合格ライン、仕様正本 / Plans.md / デグレ gate、AskUserQuestion 境界、修正後再レビュー loop が記載、(b) `skills/harness-review/references/dual-review.md` が Claude + Codex + TeamAgent Debate と acceptance_bar schema を説明、(c) `codex/.codex/skills/harness-review/` と `opencode/skills/harness-review/` が SSOT と同期、(d) `tests/test-harness-review-governance.sh` が SSOT と mirrors の必須契約文字列を検出、(e) `bash tests/test-harness-review-governance.sh` PASS、(f) `bash scripts/sync-skill-mirrors.sh --check` PASS、(g) `./tests/validate-plugin.sh` PASS | 67.1.4 | cc:完了 |
| 68.1.2 | bare `review` / `/review` / `/harness-review` の対象決定契約を更新する。引数なしは「今までの作業のレビュー」と解釈し、working tree / branch commits / recent commits から review target を決める。target が不明または複数候補の場合は `AskUserQuestion` で 2〜3 選択肢を出し、ユーザーと認識を揃えてからレビューを開始する | (a) `skills/harness-review/SKILL.md` の Step 0 が bare review を「今までの作業」と扱う、(b) target 明確時は `REVIEW_AUTOSTART` で自動開始、(c) target 不明・複数候補時は `REVIEW_TARGET_ASK` / `AskUserQuestion` 契約を持つ、(d) `tests/test-harness-review-governance.sh` が bare review Ask contract を検出、(e) mirrors が同期、(f) `bash tests/test-harness-review-governance.sh` PASS、(g) `bash scripts/sync-skill-mirrors.sh --check` PASS、(h) `./tests/validate-plugin.sh` PASS | 68.1.1 | cc:完了 |
| 68.1.3 | bare `harness-release` / `/release` の対象契約を更新する。単体入力は「今までの作業をコミットしてリリースしたい」と解釈し、未レビューなら `AskUserQuestion` で確認してから `harness-review` を実行する。review が `APPROVE` になった場合だけ work commit を作り、通常の release preflight / confirmation gate / tag / GitHub Release に進む | (a) `skills/harness-release/SKILL.md` に Bare invocation contract、Review Gate、Work Commit Gate、未レビュー時 AskUserQuestion が記載、(b) `allowed-tools` に `AskUserQuestion` と `Skill` が含まれる、(c) `REQUEST_CHANGES` 時は release を保留して `harness-work` 修正後再レビュー loop に入り、`REQUEST_CHANGES` 単体を最終停止理由にしない契約が明記、(d) `tests/test-harness-release-governance.sh` が SSOT と mirrors の必須契約文字列を検出、(e) `codex/.codex/skills/harness-release/` と `opencode/skills/harness-release/` が SSOT と同期、(f) `bash tests/test-harness-release-governance.sh` PASS、(g) `bash scripts/sync-skill-mirrors.sh --check` PASS、(h) `./tests/validate-plugin.sh` PASS | 68.1.2 | cc:完了 |

---

## Phase 69: harness-review lightweight closeout + progressive disclosure refactor [P1]

Purpose: `steipete/agent-scripts` の `codex-review` と比較した結果、`harness-review` は Harness の品質 gate として必要だが、878 行の単一 SKILL.md に review target 検出、governance、Codex second opinion、security、UI、plan/scope review、修正ループが集中しており、通常の小さな closeout には重すぎる。別の軽量 skill を増やすのではなく、`harness-review` を単一入口のまま薄い dispatcher に分割し、`--quick` / `--codex-closeout` で Codex review + focused tests の軽量 path を選べるようにする。外部 `codex-review` からは「AI 指摘は助言として扱い実コードで検証」「dirty / PR / branch / commit の対象選択」「accepted / rejected findings」「clean 後に追加 review しない」「review 目的だけでは push しない」を取り込む。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 69.1.1 | `harness-review` の運用設計を SSOT 化する。`docs/harness-review-operating-model.md` を新規作成し、単一入口を維持する理由、軽量 path と full gate の使い分け、外部 `codex-review` から採用する契約 / 採用しない契約、review skill が commit / push を行う境界を明文化する | (a) `docs/harness-review-operating-model.md` がある、(b) `quick / codex-closeout / code / plan / scope / security / ui-rubric / full` の使い分け表がある、(c) `codex-review` 由来の採用項目が `advisory`, `accepted/rejected`, `stop-on-clean`, `target selection`, `no push just to review` の 5 件以上で明記、(d) 別 skill を作らない理由が discovery noise / contract drift / governance split の観点で説明される、(e) `APPROVE` 時の commit は明示要求または release/work flow 側に寄せ、review 単体の default side effect にしない方針が記録される | 68.1.3 | cc:完了 |
| 69.1.2 | `skills/harness-review/SKILL.md` を薄い dispatcher にリファクタリングする。詳細本文を `references/governance.md`, `references/code-review.md`, `references/codex-closeout.md`, `references/team-debate.md`, `references/plan-review.md`, `references/scope-review.md` へ移し、SKILL.md 本体は metadata、mode decision、auto-start 契約、参照先一覧、最小出力契約に絞る | (a) `skills/harness-review/SKILL.md` が 350 行以下、(b) 既存の `AskUserQuestion`, `REVIEW_TARGET_ASK`, `REVIEW_AUTOSTART`, `TeamAgent Debate`, `仕様正本`, `Plans.md`, `デグレ`, `decision_needed` 契約が失われていない、(c) 新規 reference files が存在し、旧本文の必須契約が移設されている、(d) `tests/test-harness-review-governance.sh` が dispatcher + references の両方を検査する、(e) `bash tests/test-harness-review-governance.sh` PASS | 69.1.1 | cc:完了 |
| 69.1.3 | `--quick` / `--codex-closeout` 軽量 review path を追加する。小さな dirty change / PR branch / single commit では Codex review と focused tests を優先し、指摘は必ず実コードで確認して `accepted` / `rejected` に分類する。clean 結果が出た後に、見栄えのためだけの追加 review を禁止する | (a) `skills/harness-review/references/codex-closeout.md` に dirty / PR base / branch / commit の target selection decision tree がある、(b) `scripts/harness-review-closeout.sh` または既存 `scripts/codex-companion.sh review` 経由の helper contract が `--dry-run`, `--parallel-tests`, `--base`, `--commit`, `--uncommitted` 相当を説明または実装、(c) final report が review command / tests / accepted findings / rejected findings / clean result を含む、(d) clean 後に追加 review しない契約を tests が検出、(e) Codex 利用不可時は full manual pass へ fallback し、失敗を成功扱いしない | 69.1.2 | cc:完了 |
| 69.1.4 | `harness-review` の side-effect 境界を修正する。review skill は原則 read-only 判定を返し、commit / push / release は `harness-work` / `harness-release` / ユーザー明示依頼の責務に寄せる。既存の「APPROVE なら自動コミット」表現を削除または明示 opt-in に変える | (a) `skills/harness-review/SKILL.md` と references から default auto-commit の表現が消える、または `--commit-on-approve` のような明示 opt-in に限定される、(b) `harness-release` の Work Commit Gate と矛盾しない、(c) `tests/test-harness-review-governance.sh` が `Do not push just to review` と review default read-only boundary を検出、(d) `tests/test-harness-release-governance.sh` PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 69.1.3 | cc:完了 |
| 69.1.5 | Phase 69 の mirror / docs / validation を完了する。`skills/` SSOT から `.agents/skills/`, `codex/.codex/skills/`, `opencode/skills/` へ同期し、OpenCode native frontmatter と support files drift を検証する。CHANGELOG には「review gate の重さを選べるようにした」Before/After を追記する | (a) `bash scripts/sync-skill-mirrors.sh --check` PASS、(b) `node scripts/validate-opencode.js` PASS、(c) `bash tests/test-harness-review-governance.sh` PASS、(d) `bash scripts/ci/check-consistency.sh` PASS、(e) `./tests/validate-plugin.sh` PASS、(f) `CHANGELOG.md` `[Unreleased]` に Phase 69 の Before/After がある、(g) `Plans.md` の 69.1.1-69.1.5 が実装後に `cc:完了` へ更新される | 69.1.1, 69.1.2, 69.1.3, 69.1.4 | cc:完了 |

---

## Phase 70: Hokage Core boundary + Claude/Codex/OpenCode no-regression path [P1]

Purpose: Superpowers 型の「共通 skills + host adapter + bootstrap acceptance」を Claude Code Harness に取り込む準備をする。ただし即時リブランドや汎用 product 宣言は行わず、`Hokage Core` は当面 internal architecture label として扱う。`docs/architecture/hokage-core.md` を仕様正本にし、Plan → pre-validation → implementation → post-validation → positioning closeout の順で進める。対象は Claude / Codex / OpenCode の 3 面に絞り、Cursor / Gemini / Copilot は matrix 上の future / unsupported に留める。Claude Code の hook 強制力、Codex の post quality gate、OpenCode の mirror/native skill 差分を混同せず、false parity を防ぐ。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 70.1.1 | [Plan / SSOT] Hokage Core の仕様正本を確定する。`docs/architecture/hokage-core.md` を更新し、(i) Claude Code Harness は当面 Claude-first product、(ii) Hokage Core は internal reusable workflow contract label、(iii) adapter は host-specific mechanics、(iv) public `Hokage Harness` spin-off gate、(v) explicit Rejects を固定する。[tdd:skip:docs-only] | (a) `docs/architecture/hokage-core.md` に Purpose / Core Contract / Adapter Contract / Implementation Flow / Public Spin-Off Gate / Explicit Rejects がある、(b) `rg "internal near-term positioning|Public Spin-Off Gate|Explicit Rejects" docs/architecture/hokage-core.md` が全て hit、(c) `docs/distribution-scope.md` の `codex/`, `opencode/`, `skills-codex/` mirror-only 現状と矛盾しない、(d) Cursor / Gemini / Copilot は future capability notes だけと明記、(e) `git diff --check` PASS | - | cc:完了 [65f8c31e] |
| 70.1.2 | [Pre-validation] Tool Capability Matrix を追加し、Claude Code / Codex / OpenCode の差分を false parity gate として検証可能にする。Cursor / Gemini / Copilot は future / unsupported 行だけに留める。`docs/tool-capability-matrix.md` と `tests/test-tool-capability-matrix.sh` を追加し、hook あり host と hook なし host を同じ安全性として宣伝しない contract を固定する。[tdd:required] | (a) matrix が `skill_loading`, `bootstrap_notice`, `prompt_routing`, `pre_use_guard`, `post_use_gate`, `review_artifact`, `memory_bridge` を含む、(b) Codex は `docs/hardening-parity.md` の「contract injection + post quality gate + merge gate」方針を参照、(c) test が Claude/Codex/OpenCode の required capability rows と `false parity` 禁止文を検出、(d) Cursor / Gemini / Copilot は setup や adapter manifest を要求せず future / unsupported として検出、(e) `bash tests/test-tool-capability-matrix.sh` PASS | 70.1.1 | cc:完了 [65f8c31e] |
| 70.1.3 | [Pre-validation] Bootstrap Routing Contract を追加する。`docs/bootstrap-routing-contract.md` と `tests/test-bootstrap-routing-contract.sh` を作り、session-start bootstrap と prompt routing fixture を分離して検査する。Golden prompts は capability proof ではなく static contract fixture として扱う。[tdd:required] | (a) docs が Claude `SessionStart` / Codex `AGENTS.md` / OpenCode `AGENTS.md` の bootstrap route を分けて説明、(b) `Todoアプリを作って`, `review this PR`, `implement all Plans.md tasks` が planning / review / execution の候補 skill に routing される期待値を持つ、(c) docs が「fixture は runtime auto-routing proof ではない」と明記、(d) unsupported host は成功扱いせず `unsupported` or `manual` として明示、(e) test が routing fixture と opt-out / evidence 文言を検証、(f) `bash tests/test-bootstrap-routing-contract.sh` PASS | 70.1.2 | cc:完了 [65f8c31e] |
| 70.2.1 | [Implementation] OpenCode の stale surface を単独で修理する。`opencode/README.md`, `scripts/build-opencode.js`, `scripts/validate-opencode.js`, `scripts/setup-opencode.sh` / `scripts/opencode-setup-local.sh` の責務を揃え、古い `opencode/commands` 必須案内や missing `mcp-server/` consumer default 前提を削除または development-only と明記する。[tdd:required] | (a) OpenCode README が skills-primary setup を案内し、`opencode/commands` copy を必須扱いしない、(b) `mcp-server/` は `docs/distribution-scope.md` と同じ development-only 表現になり、存在しない path の build 手順を consumer default にしない、(c) setup script が OpenCode-native path / AGENTS / skills / opencode.json を検証、(d) `node scripts/build-opencode.js`, `node scripts/validate-opencode.js`, `bash scripts/sync-skill-mirrors.sh --check` PASS、(e) new or updated OpenCode setup test PASS | 70.1.3 | cc:完了 [65f8c31e] |
| 70.2.2 | [Implementation] Existing manifest generation を拡張しすぎず、core boundary metadata の検査だけを追加する。`scripts/generate-skill-manifest.sh` / `tests/test-skill-design-contract.sh` の範囲で、official runtime field と Harness metadata を混同しないこと、core workflow skill が capability requirements を文書参照できることを検証する。新規 `adapters/*/manifest.yaml` は setup/docs/preflight consumer ができるまで作らない。[tdd:required] | (a) `tests/test-skill-design-contract.sh` または manifest test が official field と Harness metadata の混同禁止を検出、(b) core workflow skills が capability matrix / Hokage Core doc を参照できる、(c) adapter manifest files を作らない理由が `docs/architecture/hokage-core.md` または CHANGELOG に残る、(d) `bash tests/test-skill-design-contract.sh` PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 70.2.1 | cc:完了 [65f8c31e] |
| 70.2.3 | [Implementation] Release / CI gate は non-shipping adapter で Claude plugin release を不必要に止めない形へ整理する。`scripts/release-preflight.sh`, `.github/workflows/opencode-compat.yml`, `tests/test-codex-package.sh`, `tests/test-distribution-archive.sh` を確認し、adapter checks は path-triggered / release-claim-triggered / explicit flag のどれかにする。[tdd:required] | (a) release preflight が Claude plugin payload と source-repo mirrors の境界を区別、(b) adapter drift checks は release が adapter support を claim する場合または adapter files changed の場合に fail gate になる、(c) Claude plugin archive には non-Claude adapter payload を混入しない、(d) `bash scripts/release-preflight.sh`, `bash tests/test-codex-package.sh`, `node scripts/validate-opencode.js`, `bash tests/test-distribution-archive.sh` PASS | 70.2.2 | cc:完了 [65f8c31e] |
| 70.3.1 | [Post-validation / Positioning] README / README_ja / CHANGELOG の表現を検証済み範囲へ更新する。汎用 `Hokage Harness` でも public `powered by Hokage Core` でもなく、当面は「Hokage Core extraction underway」程度に留め、Cursor / Gemini / Copilot は future / unsupported として gate 未通過扱いにする。[tdd:skip:docs-only] | (a) README と README_ja が premature generic support claim を含まない、(b) public spin-off gate 未通過の間は `Hokage Harness` を product claim として使わない、(c) CHANGELOG に Phase 70 の Before/After がある、(d) `rg "Hokage Harness|powered by Hokage Core" README.md README_ja.md CHANGELOG.md` が premature public claim を含まない、(e) `git diff --check` PASS | 70.2.3 | cc:完了 [65f8c31e] |
| 70.3.2 | [Post-validation] Spin-off readiness decision を作成する。`docs/hokage-spin-off-readiness.md` に Claude/Codex/OpenCode gate の pass/fail、未対応 host の理由、次に増やす adapter 候補を記録する。全 gate が green になるまでは `No public spin-off yet` を結論にする。[tdd:skip:docs-only] | (a) readiness doc が gate table を持つ、(b) Claude / Codex / OpenCode の検証コマンドと結果欄がある、(c) Cursor / Gemini / Copilot は explicit future scope、(d) gate 未達時の結論が `No public spin-off yet`、(e) README positioning と矛盾しない | 70.3.1 | cc:完了 [65f8c31e] |
| 70.4.1 | [Final validation] Phase 70 全体の regression closeout。仕様正本、capability matrix、bootstrap routing、OpenCode修理、manifest metadata boundary、release/CI gate、README positioning をまとめて検証し、Plans marker を更新する。 | (a) `bash tests/test-tool-capability-matrix.sh` PASS、(b) `bash tests/test-bootstrap-routing-contract.sh` PASS、(c) `bash scripts/sync-skill-mirrors.sh --check` PASS、(d) `bash tests/test-codex-package.sh` PASS、(e) `node scripts/validate-opencode.js` PASS、(f) `bash scripts/release-preflight.sh` PASS、(g) `./tests/validate-plugin.sh` PASS、(h) `Plans.md` Phase 70 tasks are updated with evidence commit refs | 70.1.1, 70.1.2, 70.1.3, 70.2.1, 70.2.2, 70.2.3, 70.3.1, 70.3.2 | cc:完了 [65f8c31e] |

---

## Phase 71: R03 protected-path break-glass ask gate with narrow audit trail [P1]

Purpose: Issue #140 の「`.env` など protected path への deploy 運用上の shell write を project 単位で進めたい」要望を、guardrail 全体停止や `disabled_rules` の粗い無効化にせず、安全側へ寄せて実装する。採点レビューでは、`disabled_rules: [R02/R03]` は blast radius が大きく Reject、R02 の local Write/Edit opt-out も自己昇格リスクが高いため Reject、R03 の `.env` / `.env.*` shell write を deny から `ask` へ落とす break-glass だけ Required と判断する。既定挙動は R02/R03 deny のまま維持し、break-glass できる対象は exact/narrow な `.env` 系 secret path に限定する。`.git/`, `secrets/`, `*.pem`, `*.key`, SSH key/trust file, `.claude/hooks`, `.husky`, shell rc/profile などの hard-deny class は引き続き deny とし、ask 時は reason / source / rule / path を permission reason に残す。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 71.1.1 | [tdd:required] R03 protected path break-glass ask gate を実装する。`harness.toml` の project-level 設定から `[[safety.guardrail.protectedPathAskList]]` を読み、`path`, `reason` で `.env` / `.env.*` への R03 shell write だけを deny から ask に落とす。`disabled_rules`、R02 opt-out、silent allow、env bypass は実装しない。YAML support は silent ignore / 自己昇格リスクがあるため v1 では対象外。R03 ask には rule id / matched path / config source / reason を含め、secret 値は出さない。仕様・テンプレート・README も「deny by default / R03 ask-only break-glass」として更新する。 | (a) 未設定時の `TestR02_WriteToEnv`, `TestR03_EchoToEnv`, `.git/config` deny が現状通り PASS、(b) TOML ask-list + reason ありで R03 `.env` shell write が `DecisionAsk` になり、reason に `R03`, path, `harness.toml`, configured reason が含まれる、(c) TOML ask-list があっても R02 `.env` Write/Edit は deny のまま、(d) missing reason / path mismatch / `.git`, `secrets/`, `*.pem`, `*.key`, SSH key/trust file, shell rc/profile, `.claude/hooks`, `.husky` は deny のまま、(e) Issue の `sed -i` 例が現行 extractor 対象外であることを test/doc で明示するか、別 task 化せず scope 外として記録、(f) `go/SPEC.md`, `harness.toml`, `templates/.claude-code-harness.config.yaml.template`, `go/cmd/harness/init.go`, README/README_ja が新契約を説明、(g) `cd go && go test ./internal/guardrail ./pkg/config ./cmd/harness` PASS、(h) `bash tests/test-claude-upstream-integration.sh` PASS、(i) `./tests/validate-plugin.sh` PASS | 70.4.1 | cc:完了 |

---

## Phase 72: New Harness root spec + lane/stage planning contract [P1]

Purpose: `Plans.md` は作業台帳、root `spec.md` は repo 全体の product contract として分離し、AI が未取得・未観測のデータを「存在しない」と断定しない New Harness の正本を作る。過去の成功パターンである Plans first、spec SSOT、TDD red evidence、major-only / full review の使い分け、OK まで再レビュー、PR / release evidence を、`[lane:fast|gate|release]` と stage gate で一貫運用する。Plan の作り方は「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout」とし、既存 `cc:TODO/WIP/完了` marker は壊さず、lane / stage metadata と DoD で表現する。`harness-review` は read-only boundary を維持し、PR 作成は `harness-work` 側の明示 closeout として扱う。Release lane は PR と公開配布を混同せず、tag / GitHub Release / CI / version surface まで削らない。

V2 Spec Snapshot: root `spec.md` を上位正本、`docs/plans/spec-ssot.md` を spec 運用ルール、`docs/architecture/hokage-core.md` と `go/SPEC.md` を sub-spec とする。作業は `[lane:fast]`, `[lane:gate]`, `[lane:release]` で分類し、stage gate は「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout -> release closeout」。未取得・未観測の情報は `unknown` とし、`not_observed != absent` を V2 の必須契約にする。PR closeout は `harness-work` 側、release は `harness-release` 側、`harness-review` は read-only quality gate のまま維持する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 72.1.1 | [Plan / SSOT] root `spec.md` を `Plans.md` と同階層に新設し、repo 全体の上位 product contract として固定する。`docs/plans/spec-ssot.md` は「spec 運用ルール」、`docs/architecture/hokage-core.md` は architecture sub-spec、`go/SPEC.md` は Go runtime sub-spec と明記し、二重正本化を防ぐ。`spec.md` には Purpose / Users And Workflows / Core Workflow / Lane Taxonomy / Unknown Data Contract / Host Adapter Boundary / Data And Contracts / Non-Goals / Open Decisions / Links を置く。[tdd:skip:docs-only] | (a) root `spec.md` が存在し、`Plans.md is the task ledger` と `spec.md is the product contract` の役割分担が明記される、(b) `spec.md` に `not_observed != absent` または同等の未観測データ契約がある、(c) `docs/plans/spec-ssot.md` の優先 spec path が root `spec.md` -> existing project spec -> `docs/spec/00-project-spec.md` の順になる、(d) `spec.md` が `docs/architecture/hokage-core.md` と `go/SPEC.md` を sub-spec としてリンクし、置き換えないと明記、(e) `bash tests/test-spec-ssot-workflow.sh` PASS | 71.1.1 | cc:完了 |
| 72.1.2 | [Planning Flow] `harness-plan` の create/add 契約を更新し、Plan 作成を「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout」の stage gate で出力する。各 task に `[lane:fast]`, `[lane:gate]`, `[lane:release]` のいずれかを付け、Fast/Gate/Release を新 skill ではなく Plans metadata として扱う。AI が見えていない data / failed search / missing fixture / API unavailable は `unknown` とし、`absent` と断定しない。 | (a) `skills/harness-plan/SKILL.md` と `skills/harness-plan/references/create.md` が root `spec.md` 優先、lane taxonomy、stage gate、unknown data contract を説明、(b) Plans template が既存 5 column format を維持し、Content または DoD に lane/stage metadata を入れる、(c) `[lane:fast]` は low-risk local work、`[lane:gate]` は spec / workflow / mirror / guardrail、`[lane:release]` は public artifact / version / tag / GitHub Release と明記、(d) `bash tests/test-harness-plan-quality.sh` または同等の new governance test が lane/stage/root spec/unknown data contract を検出、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 72.1.1 | cc:TODO |
| 72.1.3 | [Execution / TDD] `harness-work` と Worker / Scaffolder / Reviewer input を更新し、`spec_path`, `lane`, `stage`, `research_evidence`, `tdd_red_log`, `review_artifact`, `pr_closeout` を sprint contract へ渡す。`[tdd:required]` task では red-log または明示 `skip_tdd_reason` がない限り完了扱いにしない。Fast lane は軽くするが、unknown data contract と focused checks は省かない。 | (a) `skills/harness-work/SKILL.md`, `skills/harness-work/references/execution-modes.md`, `agents/worker.md`, `agents/scaffolder.md`, `agents/reviewer.md` が `spec_path` と `lane` を扱う、(b) `scripts/generate-sprint-contract.js` または contract enrichment が lane/stage/evidence fields を出力、(c) TDD required で red evidence 不足なら review/closeout が fail する test がある、(d) Fast lane の closeout は focused tests + evidence pack を要求し、full review を必須にしない、(e) `bash tests/test-spec-ssot-workflow.sh` と `bash tests/test-tdd-enforcement-l1l2l4.sh` PASS | 72.1.2 | cc:TODO |
| 72.1.4 | [Review Gate] `harness-review` / `harness-release` の gate を更新し、root `spec.md` alignment、Plans lane/stage、TDD evidence、unknown data contract、accepted/rejected findings、release-preflight warning handling を確認する。`harness-review` は read-only のままにし、`APPROVE` は commit / push / PR 作成命令ではないと維持する。 | (a) `skills/harness-review/SKILL.md` と references が `spec.md` alignment、`not_observed != absent`、lane/stage、TDD evidence、evidence pack を合格条件に含む、(b) 証拠なしの「問題なし」「データなし」を APPROVE しない contract が test で検出される、(c) `skills/harness-release/SKILL.md` が PR ready と release ready を分け、release lane では version/tag/GitHub Release/CI を要求、(d) `bash tests/test-harness-review-governance.sh` と `bash tests/test-harness-release-governance.sh` PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 72.1.3 | cc:TODO |
| 72.1.5 | [PR Closeout] `harness-work --pr` または `scripts/harness-pr-closeout.sh` を追加し、review APPROVE 後に PR title/body を evidence pack から作る。PR 作成は外部 side effect なので default は dry-run または明示確認 gate とし、`harness-review` から自動 push/PR/merge はしない。 | (a) PR closeout artifact が `base_ref`, `head_ref`, `spec_path`, `lane`, `stage`, review command, focused tests, accepted findings, rejected findings, release-preflight warnings, residual risk を含む、(b) default dry-run で PR body preview を出し、明示 flag または confirmation なしに `git push` / `gh pr create` しない、(c) detached HEAD では branch 作成が必要なことを fail-fast または ask gate で扱う、(d) `bash tests/test-pr-closeout.sh` または同等 test PASS、(e) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` 経路と矛盾しない | 72.1.4 | cc:TODO |
| 72.1.6 | [Mirror / Final Validation] Phase 72 全体の mirror / distribution / no-regression closeout。`skills/` SSOT から `codex/.codex/skills/`, `opencode/skills/`, `.agents/skills/` へ同期し、root `spec.md` と Plans lane/stage 契約が Claude / Codex / OpenCode で drift しないことを検証する。 | (a) `bash scripts/sync-skill-mirrors.sh --check` PASS、(b) `bash tests/test-skill-design-contract.sh` PASS、(c) `bash tests/test-bootstrap-routing-contract.sh` PASS、(d) `bash tests/test-codex-package.sh` PASS、(e) `node scripts/validate-opencode.js` PASS、(f) `bash tests/test-distribution-archive.sh` PASS、(g) `bash scripts/release-preflight.sh --check-adapters` または current supported equivalent PASS、(h) `./tests/validate-plugin.sh` PASS、(i) `Plans.md` の 72.1.1-72.1.6 が evidence commit refs 付きで `cc:完了` に更新される | 72.1.1, 72.1.2, 72.1.3, 72.1.4, 72.1.5 | cc:TODO |

---

## Phase 73: Superpowers-grade tool-first onboarding + adapter expansion [P1]

Purpose: Superpowers から「共通 skills + 薄い host adapter + 起動時 bootstrap + skill trigger smoke + migration warning」の勝ち筋を最大限 cherry-pick する。ただし、Harness は Claude-first / Hokage Core 内部抽出中という現実を崩さず、support claim は `supported`, `internal-compatible`, `candidate`, `future/unsupported` の tier で管理する。新規ユーザーは「今使っている tool から始める」動線へ寄せ、既存ユーザーは旧 Claude plugin / Codex setup / OpenCode mirror を壊さず、移行影響・backup・rollback・harness-mem state を明示する。Plan の作り方は Phase 72 の stage gate をそのまま適用し、「検証・調査 -> 実装計画確定 -> 実装(TDD) -> レビュー -> PR closeout -> release closeout」の順に進める。Cursor、Codex app、OpenCode、GitHub Copilot CLI、Antigravity CLI は同時に見に行くが、公式/current docs と runtime smoke がない host は public support claim に上げない。

Superpowers Cherry-Pick Snapshot: local Superpowers は `.claude-plugin/marketplace.json`, `.codex-plugin/plugin.json`, `.cursor-plugin/plugin.json`, `.opencode/plugins/superpowers.js`, `hooks/session-start`, `skills/using-superpowers`, skill-trigger tests, Codex plugin sync tests, OpenCode plugin tests を持つ。Harness に取り込むのは「具体ファイルの丸写し」ではなく、(1) tool-first install front door、(2) bootstrap-first UX、(3) host-specific tool mapping、(4) support tier と false-parity gate、(5) skill-trigger/runtime smoke、(6) migration warning / rollback の contract である。

New Session Start: 新しいセッションは 73.1.1 から開始する。最初の作業は実装ではなく `docs/research/superpowers-cherrypick.md` の evidence artifact 作成であり、ここで Adopt / Adapt / Reject / Unknown、対象 files、必要 tests、外部 current-doc links、host claim boundary を確定してから 73.1.2 以降へ進む。73.1.4-73.1.10 の実装に入る前に、73.1.1 と 73.1.2 が完了していることを必須条件にする。途中で公式 docs / runtime smoke / local tool availability が未観測なら `not_observed != absent` として止め、support claim を上げない。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 73.1.1 | [Research / Evidence][lane:gate][tdd:skip:research-doc] Superpowers cherry-pick matrix を作成する。local Superpowers の Claude / Codex / Cursor / OpenCode / Copilot surfaces と、Harness の Phase 70 / 72 docs を突き合わせ、Adopt / Adapt / Reject / Unknown を分ける。Cursor、OpenCode、GitHub Copilot CLI、Antigravity CLI は公式/current docs を確認し、Codex は local `codex plugin --help` と既存 `setup-codex` 導線を確認する。 | (a) `docs/research/superpowers-cherrypick.md` が作成され、local Superpowers evidence と Harness evidence を file path 付きで持つ、(b) 各 cherry-pick に Adopt / Adapt / Reject / Unknown と Why がある、(c) Cursor / OpenCode / GitHub Copilot CLI / Antigravity CLI の current-doc check 結果が URL / date / observed capability 付きである、(d) Codex CLI direct plugin path は local help output と smoke plan を分けて記録、(e) Antigravity は adapter route が未確認なら `future/unsupported` として残す、(f) `not_observed != absent` を維持する | 72.1.1 | cc:完了 |
| 73.1.2 | [Implementation Plan Freeze][lane:gate][tdd:skip:docs-contract] support tier / host adapter claim contract を確定する。root `spec.md`, `docs/architecture/hokage-core.md`, `docs/tool-capability-matrix.md`, `docs/hokage-spin-off-readiness.md` を、Claude Code / Codex CLI / Codex app / OpenCode / Cursor / GitHub Copilot CLI / Antigravity CLI の tier と gate で揃える。 | (a) `spec.md` に Host Adapter Boundary / Support Tiers / Onboarding Contract がある、(b) capability matrix が target hosts を `supported`, `internal-compatible`, `candidate`, `future/unsupported` の tier で表現する、(c) README support wording は tier に反する host claim を含まない、(d) `bash tests/test-tool-capability-matrix.sh` が false parity と unsupported claim を検出、(e) `bash tests/test-bootstrap-routing-contract.sh` PASS | 73.1.1 | cc:完了 |
| 73.1.3 | [Onboarding Front Door][lane:gate][tdd:skip:docs-first] 新規ユーザー向けの tool-first onboarding を追加する。Claude Code / Codex CLI / Codex app / OpenCode / Cursor / GitHub Copilot CLI / Antigravity CLI を同じ一覧に置くが、未検証 host は install 手順ではなく candidate / unsupported と明記する。 | (a) `docs/onboarding/index.md` と `docs/onboarding/install.md` があり、最初に「今使っている tool」を選ばせる、(b) 各 host に install/update/uninstall または unsupported reason がある、(c) first prompt / first command / verification command / success look を持つ、(d) Claude-first install と既存 `scripts/setup-codex.sh --user` 導線を壊さない、(e) `README.md` / `README_ja.md` の front door が onboarding doc へ誘導する | 73.1.2 | cc:完了 |
| 73.1.4 | [Codex CLI / Codex App Adapter][lane:gate][tdd:required] Codex direct plugin path を Superpowers 方式で検証し、可能なら `.codex-plugin/plugin.json` と marketplace install smoke を追加する。Codex app は CLI と同一扱いにせず、app worktree / sandbox / UI handoff の smoke を別 gate にする。 | (a) failing test が `.codex-plugin/plugin.json` / marketplace source / installed skills / composer metadata の期待を先に示す、(b) isolated `CODEX_HOME` で `codex plugin marketplace add` と `codex plugin add` または current equivalent が smoke される、(c) `scripts/setup-codex.sh --user` fallback が残る、(d) `scripts/codex-companion.sh task/review` 経路と矛盾しない、(e) Codex app は `supported` ではなく CLI と別の proof artifact を要求する、(f) `bash tests/test-codex-package.sh` と new Codex plugin smoke PASS | 73.1.3 | cc:完了 |
| 73.1.5 | [OpenCode Adapter][lane:gate][tdd:required] OpenCode を mirror-only から bootstrap-proven adapter へ上げる。Superpowers の `.opencode/plugins/superpowers.js` から、skills path registration、bootstrap injection、bootstrap caching、native skill tool smoke の考え方を取り込む。 | (a) failing test が OpenCode bootstrap injection / no duplicate injection / native skill load を先に示す、(b) `.opencode/plugins/` または current official plugin route を使う場合は setup/docs/preflight が同じ phase で消費する、(c) `opencode/README.md` が stale commands/MCP wording を含まない、(d) `node scripts/validate-opencode.js` PASS、(e) OpenCode runtime smoke が unavailable の場合は `internal-compatible` のまま残し、public support wordingを上げない | 73.1.3 | cc:完了 |
| 73.1.6 | [Cursor Adapter Candidate][lane:gate][tdd:required] Cursor は Superpowers の `.cursor-plugin` / rules 形を参考にしつつ、Harness では current Cursor docs と runtime smoke が通るまで candidate に留める。既存 Cursor PM handoff docs と adapter support を混同しない。 | (a) official/current Cursor docs に基づく adapter route が evidence doc に記録される、(b) `.cursor-plugin` / `.cursor/rules` / hook equivalent を追加する場合は setupまたはtestが同じ phase で消費する、(c) existing 2-agent handoff docs は `handoff integration` と明記し support claim にしない、(d) smoke unavailable なら `future/unsupported` または `candidate` のまま、(e) README が Cursor support を premature claim しない test がある | 73.1.3 | cc:完了 |
| 73.1.7 | [GitHub Copilot CLI Adapter Candidate][lane:gate][tdd:required] GitHub Copilot CLI は Superpowers の tool mapping を参考にするが、Harness-specific bootstrap / skill routing / CLI smoke がない限り support claim しない。 | (a) official GitHub docs に基づく CLI capability と command surface が evidence doc に記録される、(b) Copilot CLI が plugin/skill/bootstrapを持たない場合は manual instruction profile として扱う、(c) support matrix は Superpowers evidence を Harness support proof として使わない、(d) smoke command が存在する場合は isolated環境で transcript proof を残す、(e) README support wording gate が Copilot premature claim を fail する | 73.1.3 | cc:完了 |
| 73.1.8 | [Antigravity CLI Adapter Candidate][lane:gate][tdd:skip:unknown-until-docs] Antigravity CLI はまず official/current docs と local install availability を確認する。plugin/skill/hook/bootstrap contract がなければ adapter ではなく manual instruction profile として扱う。 | (a) official/current Antigravity CLI docs の observed capabilities が evidence doc にある、(b) plugin/skill/hook/bootstrap route が未確認なら `future/unsupported` と明記、(c) manual profile を追加する場合も support claim ではなく candidate doc とする、(d) no local evidence を `absent` と断定せず `not observed` と書く、(e) README / matrix が Antigravity support を premature claim しない | 73.1.3 | cc:完了 |
| 73.1.9 | [Existing User Migration][lane:gate][tdd:required] 既存ユーザー向け migration impact / compatibility / rollback を実装する。Claude plugin install、Codex user/project setup、OpenCode mirror setup、harness-mem state を inventory し、削除なしで report する `doctor --migration-report` または equivalent を追加する。 | (a) failing tests が stale plugin cache、duplicate local skills、old symlink、missing slash entries、Codex backup path、OpenCode backup path、harness-mem state を検出する、(b) report は backup location と rollback proposal を出す、(c) memory DB を削除しない、(d) destructive cleanup は explicit confirm gate、(e) docs/onboarding/migration.md が existing users向け影響と互換性を説明、(f) relevant doctor/setup tests PASS | 73.1.3 | cc:完了 |
| 73.1.10 | [Bootstrap / Skill Trigger Acceptance][lane:gate][tdd:required] Superpowers の skill-trigger tests を Harness に移植し、install 後に agent が正しい skill / workflow を使うところまで onboarding の合格条件にする。 | (a) Claude explicit/implicit skill trigger runner が `harness-plan`, `harness-work`, `harness-review`, `harness-release`, `harness-setup`, `breezing` を検査、(b) Codex companion smoke が raw `codex exec` ではなく `scripts/codex-companion.sh` 経由を検査、(c) OpenCode native skill smoke が availableなら実行、unavailableなら evidenceに理由を残す、(d) first prompt transcript または fixtureが `not_observed != absent` を守る、(e) release preflight が claimed host の smoke absence を検出 | 73.1.4, 73.1.5, 73.1.6, 73.1.7, 73.1.8 | cc:完了 |
| 73.1.11 | [README / Product Surface][lane:fast][tdd:skip:docs-only] README / README_ja を Superpowers の読みやすい front-door 構造に寄せる。Quickstart、How it works、Install by tool、First 15 minutes、Existing user migration、Advanced を分け、support tier に反する文言を削る。 | (a) README/README_ja が tool-first onboarding docsへリンク、(b) Claude-first positioning と Hokage Core extraction underway を維持、(c) Cursor / Copilot / Antigravity の premature support wording がない、(d) `rg` claim scan と `git diff --check` PASS | 73.1.10 | cc:完了 |
| 73.1.12 | [Review Gate][lane:gate] Phase 73 全体を harness-review で確認する。support tier、false parity、migration safety、skill-trigger smoke、unknown data handling、PR/release boundary を重点にする。 | (a) `harness-review` または `scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` が evidence pack をレビュー、(b) major finding は `REQUEST_CHANGES`、(c) accepted/rejected findings が PR closeout artifact に残る、(d) review は read-only で commit/push/PR をしない | 73.1.11 | cc:完了 |
| 73.1.13 | [PR / Release Closeout][lane:release] Phase 73 の PR / release readiness を閉じる。claimed host だけを release preflight hard gate にし、candidate / unsupported host は evidence doc と wording gate で管理する。 | (a) `bash scripts/sync-skill-mirrors.sh --check` PASS、(b) `bash tests/test-bootstrap-routing-contract.sh` PASS、(c) `bash tests/test-tool-capability-matrix.sh` PASS、(d) `bash tests/test-codex-package.sh` PASS、(e) `node scripts/validate-opencode.js` PASS、(f) `bash scripts/release-preflight.sh --check-adapters` PASS or explicit unavailable reasons、(g) `./tests/validate-plugin.sh` PASS、(h) PR body includes support tiers, host smoke evidence, migration impact, residual risk, unsupported hosts | 73.1.12 | cc:完了 |

---

## Phase 74: Repo health gates + release/runtime distribution assurance [P1]

Purpose: Phase 73 の closeout 後に見えた弱点を、表面的な style 論ではなく release safety と配布先保証として閉じる。現状は Go build/test/vet、actionlint、plugin validation、multi-OS smoke、OpenCode mirror validation、release-preflight adapter gates まである一方で、`gofmt` gate がなく `gofmt -l go` が 28 files を返す、shell script 群に `shellcheck` / `shfmt` gate がない、tag-triggered `release.yml` は直前の `release-preflight --check-adapters` を必須化していない、Codex / OpenCode は実 host runtime smoke ではなく static / Node-level smoke に寄っている。Phase 74 は「lint/format を重くしすぎず、配布先で壊れないことを claim 境界に合わせて強める」ための改善計画とする。

New Session Start: まず 74.1.1 で現状の repo-health baseline を evidence doc に固定し、`gofmt -l go` の件数、CI workflow の現行 gate、Codex/OpenCode smoke の skip/runtime 境界、`git archive HEAD` が dirty worktree を見ない事実を記録する。過去 Phase 66 / 70 の release-preflight / mirror drift gate を車輪の再発明せず再利用し、harness-mem evidence と generated mirror checks の serial execution 境界も固定する。74.1.2 以降の実装前に、formatter による大きな機械差分と機能変更を同じ commit に混ぜない方針を確定する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 74.1.1 | [Research / Evidence][lane:gate][tdd:skip:research-doc] repo-health / CI-CD 配布保証 baseline を作成する。`docs/research/repo-health-ci-cd-gaps.md` に、現行 CI (`validate-plugin`, `smoke-install`, `opencode-compat`, `release.yml`)、`scripts/release-preflight.sh`, `tests/test-distribution-archive.sh`, Go / shell / JS toolchain の lint/format 有無を evidence 付きで整理する。 | (a) `docs/research/repo-health-ci-cd-gaps.md` がある、(b) `gofmt -l go` の観測結果と件数が記録される、(c) `go test` / `go vet` / `actionlint` / plugin validation / multi-OS smoke / OpenCode mirror validation の既存 gate が file:line 付きで記録される、(d) `shellcheck` / `shfmt` / root ESLint / Prettier が未導入であることを「不要」ではなく「未整備」として記録、(e) Codex CLI / OpenCode runtime smoke が static or skipped になり得る境界を明記、(f) `not_observed != absent` を維持する、(g) Phase 66 / 70 の release-preflight / mirror drift gate を再利用し、二重実装しない方針が明記される、(h) harness-mem observation IDs と、parallel search / generated mirror checks の timeout・drift 誤判定境界が記録される | 73.1.13 | cc:完了 |
| 74.1.2 | [Format Gate][lane:gate][tdd:required] Go formatting gate を最小導入する。まず `gofmt -w` の機械差分を機能変更と分けて処理し、`tests/test-format-lint.sh` または CI step で `gofmt -l go` が空であることを fail gate にする。 | (a) `gofmt -l go` が 0 件、(b) format-only diff と機能 diff を分離した closeout note が `docs/research/repo-health-ci-cd-gaps.md` に残る、(c) `tests/test-format-lint.sh` と `.github/workflows/validate-plugin.yml` が `gofmt -l go` を fail gate として実行、(d) `go test ./...` と `go vet ./...` は既存通り PASS、(e) `git diff --check` PASS | 74.1.1 | cc:完了 |
| 74.1.3 | [Shell Lint Baseline][lane:gate][tdd:required] shell script の lint を段階導入する。いきなり全 repo を fail gate にせず、release / setup / ci / test helper の高リスク subset から `shellcheck` を導入し、`shfmt -d` は対象 subset または advisory として始める。 | (a) 対象 subset が `scripts/release-preflight.sh`, setup scripts, `scripts/ci/*.sh`, distribution/release smoke tests, new lint tests として `docs/research/repo-health-ci-cd-gaps.md` に明記、(b) CI に `shellcheck` install + targeted run が入る、(c) 初回 warning/info と broader test errors は allowlist / TODO ではなく evidence doc に分類、(d) `shfmt -d` は未観測のため advisory、(e) release path の shell 構文劣化は `tests/test-shell-lint.sh` の `shellcheck --severity=error` で検出 | 74.1.2 | cc:完了 |
| 74.1.4 | [Release Preflight Hardening][lane:release][tdd:required] tag / GitHub Release 前の配布保証を強める。`release.yml` または `harness-release` flow が、公開 tag / asset upload の前に clean tree の `scripts/release-preflight.sh --check-adapters` 相当を要求するようにし、dirty worktree と `git archive HEAD` の差を明示する。 | (a) `.github/workflows/release.yml` が release asset publish 前に `scripts/release-preflight.sh --check-adapters` を実行、(b) `scripts/release-preflight.sh --check-adapters` が release claim 前の正規 command として `docs/release-preflight.md` / closeout に出る、(c) `tests/test-distribution-archive.sh` が `git archive HEAD` の性質を docs で明記、(d) CI status unavailable は release-ready ではなく warning boundary として明記、(e) `bash tests/test-release-preflight.sh` PASS | 74.1.3 | cc:完了 |
| 74.1.5 | [Host Runtime Smoke][lane:gate][tdd:required] Codex CLI / OpenCode の実 runtime smoke を CI に入れるか、入れられない場合は support tier を据え置く contract を強化する。Codex は CLI install 可否、isolated `CODEX_HOME` marketplace smoke、OpenCode は real binary / native skill smoke 可否を分ける。 | (a) Codex CLI は CI で `@openai/codex@0.133.0` を install し、`tests/test-codex-plugin-adapter.sh` を required runtime smoke として実行、(b) Codex install 不可時は `HARNESS_CODEX_PLUGIN_SMOKE_REQUIRED=1` で fail、(c) OpenCode real binary smoke は未観測のため explicit warning + `internal-compatible` 維持、(d) candidate hosts は runtime smoke なしに tier を上げない、(e) capability matrix / evidence doc が runtime smoke の有無を反映 | 74.1.4 | cc:完了 |
| 74.1.6 | [Closeout / Confidence Update][lane:release] Phase 74 の結果を confidence HTML / PR body / release boundary に反映する。Phase 73 の「作業完了」と Phase 74 の「repo-health / distribution assurance」を分け、release score を clean preflight と CI 結果で更新する。 | (a) `docs/phase-73-completion-confidence.html` が Phase 74 の lint/format / CI-CD / runtime smoke 改善結果を反映、(b) `docs/research/phase-73-pr-closeout.md` に repo-health gate と residual risk が含まれる、(c) pre-commit dirty worktree / mirror drift と post-commit `bash scripts/release-preflight.sh --check-adapters` PASS with warnings を `docs/research/repo-health-ci-cd-gaps.md` に記録、(d) `bash tests/validate-plugin.sh` PASS、(e) `Plans.md` 74.1.1-74.1.6 が evidence refs 付きで更新される | 74.1.5 | cc:完了 |

---

## Phase 75: README front door + bilingual visual refresh [P1]

Purpose: README / README_ja を単なる説明書ではなく、Superpowers と Hermes Agent 級の product front door に上げる。Phase 73.1.11 の軽量 refresh では、benchmark artifact、claim-vs-visual audit、画像再生成、asset manifest、GPT-image2.0 実行経路確認まで含めていなかったため、Phase 75 で切り直す。採る形は「短い価値提示 -> すぐ試せる導入 -> command の中身 -> 実際の workflow -> support tier -> deeper docs」。ただし Superpowers の host support claim や Hermes の runtime/infrastructure claim はコピーしない。Harness は `spec.md` の support tiers と unknown data contract を維持し、候補 host は候補としてだけ扱う。

Benchmark Snapshot: `docs/research/readme-superpowers-hermes-benchmark.md` に、Superpowers の tool-first install / basic workflow / skill library と、Hermes の hero / quick install / command quick reference / docs map を比較した。Harness が採用するのは front-door 構造、first-run clarity、command の中身説明、bilingual parity、asset manifest であり、未検証 cross-host support や内部コード名の public positioning は採用しない。

New Session Start: 最初は 75.1.1 から開始する。README 本文や画像をすぐ生成せず、まず claim inventory と asset inventory を固定する。GPT-image2.0 の使用は 75.1.4 で local / official image generation path と model selector を確認してから実行し、画像に文字が入る場合は英語版と日本語版を別 asset として生成する。画像生成後は 75.1.5 で README 参照、alt text、file size、support claim boundary を検証する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 75.1.1 | [Research / Evidence][lane:gate][tdd:skip:research-doc] README claim / asset inventory を固定する。`docs/research/readme-superpowers-hermes-benchmark.md` を起点に、現行 `README.md`, `README_ja.md`, `CHANGELOG.md`, README 参照画像、`docs/onboarding/*`, `docs/tool-capability-matrix.md`, `spec.md` を突き合わせ、残す claim / 削る claim / 画像差し替え対象 / bilingual parity 不足を一覧化する。 | (a) research doc または追補が Superpowers / Hermes source URL、local file evidence、Adopt / Adapt / Reject / Unknown、対象 files、必要 tests、support claim boundary を持つ、(b) `docs/images/hokage/hokage-hero.jpg` など obsolete/dark/internal-code-name 画像の扱いが決まる、(c) text-bearing images の EN/JA 別 asset list がある、(d) GPT-image2.0 実行経路は未確認なら `Unknown` のまま残す、(e) `not_observed != absent` を維持する | 74.1.6 | cc:完了 |
| 75.1.2 | [Spec / Copy Architecture Freeze][lane:gate][tdd:skip:docs-contract] README product surface contract を確定する。`spec.md` の README Product Surface Contract と Phase 75 research を合わせ、README / README_ja の新構成を freeze する。 | (a) README outline が hero, quickstart, install-by-tool, command internals, basic workflow, proof/support tiers, migration, docs map の順で固定される、(b) 日本語版は専門用語を減らし、英語版の直訳ではなく同じ claim を低認知負荷で伝える、(c) internal history / Hokage meta / operator-only HTML artifact を top-level README から外す、(d) support tier wording は `spec.md` と `docs/tool-capability-matrix.md` に一致、(e) `tests/test-readme-product-surface.sh` の更新方針が決まる | 75.1.1 | cc:完了 |
| 75.1.3 | [README Rewrite][lane:gate][tdd:required] README / README_ja を新構成に書き換える。まず README surface test を red にし、hero copy、Quickstart、各 command の「中でやること」、Basic Workflow、Install by tool、Existing user migration、Docs map を更新する。 | (a) `README.md` と `README_ja.md` が新構成に揃う、(b) 各 command に concise な internal-behavior explanation がある、(c) support tier に反する Cursor / Copilot / Antigravity / Codex app claim がない、(d) user pain と latest Harness state が最初に伝わる、(e) `bash tests/test-readme-product-surface.sh`, `bash tests/test-support-claim-wording.sh`, `git diff --check` PASS | 75.1.2 | cc:完了 |
| 75.1.4 | [Visual Prompt Pack][lane:gate][tdd:skip:asset-plan] GPT-image2.0 で再生成する画像の prompt pack と asset manifest を作る。公式ロゴのトンマナ、白背景、文字あり/なし、英語/日本語別生成、alt text、出力先、寸法、禁止 claim を定義する。 | (a) asset manifest が source prompt, model/tool path, output path, language, dimensions, alt text, text-bearing flag を持つ、(b) GPT-image2.0 の実行方法が local/official path で確認される。確認できない場合は実行せず blocker とする、(c) official logo tone / white background / no unsupported host parity / no obsolete code-name claim が prompt contract に入る、(d) text-bearing images は EN/JA ペアが必須、(e) generated image provenance を README claim と混同しない | 75.1.2 | cc:完了 |
| 75.1.5 | [Generate / Integrate Visuals][lane:gate][tdd:required] GPT-image2.0 で README 画像を再生成し、README / README_ja へ統合する。古い暗色 hero や obsolete claim を持つ画像は差し替えまたは参照解除する。 | (a) text-bearing generated assets が英語版/日本語版それぞれ存在、(b) README / README_ja の image references が存在する files を指す、(c) alt text が claim boundary と一致、(d) file size が配布物として妥当、(e) asset parity test または README image reference test PASS、(f) `git diff --check` PASS | 75.1.4 | cc:完了 |
| 75.1.6 | [Review / Release Surface Closeout][lane:release] README / visual refresh の最終 gate。claim audit、bilingual parity、support tier wording、asset manifest、CHANGELOG 記載要否、release artifact suitability を review し、PR/release に載せる根拠を作る。 | (a) `harness-review` または `scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` が README surface / visual assets / support claim を read-only review、(b) `bash tests/test-readme-product-surface.sh`, `bash tests/test-support-claim-wording.sh`, `bash tests/test-tool-capability-matrix.sh`, asset parity check, `git diff --check` PASS、(c) CHANGELOG は user-facing change として記録し、operator-only HTML や内部経緯を配布価値として書かない、(d) PR body includes source benchmark, tests, generated asset provenance, residual risk, unsupported hosts, (e) Plans marker updates after evidence is complete | 75.1.3, 75.1.5 | cc:承認待ち |

---

## Phase 76: Spec.md / Plans.md co-required planning output [P1]

Purpose: `/harness-plan` を「Plans.md 生成」ではなく、root `spec.md` と `Plans.md` の co-required planning output を返す planning surface へ更新する。正本順序は `spec.md > sub-spec > Plans.md` を維持し、`spec.md` は「何が正しいか」を固定する product contract、sub-spec は scoped contract、`Plans.md` は「何をどの順でやるか」を固定する task ledger とする。AI が見えていない仕様を勝手に補わないため、`create` / product-impacting `add` は毎回 root `spec.md` を確認し、Harness が `Spec delta` または `Spec skip reason` を生成する。consumer は一から spec を手書きするのではなく、生成された `Spec delta` / `Spec skip reason` と `Plans.md` draft を承認または修正する。これは Phase 72.1.2 の harness-plan planning surface 部分を具体化して閉じる差分であり、harness-work / review / PR closeout 側の残件は Phase 72.1.3 以降に残す。

New Session Start: まず 76.1.1 で `spec.md` / `docs/plans/spec-ssot.md` / `harness-plan` skill の契約を読み、現行の「必要時だけ spec」文言を「毎回 spec delta または skip reason」へ変える scope を確定する。実装は `skills/harness-plan` を SSOT とし、`codex/.codex/skills/harness-plan`, `opencode/skills/harness-plan`, `.agents` mirror を `scripts/sync-skill-mirrors.sh` で同期する。TDD は `tests/test-harness-plan-quality.sh` と `tests/test-spec-ssot-workflow.sh` を先に red にしてから実装する。Phase 75.1.6 は README release-surface closeout なので Phase 76 の開始 gate にはしないが、README の「Harness が spec/Plans を draft し、user は approve/correct」説明とは矛盾させない。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 76.1.1 | [Plan / Contract Freeze][lane:gate][tdd:skip:docs-contract] `harness-plan` の二正本契約を確定する。`spec.md` と `docs/plans/spec-ssot.md` に、root `spec.md` が product contract、sub-spec が scoped contract、Plans.md が task ledger であり、planning output は `Spec delta` または `Spec skip reason` と `Plans.md task` の co-required output であることを明記する。 | (a) root `spec.md` が `harness-plan` create/add の co-required output を説明、(b) `docs/plans/spec-ssot.md` が root `spec.md` first、`spec.md > sub-spec > Plans.md` precedence、spec delta、skip reason、Plans.md link を説明、(c) skip 可能な docs-only / mechanical work でも skip reason を task context または sprint contract に残す契約がある、(d) `Spec delta` / `Spec skip reason` は Harness が生成し consumer は承認・修正だけと明記、(e) `not_observed != absent` と unknown data contract を維持、(f) 新しい正本 path を増やさない | 75.1.5 | cc:完了 |
| 76.1.2 | [Tests First][lane:gate][tdd:required] 二正本契約の governance test を先に更新する。`tests/test-harness-plan-quality.sh` と `tests/test-spec-ssot-workflow.sh` が root `spec.md` first、`Spec delta`、`Spec skip reason`、Plans.md link、consumer approve/correct wording、mirror 同期を検出するようにする。 | (a) test が `Spec delta` と `Spec skip reason` のどちらもない `create` / product-impacting `add` 出力契約を許さない意図を検出、(b) `spec.md` precedence は `spec.md` > sub-spec > Plans.md として検出、(c) `.agents` を含む mirror sync 境界を検出、(d) `bash tests/test-harness-plan-quality.sh` は実装前に red になり得る、(e) test 変更が既存 maxplan / opencode validation guard を壊さない | 76.1.1 | cc:完了 |
| 76.1.3 | [Skill Update][lane:gate][tdd:required] `harness-plan` skill を `spec.md + Plans.md` の co-required planning output surface に更新する。purpose / Quick Reference / create flow / 仕様正本チェック / optional briefs を変更し、`create` と product-impacting `add` は root `spec.md` を必ず読み、出力に `Spec delta` または `Spec skip reason` を含める。 | (a) `skills/harness-plan/SKILL.md` の purpose が `co-required planning output` と `spec.md product contract and Plans.md task contract` を含む、(b) Quick Reference が `/harness-plan create` を `Spec delta` / `Spec skip reason` + `Plans.md` 生成として説明、(c) create flow が Plans.md 生成前に root `spec.md` 確認を要求、(d) 仕様正本チェックが「二正本チェック」に改名され、毎回確認 + 更新 or skip reason の契約を持つ、(e) `skills/harness-plan/references/create.md` と `references/planning-quality.md` が同じ出力契約を説明、(f) `Spec delta` / `Spec skip reason` は Harness が生成し consumer は承認・修正だけと明記 | 76.1.2 | cc:完了 |
| 76.1.4 | [Mirrors / Validation / Closeout][lane:gate] Phase 76 の最終検証。`harness-plan` が Plans.md だけの surface に戻っていないこと、consumer 向け説明が「手で spec を書く前提」に見えないこと、README の説明と矛盾しないことを確認する。 | (a) `bash tests/test-harness-plan-quality.sh`, `bash tests/test-spec-ssot-workflow.sh`, `bash tests/test-codex-package.sh`, `bash tests/test-readme-product-surface.sh`, `bash scripts/sync-skill-mirrors.sh --check`, `node scripts/validate-opencode.js`, `bash tests/validate-plugin.sh` PASS、(b) `Plans.md` 76.1.1-76.1.4 が evidence refs 付きで更新される、(c) CHANGELOG に user-facing change として記録、(d) Phase 72.1.2 の harness-plan 部分を Phase 76 が閉じたこと、残件は harness-work/review/pr closeout 側に残ることを closeout に残す、(e) residual risk があれば closeout note に残る | 76.1.3 | cc:完了 |

---

## Phase 77: Public issue intake closeout + Done marker canonicalization [P1]

Purpose: v4.12.2 release 後に残った open PR / Issue を、公開ユーザーの痛みと release safety の順に整理する。PR #121 は中国語 description i18n の大型・競合中 PR として後日対応に回し、この Phase では触らない。Issue #147 は単なる parser alias 追加では相手の痛みを解消しないため、新規・更新時の Plans.md / summaries / `.claude/state` notification が `cc:done` を含む English marker family に統一されることを product contract にする。既存 `cc:TODO`, `cc:WIP`, `cc:完了`, `pm:依頼中`, `pm:確認済` は read-compatible として残し、bulk migration は明示コマンドまたはユーザー承認なしに実行しない。Issue #146 は guardrail / runtime reason strings の English default 化、Issue #140 / #131 は既存実装が満たしている範囲を再検証して closeout、PR #136 / #137 は checks rerun 後の低リスク dependency merge、Issue #149 は listing PR を出すかコメントで判断する。

Spec delta: `spec.md` に I18n And Status Marker Contract を追加し、`docs/i18n-language-contract.md` を Issue #147 対応へ更新した。新しい product rule は「新規・更新 writer output は English marker family (`cc:done` など) を emit し、legacy/Japanese markers は read-compatible として受け続ける」。これにより前回案の弱点だった「raw Plans.md の混在言語は残る」問題を解消対象に入れる。Japanese opt-in は説明文・テンプレート文・runtime prose を日本語にできるが、status marker の新規・更新出力は English marker family のままにする。

New Session Start: 次は 77.1.10 から開始する。77.1.2-77.1.3 で #147 の failing tests と canonical marker writer/read compatibility は green になった。77.1.4 は既存 i18n locale resolver / Go guardrail tests で #146 対応済みを確認した。77.1.5-77.1.6 は `docs/research/phase-77-issue-closeout.md` に closeout evidence / comment draft を残した。77.1.7 は PR #136/#137 を個別 merge し、main の `validate-plugin` / `scorecard` が両方 success になったことを確認した。77.1.8 は awesome-codex-plugins の current CONTRIBUTING / README / bundle 状態を確認し、現時点では icon + `interface.composerIcon` と同名 entry ambiguity が未解消のため external PR はまだ出さない判断を evidence 付きで固定した。77.1.9 は Codex companion review の auth failure を記録したうえで manual-pass review closeout を実施し、`docs/research/phase-77-review-closeout.md` に APPROVE evidence を残した。Public issue close/comment は #147 と #131 が main に入ってから実施するため 77.1.10 に分離する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 77.1.1 | `[Plan / Contract Freeze]` `[lane:gate]` `[tdd:skip:docs-contract]` open PR / Issue intake の方針を固定する。#121 は後日対応に回し、#147 は alias-only ではなく new/update writer / notification cleanup として扱う。`spec.md`, `docs/i18n-language-contract.md`, `Plans.md` に product contract と task contract を追加する。 | (a) `spec.md` に new/update status marker contract がある、(b) `docs/i18n-language-contract.md` が Issue #147 と English marker writer output / legacy read compatibility を説明、(c) `Plans.md` に Phase 77 task があり #121 deferred / #147 non-alias-only 方針が明記される、(d) `git diff --check` PASS | 76.1.4 | cc:done |
| 77.1.2 | `[Issue #147 / Tests First]` `[lane:gate]` `[tdd:required]` Status marker localization の failing tests を先に作る。new/update writer/template/notification が `pm:requested`, `cc:todo`, `cc:wip`, `cc:done`, `pm:approved` の同一 family を使い、legacy `cc:TODO`, `cc:WIP`, `cc:完了`, `pm:依頼中`, `pm:確認済` を読み続けることを固定する。 | (a) parser tests が `cc:todo`, `cc:wip`, `cc:done`, `pm:requested`, `pm:approved` と legacy markers の両方を PASS、(b) writer/template tests が mixed marker family を出さない、(c) Japanese opt-in test が prose を日本語化しても marker writes は English family のままになることを固定、(d) `.claude/state` notification / plans watcher / session summary の visible prose が locale resolver を通る、(e) `bash tests/test-i18n-locale-resolver.sh`, relevant Go tests, `bash scripts/plans-format-check.sh` fixture PASS | 77.1.1 | cc:done |
| 77.1.3 | `[Issue #147 / Implementation]` `[lane:gate]` `[tdd:required]` canonical marker writer と locale-aware status summaries を実装する。対象は Plans writer/update helpers、`go/internal/hookhandler/plans_watcher.go`, `go/internal/session/*`, `scripts/stop-plans-reminder.sh`, `scripts/session-monitor.sh`, `scripts/plans-format-check.sh`, sprint-contract status parsing。 | (a) new Plans rows and marker updates emit English aliases, (b) legacy Plans files are not rewritten during read/sync/dry-run, (c) update commands accept both English and Japanese marker inputs, (d) notification prose follows locale while status marker writes stay English family, (e) Issue #147 closeout comment can truthfully say both Plans marker and notification pain are addressed | 77.1.2 | cc:done |
| 77.1.4 | `[Issue #146 / Guardrail English Reasons]` `[lane:gate]` `[tdd:required]` Guardrail / runtime reason strings の English default 化を完了する。Go guardrail の deny/ask/warn reason と shell hook outputs が locale resolver に従い、`CLAUDE_CODE_HARNESS_LANG=ja` では日本語を維持する。 | (a) default env/config で R01/R02/R03/R04/R05 など user-facing deny/ask reason が English、(b) `CLAUDE_CODE_HARNESS_LANG=ja` で既存相当の日本語、(c) hook JSON shape と Decision は変わらない、(d) `cd go && go test ./internal/guardrail ./internal/hookhandler ./internal/session` PASS、(e) `bash tests/test-i18n-locale-resolver.sh` と `bash tests/test-i18n-japanese-ux-regression.sh` PASS | 77.1.1 | cc:done |
| 77.1.5 | `[Issue #140 / Verify and Close Scope]` `[lane:gate]` `[tdd:skip:verification]` R02/R03 protected-path opt-out issue を現行 Phase 71 実装に照らして再検証する。R03 `.env` ask-list break-glass は満たしているが、R02 opt-out / disabled_rules は安全上 Reject した範囲であることを evidence 付きで整理し、必要なら issue comment / close を行う。 | (a) `harness.toml` の `protectedPathAskList` と `go/internal/guardrail/protected_path_policy.go` の挙動を確認、(b) R03 `.env` ask / hard-deny paths / R02 deny の tests PASS、(c) issue comment draft が「満たした範囲」と「意図的にやらない範囲」を分ける、(d) close する場合は user-facing workaround と safety reason を含む | 77.1.1 | cc:done |
| 77.1.6 | `[Issue #131 / Verify and Close]` `[lane:gate]` `[tdd:skip:verification]` codex-loop orphan active job issue を現行実装で再検証する。`runner_lost_job_running`, stop cancellation, cancel failure state, runner exit trap の tests を実行し、再現が解消しているなら closeout comment を作る。 | (a) `bash tests/test-codex-loop-cli.sh` の runner-lost / cancellation 関連 cases PASS、(b) `scripts/codex-loop.sh` が dead runner + active job を `state_stale` だけで潰さない、(c) stop が active companion job を cancel or failure state に残す、(d) issue closeout comment に test command と behavior summary がある | 77.1.1 | cc:done |
| 77.1.7 | `[PR #136 / #137 Dependency Merge]` `[lane:release]` `[tdd:skip:dependency-bump]` Dependabot PR #136 と #137 を checks rerun 後に個別 merge する。どちらも GitHub Actions major upgrade なので、merge 後 main の validate-plugin / scorecard / smoke-install / release-preflight 境界を確認する。 | (a) #136/#137 の current checks が rerun または最新 commit で success、(b) labels missing (`ci`, `dependencies`) は別 maintenance として記録、(c) each PR is merged one at a time, (d) main CI success after merge, (e) no tag/release action is triggered unless explicitly requested。Evidence: #136 merged as `9fd2071b9c40cdd3a4ba6b4e427af8fd89ee302e`; #137 merged as `5f33130b3e6bd95de10e97bb0dfdb1faa75bfda9`; main `validate-plugin` runs `26377642567` / `26377646959` success; main `scorecard` runs `26377642555` / `26377646960` success; no tag/release action was triggered. | 77.1.1 | cc:done |
| 77.1.8 | `[Issue #149 / Listing Decision]` `[lane:fast]` `[tdd:skip:docs-only]` awesome-codex-plugins 掲載提案を判断する。出すなら external README PR のみ、出さないなら issue に理由付きでコメントする。Harness 側 README に premature support claim を追加しない。 | (a) awesome-codex-plugins の CONTRIBUTING format を確認、(b) listing text が current Codex support tier を超えない、(c) external PR or issue comment draft がある、(d) Harness repo に不要な docs churn を入れない。Evidence: target main `68f58ceb19873302b0e771040d47050fc0e5f638`; `CONTRIBUTING.md` requires mirrored bundle + `.codex-plugin/plugin.json` + `interface.composerIcon`; existing target README has `dadwadw233/claude-code-harness` same-name entry; this repo has no `interface.composerIcon` or dedicated icon asset; decision/comment draft recorded in `docs/research/phase-77-issue-closeout.md`. | 77.1.7 | cc:done |
| 77.1.9 | `[Review / Closeout]` `[lane:gate]` Phase 77 の review closeout。#147 / #146 の user pain が実際に解消したか、#140/#131 の close 判断が過大 claim でないか、#136/#137 merge 後 CI が green かをまとめて確認する。 | (a) `harness-review` or `scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE、(b) `bash tests/test-i18n-locale-resolver.sh`, `bash tests/test-i18n-japanese-ux-regression.sh`, relevant Go tests, `bash tests/test-codex-loop-cli.sh`, `bash tests/validate-plugin.sh` PASS、(c) open issues/PRs の残件と public close boundary を明記、(d) CHANGELOG に user-facing fixes を記録、(e) release が必要なら separate `harness-release` gate に渡す。Evidence: companion review failed due Codex auth refresh; `harness-review` manual-pass fallback APPROVE recorded in `docs/research/phase-77-review-closeout.md`; tests passed (`test-codex-loop-cli.sh` 42/42, `validate-plugin.sh` 95/95); public issue close/comment moved to 77.1.10 because #147/#131 fixes must land on main first. | 77.1.2, 77.1.3, 77.1.4, 77.1.5, 77.1.6, 77.1.7, 77.1.8 | cc:done |
| 77.1.10 | `[PR / Public Issue Closeout]` `[lane:release]` `[tdd:skip:external-closeout]` Phase 77 branch を PR 化し、main merge 後に public issue close/comment を実行する。#147 と #131 はこの branch の fix が main に入ってから close、#140/#146 は evidence comment 付き close、#149 は listing blocker comment を残して open 維持、#121 は deferred のまま維持する。 | (a) PR body が #147/#146/#140/#131/#149 の boundary と test evidence を含む、(b) branch CI が green、(c) merge 後 main CI が green、(d) #147/#146/#140/#131 に過大 claim のない close/comment を残す、(e) #149 に icon / `interface.composerIcon` / same-name ambiguity の blocker comment を残す、(f) #121 を触らない、(g) release が必要なら別 `harness-release` gate に渡す。Evidence: PR #150 merged as `d015c803`; branch checks passed including CodeRabbit; main `validate-plugin` run `26379521125`, `smoke-install` run `26379521124`, and `scorecard` run `26379521127` succeeded; clean main preflight passed (`git diff --check`, `bash tests/test-plans-status-markers.sh`, `bash tests/validate-plugin.sh` 95/95); #147/#131/#140/#146 closed with boundary comments; #149 commented and left open; #121 untouched; no release was triggered because Phase 77 closeout did not request a new version/tag, so any release remains a separate `harness-release` gate. | 77.1.9 | cc:done |

---

## Phase 78: Dependabot benchmark alert closeout [P1]

Purpose: v4.12.4 後に GitHub が default branch で警告している残 Dependabot alerts 10 件を閉じる。live GitHub alert API では全件 `benchmarks/breezing-bench/agent-eval/package-lock.json` に集中し、内訳は `undici` 6 件、`minimatch` 3 件、`uuid` 1 件。これは public release artifact そのものではなく benchmark tooling の tracked lockfile だが、repo-health / supply-chain signal として放置しない。`npm audit fix` の downgrade 提案 (`@vercel/agent-eval@0.0.4`) は採らず、current line の `@vercel/agent-eval@0.14.x` と scoped npm overrides で patched ranges へ進める。GitHub alert closeout、local `npm audit`、benchmark smoke、CI、release gate は分けて確認する。

Spec delta: `spec.md` に Supply Chain Alert Contract を追加した。新しい rule は「tracked source/tooling/benchmark/distribution lockfile の Dependabot alert は repo-health finding として live inventory -> grouped remediation -> native override only when needed -> smoke evidence -> Dependabot/CI automation -> GitHub alert closeout の順に扱う」。benchmark-only manifest は full benchmark が unavailable の場合も focused smoke と residual risk を分けて記録する。

New Session Start: まず 78.1.2 から開始する。78.1.1 で alert inventory、temp remediation matrix、spec delta、decision record、Plans task contract は作成済み。実装は `benchmarks/breezing-bench/agent-eval/package.json` / `package-lock.json` と、必要に応じて `.github/dependabot.yml` / audit test / benchmark smoke mismatch に限定する。release は不要想定だが、merge 後に GitHub alerts が閉じることまでは確認する。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 78.1.1 | `[Plan / Alert Inventory]` `[lane:gate]` `[tdd:skip:research-doc]` Dependabot 残 alert を live API と lockfile で棚卸しし、公式 docs / npm audit / temp remediation を evidence 化する。`npm audit fix` の downgrade 提案を reject し、current-line update + scoped overrides を primary plan とする。 | (a) `docs/evidence/dependabot-alerts-2026-05-27.md` に alert 10 件、manifest path、package grouping、patched ranges、temp proof がある、(b) `spec.md` に Supply Chain Alert Contract がある、(c) `.claude/memory/decisions.md` に downgrade reject と scoped override 採用理由がある、(d) `git diff --check` PASS | 77.1.10 | cc:done |
| 78.1.2 | `[Tests First / Audit Gate]` `[lane:gate]` `[tdd:required]` benchmark agent-eval manifest 用の audit gate を追加する。現在の lockfile では red になり、修正後は `npm audit --audit-level=moderate` が 0 件になることを固定する。GitHub alerts は advisory 単位、npm audit は package/meta-vulnerability 単位で数が違うことを test/doc comment に残す。 | (a) `tests/test-breezing-agent-eval-deps.sh` または同等 test が `benchmarks/breezing-bench/agent-eval` で `npm audit --audit-level=moderate` を実行、(b) current lockfile では `undici` / `minimatch` / `uuid` のいずれかを検出して red になり得る、(c) fixed lockfile では 0 vulnerabilities、(d) test は model key / Docker / full benchmark を要求しない、(e) `bash tests/test-breezing-agent-eval-deps.sh` PASS。Evidence: `bash tests/test-breezing-agent-eval-deps.sh` PASS; audit gate checks lockfile versions, npm audit, no `npx`, and experiment fixture existence. | 78.1.1 | cc:done |
| 78.1.3 | `[Dependency Remediation]` `[lane:gate]` `[tdd:skip:dependency-bump]` `@vercel/agent-eval` を `^0.14.1` へ更新し、npm `overrides` で `undici:^7.24.0`, `minimatch:^10.2.4`, `dockerode.uuid:^11.1.1` を固定する。`dockerode@5` override は fallback とし、primary path では direct owner package の declared `dockerode:^4.0.9` を保つ。 | (a) `benchmarks/breezing-bench/agent-eval/package.json` が current-line direct dependency と scoped overrides を持つ、(b) `package-lock.json` で `@vercel/agent-eval >=0.14.1`, `undici >=7.24.0`, `minimatch >=10.2.4`, `uuid >=11.1.1`、(c) `cd benchmarks/breezing-bench/agent-eval && npm audit --audit-level=moderate` PASS、(d) `./node_modules/.bin/agent-eval --help` または package-manager equivalent PASS、(e) `dockerode@5` fallback を使う場合は why と smoke 差分を research doc に追記。Evidence: lockfile now has `@vercel/agent-eval 0.14.1`, `undici 7.26.0`, `minimatch 10.2.5`, `uuid 11.1.1`; `npm audit --audit-level=moderate` PASS; `./node_modules/.bin/agent-eval --help` PASS. | 78.1.2 | cc:done |
| 78.1.4 | `[Benchmark Smoke Boundary]` `[lane:gate]` `[tdd:required]` dependency update 後の runtime smoke を過大 claim しないため、現行 `eval:vanilla:dry` の `task-01`-`task-10` 参照と実体 `task-11`-`task-20` の mismatch を扱う。修正するなら config/scripts を揃え、修正しないなら replacement smoke を定義して residual risk を残す。 | (a) `npm run eval:vanilla:dry` または replacement dry smoke が dependency update 後に PASS、(b) `experiments/vanilla.ts` / `experiments/breezing.ts` / `experiments/smoke*.ts` の task id が実在 eval と一致、または mismatch を意図的に別 issue 化、(c) model key / Docker / sandbox unavailable は fail と skip を分けて記録、(d) `docs/evidence/dependabot-alerts-2026-05-27.md` に final smoke boundary を追記、(e) dependency closeout PR body に full benchmark 未実行なら理由がある。Evidence: experiment references updated to `task-11`-`task-20`; `npm run eval:smoke:dry`, `npm run eval:vanilla:dry`, and `npm run eval:breezing:dry` PASS; full model/sandbox run remains out of scope. | 78.1.3 | cc:done |
| 78.1.5 | `[Dependabot / CI Automation]` `[lane:gate]` `[tdd:required]` 同じ alert 残り方を再発させないため、npm package-ecosystem entry と CI audit gate を追加する。対象は `/benchmarks/breezing-bench/agent-eval` に限定し、generated benchmark result fixtures や `.claude/worktrees` は Dependabot scope に入れない。 | (a) `.github/dependabot.yml` に `package-ecosystem: "npm"` / `directory: "/benchmarks/breezing-bench/agent-eval"` があり weekly schedule, labels, commit-message を持つ、(b) CI または validation script が agent-eval lockfile変更時に audit gate を走らせる、(c) broad `find` で generated/result/worktree package-lock を誤対象にしない、(d) `bash tests/test-breezing-agent-eval-deps.sh`, `bash tests/validate-plugin.sh`, `bash scripts/ci/check-consistency.sh` PASS。Evidence: `.github/dependabot.yml` has a scoped npm entry; `validate-plugin.yml` runs the audit+dry-smoke gate; focused test, validate-plugin 95/0/0, check-consistency, and actionlint PASS. | 78.1.4 | cc:done |
| 78.1.6 | `[Review / PR / Alert Closeout]` `[lane:release]` `[tdd:skip:external-closeout]` dependency fix を PR 化し、review / CI / GitHub alert closeout を分けて確認する。release は原則不要だが、main merge 後の Dependabot alert API が 0 または今回対象 alert 消滅を返すことを確認する。 | (a) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE、(b) PR body に alert inventory、npm audit result、smoke boundary、Dependabot/CI automation、residual risk がある、(c) branch CI と merge 後 main CI が green、(d) `gh api .../dependabot/alerts -f state=open` で今回対象 10 件が消えている、(e) release/tag を作らない場合は理由を closeout に残す。Evidence: PR #159 merged as `942fa996899179619b1ddeae7745eeedc70a4281`; branch `validate-plugin` run `26490923383`, branch `smoke-install` run `26490923384`, CodeRabbit success; main `validate-plugin` run `26491162442` success; main `scorecard` run `26491162444` success; Dependabot Updates runs `26491164580`, `26491164453`, `26491164490`, `26491164507` success; open Dependabot alerts API returned `0`; target manifest query returned no alerts; release/tag skipped because this is benchmark tooling lockfile remediation, not shipped plugin runtime/distribution metadata. | 78.1.5 | cc:done |

---

## Phase 79: harness-plan team-backed planning gates [P1]

Purpose: `/harness-plan` が単なる Plans.md 作成係に戻らないよう、単発・軽微ではない planning では TeamAgent / サブエージェント、`spec.md` / sub-spec / `Plans.md` 整合、harness-mem / repo memory による車輪の再発明防止、product fit、security fit、works-in-practice を明示 gate にする。手戻りを減らすため、実装プランは「目的から外れていないか」「危険な権限・秘密情報・外部送信・supply chain リスクがないか」「test / smoke / CI / review / release closeout で本当に動くと確認できるか」まで検証してから Required 化する。

Spec delta: `spec.md` と `docs/plans/spec-ssot.md` に Non-trivial planning gate を追加する。新しい product rule は、non-trivial planning では TeamAgent / sub-agent perspectives を使い、使えない場合は `サブエージェント未使用` fallback を明示し、`team_validation_mode` (`not_required_lightweight` / `native` / `subagent` / `manual-pass` / `unavailable`) と Spec / Plans Fit、Memory / Wheel Check、Product Fit、Security Fit、Works In Practice を通過するまで Required にしない。Product / Architecture / Security / QA / Skeptic は perspective であり agent_type 名ではない。Security gate は secret 実読取を要求せず、必要なら Risk Gate として止める。

New Session Start: まず 79.1.1-79.1.3 の diff を確認する。実装は `skills/harness-plan` を SSOT とし、`scripts/sync-skill-mirrors.sh` で `.agents`, Codex, OpenCode mirror に同期する。検証は `bash tests/test-harness-plan-quality.sh`, `bash tests/test-spec-ssot-workflow.sh`, `bash scripts/sync-skill-mirrors.sh --check`, `bash tests/validate-plugin.sh` を最低ラインにする。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 79.1.1 | `[Contract Freeze]` `[lane:gate]` `[tdd:skip:docs-contract]` Non-trivial planning gate を product contract に追加する。`spec.md` / `docs/plans/spec-ssot.md` / `Plans.md` に、TeamAgent / sub-agent 前提、memory wheel check、product/security/works-in-practice gate、fallback 表記を固定する。 | (a) `spec.md` に Non-trivial planning must be team-validated がある、(b) `docs/plans/spec-ssot.md` に TeamAgent / sub-agent perspectives と wheel check がある、(c) `team_validation_mode` と lightweight / non-trivial mode 境界がある、(d) perspective と agent_type が分離されている、(e) secret read を要求せず Risk Gate にする契約がある、(f) `Plans.md` Phase 79 に Spec delta と検証 task がある、(g) `git diff --check` PASS。Evidence: `git diff --check` PASS; `bash tests/test-spec-ssot-workflow.sh` PASS. | 78.1.6 | cc:done |
| 79.1.2 | `[Skill Update]` `[lane:gate]` `[tdd:required]` `harness-plan` skill の標準品質契約を更新し、単発・軽微ではない planning で TeamAgent / サブエージェント検証、`spec.md` / `Plans.md` 整合、harness-mem / repo memory、product fit、security fit、works-in-practice を必須出力にする。 | (a) `skills/harness-plan/SKILL.md` が Non-trivial planning gate を持つ、(b) `references/planning-quality.md` が Step 5.5 実装プラン検証ゲートを持つ、(c) `references/create.md` が Step 3 で product/security/works 観点を要求する、(d) `team_validation_mode` の values と `unavailable` Required 禁止が明記される、(e) Product / Architecture / Security / QA / Skeptic は perspective であって agent_type ではない、(f) subagent unavailable fallback が明記される、(g) secret read を必須にしない、(h) mirror sync 後も `.agents`, Codex, OpenCode で同じ契約が見える。Evidence: `bash scripts/sync-skill-mirrors.sh` 実行; `bash scripts/sync-skill-mirrors.sh --check` PASS. | 79.1.1 | cc:done |
| 79.1.3 | `[Validation / Closeout]` `[lane:gate]` Phase 79 の回帰防止 test を更新して実行する。`harness-plan` が軽微 task 以外で team-backed validation を省略しないこと、Spec / Plans / memory / product / security / works gates を出力契約として持つことを検出する。 | (a) lightweight fixture は `team_validation_mode: not_required_lightweight` で PASS、(b) non-trivial fixture は Spec / Memory / Product / Security / Works gate 欠落で FAIL、(c) OpenCode / Task unavailable fixture は `manual-pass` で PASS、(d) security fixture が `.env` / secrets read を必須行動にしない、(e) `bash tests/test-harness-plan-quality.sh` PASS、(f) `bash tests/test-spec-ssot-workflow.sh` PASS、(g) `bash scripts/sync-skill-mirrors.sh --check` PASS、(h) `bash tests/validate-plugin.sh` PASS、(i) residual risk があれば final / PR body に残る。Evidence: `bash tests/test-harness-plan-quality.sh` PASS; `bash tests/test-spec-ssot-workflow.sh` PASS; `bash scripts/sync-skill-mirrors.sh --check` PASS; `bash tests/validate-plugin.sh` 96/0/0 PASS; `bash scripts/codex-companion.sh review --base origin/main` no actionable issues. | 79.1.2 | cc:done |

---

## Phase 80: Claude Code 2.1.143-2.1.152 + Codex 0.131-0.134 upstream refresh [P1]

Purpose: 2026-05-27 時点の installed/latest は `claude 2.1.152` と `codex-cli 0.134.0` で、`harness-mem versions` でも両方 `up_to_date` と確認した。既存 snapshot は Claude Code `2.1.133-2.1.142` (`docs/upstream-update-snapshot-2026-05-15.md`) と Codex `0.130.0` (`docs/upstream-update-snapshot-2026-05-10.md`) までなので、Claude Code `2.1.143-2.1.152` と Codex `0.131-0.134` を公式 source で棚卸しし、Harness に採るものだけを A/C/P/Reject で実装計画へ落とす。Claude 側の主な候補は `disallowed-tools`, `/reload-skills`, `SessionStart.reloadSkills`, `hookSpecificOutput.sessionTitle`, `MessageDisplay`, `pluginSuggestionMarketplaces`, Auto mode consent 廃止, `/simplify` -> `/code-review` 変更, `claude agents --json`, plugin/sandbox/worktree fix 群。Codex 側の主な候補は `--profile` primary 化, `on-failure` deprecation / granular approval, MCP environment targeting / OAuth, read-only MCP parallel calls, connector schema `$ref`/`$defs`, subagent identity hook input, plugin icon assets, curl/PowerShell installer, managed network proxy, package/release artifact simplification。Feature Table / CHANGELOG 追記だけで終わらせず、snapshot -> adoption plan -> tests -> mirror -> review の gate で閉じる。

Spec delta: `spec.md` に Upstream Tracking Contract を追加した。新しい rule は「Claude Code / Codex update は release note の転記ではなく、local/latest version check、公式 source、dated snapshot、A/C/P/Reject 分類、B=0 原則、Plans/tests/docs/CHANGELOG/review 接続、support-tier gate で扱う」。また upstream 側で auto mode などの opt-in barrier が緩んでも、Harness は専用 phase と test/release note なしに安全 default を緩めない。

team_validation_mode: `subagent`。Product/Architecture/Local-map/Skeptic 視点を subagent で分担し、main run では公式 source、repo SSOT、harness-mem versions、`.claude/memory`、`/Users/tachibanashuuta/.codex/memories/MEMORY.md` を突き合わせた。Codex subagent の一部 release-detail claim は official `rust-v0.134.0` body で確認できないものがあったため、Phase 80 の DoD では「公式 source にない subagent claim は Reject or Unknown」として扱う。

New Session Start: まず 80.1.1 から開始する。実装ではなく `docs/upstream-update-snapshot-2026-05-27.md` の evidence artifact を作り、Claude `2.1.143-2.1.152` / Codex `0.131-0.134` の全 item を A/C/P/Reject に分類する。80.1.2 で security/supply-chain と support claim gate を固定してから、80.1.3 以降の Claude/Codex runtime/docs 変更に入る。release/tag はこの Phase に含めず、release が必要になった場合は別 `harness-release` gate に渡す。

| Task | 内容 | DoD | Depends | Status |
|------|------|-----|---------|--------|
| 80.1.1 | `[Research / Evidence]` `[lane:gate]` `[tdd:skip:research-doc]` Claude Code `2.1.143-2.1.152` と Codex `0.131-0.134` の upstream snapshot を作る。公式 source は Anthropic Claude Code release/changelog、OpenAI Codex release/docs、`harness-mem versions`、local `claude --version` / `codex --version`。 | (a) `docs/upstream-update-snapshot-2026-05-27.md` が作成され、local/latest version、release URL、published_at、observed gap、subagent validation summary を持つ、(b) 全 item が `A` / `C` / `P` / `Reject` に分類され `B: 書いただけ 0 件` の理由がある、(c) Claude `2.1.152` release の `disallowed-tools`, `/reload-skills`, `SessionStart.reloadSkills`, `MessageDisplay`, Auto mode consent 変更を含む、(d) Codex `0.134.0` release の `--profile`, MCP environment/OAuth, read-only MCP parallelism, connector schema, subagent identity, installer docs を含む、(e) official source にない subagent claim は `Unknown` / `Reject` として残す、(f) `git diff --check` PASS | 79.1.3 | cc:完了 |
| 80.1.2 | `[Adoption / Threat Model]` `[lane:gate]` `[tdd:skip:design-doc]` upstream item を Harness に採るかどうかの採用判断を固定する。Claude の `disallowed-tools`, `MessageDisplay`, `SessionStart.reloadSkills`, plugin marketplace suggestion, Auto mode consent 廃止、sandbox/worktree permission fix と、Codex の profile/approval/MCP/supply-chain/runtime smoke を security / supply-chain / support-tier で評価する。 | (a) `docs/upstream-adoption-plan-2026-05-27.md` があり、Adopt / Defer / Reject / Auto-inherit と Why を持つ、(b) `MessageDisplay` は採用する場合も audit付き opt-in 表示補助に限定し、assistant output の黙示改変は禁止、(c) upstream Auto mode consent 廃止を理由に `--auto-mode` default 化や hard deny 緩和をしない、(d) Codex CLI は `internal-compatible`, Codex app は `candidate` のまま support claim gate を維持、(e) `claude-codex-upstream-update` を local-only operator tooling のままにするか配布 surface に昇格するかを決める、(f) `bash tests/test-spec-ssot-workflow.sh` PASS | 80.1.1 | cc:完了 |
| 80.1.3 | `[Claude Runtime / Skill-Hook Adaptation]` `[lane:gate]` `[tdd:required]` Claude Code `2.1.143-2.1.152` に合わせて skill / hook / settings / docs を更新する。候補は `disallowed-tools` audit、`/reload-skills` と `/reload-plugins` の使い分け、`SessionStart.reloadSkills` 同一セッション反映、`hookSpecificOutput.sessionTitle`、`MessageDisplay` opt-in policy、`/simplify` references の `/code-review --fix` 置換、`claude agents --json` diagnostic usage、plugin validation強化。 | (a) `skills/*`, `agents/*`, `.claude-plugin/hooks.json`, `hooks/hooks.json`, `templates/claude/settings.security.json.template`, docs の対象箇所が更新される、(b) `allowed-tools` を restriction と誤解している説明があれば `disallowed-tools` / permission deny へ直す、(c) `/code-review --fix` は working-tree write を伴うため自動DoDには入れず operator-local spike として扱う、(d) `tests/test-claude-upstream-integration.sh` が Phase 80 Claude items を検出、(e) `bash tests/test-claude-upstream-integration.sh` と `bash tests/validate-plugin.sh` PASS | 80.1.2 | cc:完了 |
| 80.1.4 | `[Codex Native Config / Companion Adaptation]` `[lane:gate]` `[tdd:required]` Codex `0.131-0.134` と OpenAI docs に合わせて Codex setup / companion / config / docs を更新する。候補は curl/PowerShell installer docs、`--profile` primary 化、`on-failure` deprecation、granular approval、explicit sandbox flags、MCP environment/OAuth、read-only MCP parallelism、plugin icon asset sharing、AGENTS/skills discovery、structured `codex exec --output-schema`。 | (a) `scripts/setup-codex.sh`, `scripts/codex-companion.sh`, `codex/.codex/config.toml`, `.codex-plugin/plugin.json`, `codex/AGENTS.md`, docs が current OpenAI docs と矛盾しない、(b) `--full-auto` / raw `codex exec` を default route に戻さず companion policy を維持、(c) `on-failure` を新規 docs/config で推奨しない、(d) `tests/test-codex-package.sh`, `tests/test-codex-plugin-adapter.sh`, relevant companion tests PASS、(e) `bash scripts/sync-skill-mirrors.sh --check` PASS | 80.1.2 | cc:完了 |
| 80.1.5 | `[SSOT / Mirror / Support Claim Sync]` `[lane:gate]` `[tdd:required]` Phase 80 の変更を repo SSOT と public support boundary に反映する。`spec.md` status、Phase 72 TODO の残件、Feature Table、CHANGELOG、tool capability matrix、README support wording、skill mirrors を棚卸しする。 | (a) `docs/CLAUDE-feature-table.md` に Phase 80 row がある、(b) `CHANGELOG.md` `[Unreleased]` に user-facing upstream follow-up が記録される、(c) `spec.md` / `Plans.md` / `docs/plans/spec-ssot.md` の phase status と残件が矛盾しない、(d) `docs/tool-capability-matrix.md` と README/README_ja が Codex CLI/App support tier を過大 claim しない、(e) `bash tests/test-tool-capability-matrix.sh`, `bash tests/test-support-claim-wording.sh`, `bash scripts/sync-skill-mirrors.sh --check` PASS | 80.1.3, 80.1.4 | cc:完了 |
| 80.1.6 | `[Validation / Review / PR Closeout]` `[lane:release]` `[tdd:skip:external-closeout]` Phase 80 全体の validation と review closeout。implementation green、snapshot/adoption docs、support boundary、subagent findings の採否を evidence pack にする。release/tag は作らない。 | (a) `git diff --check`, `bash tests/test-claude-upstream-integration.sh`, `bash tests/test-harness-plan-quality.sh`, `bash tests/test-spec-ssot-workflow.sh`, `bash scripts/sync-skill-mirrors.sh --check`, `bash tests/test-codex-package.sh`, `bash tests/validate-plugin.sh` PASS、(b) `bash scripts/codex-companion.sh review --base "${TASK_BASE_REF}"` APPROVE or auth failure + manual-pass fallback evidence、(c) PR body includes official sources, A/C/P/Reject summary, security/supply-chain decisions, tests, support-tier boundary, residual risk、(d) no tag/release action is triggered、(e) Plans markers update after evidence is complete | 80.1.5 | cc:完了 |
